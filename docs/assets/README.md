# Documentation images

| File | Source | How it was made |
| --- | --- | --- |
| `freewrl-logo-128.png` | `freex3d/icons/X3DFreeWRLIcon.icns` (FreeWRL project artwork, in the upstream tree; identical to `OSX_gui/FreeWRL-Desktop/X3DFreeWRLIcon.icns`) | `iconutil -c iconset` on the `.icns`, then the unmodified 128×128 image (`icon_128x128.png`), the largest size in the file |

The image is not redrawn, recolored, or scaled. It is covered by the same
license as the rest of FreeWRL.

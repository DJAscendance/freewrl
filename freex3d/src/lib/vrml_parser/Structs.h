/*

  Structs.h: generated by VRMLC.pm. DO NOT MODIFY, MODIFY VRMLC.pm INSTEAD.

  Code here comes almost verbatim from VRMLC.pm

*/

/****************************************************************************
    This file is part of the FreeWRL/FreeX3D Distribution.

    Copyright 2009 CRC Canada. (http://www.crc.gc.ca)

    FreeWRL/FreeX3D is free software: you can redistribute it and/or modify
    it under the terms of the GNU Lesser Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    FreeWRL/FreeX3D is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.

    You should have received a copy of the GNU General Public License
    along with FreeWRL/FreeX3D.  If not, see <http://www.gnu.org/licenses/>.
****************************************************************************/



#ifndef __FREEWRL_STRUCTS_H__
#define __FREEWRL_STRUCTS_H__

#include <system_threads.h>
struct point_XYZ {GLDOUBLE x,y,z;};
struct orient_XYZA {GLDOUBLE x,y,z,a;};

struct X3D_Virt {
	void (*prep)(void *);
	void (*rend)(void *);
	void (*children)(void *);
	void (*fin)(void *);
	void (*rendray)(void *);
	void (*mkpolyrep)(void *);
	void (*proximity)(void *);
	void (*other)(void *);
	void (*collision)(void *);
	void (*compile)(void *, void *, void *, void *, void *, void *);
};

/* a string is stored as a pointer, and a length of that mallocd pointer */
struct Uni_String {
	int len;
	char * strptr;
	int touched;
};


/* Internal representation of IndexedFaceSet, Text, Extrusion & ElevationGrid:
 * set of triangles.
 * done so that we get rid of concave polygons etc.
 */
struct X3D_PolyRep { /* Currently a bit wasteful, because copying */
	int irep_change;
	int ccw;	/* ccw field for single faced structures */
	int ntri; /* number of triangles */
	int streamed;	/* is this done the streaming pass? */

	/* indicies for arrays. OpenGL ES 2.0 - unsigned short for the DrawArrays call */
	GLuint *cindex;   /* triples (per triangle) */
	GLuint *colindex;   /* triples (per triangle) */
	GLuint *norindex;
	GLuint *tcindex; /* triples or null */
	ushort *tri_indices;
	ushort *wire_indices;

	float *actualCoord; /* triples (per point) */
	float *actualFog; /* float (per point) */
	float *color; /* triples or null */
	float *normal; /* triples or null */
	float *flat_normal; /*triples or null*/
	int last_normal_type; /* 0=regular 1=flat last normal type we put in the vbo normal buffer */
	int last_index_type; /* 0=regular 1=wire last vertex index type we put in the vbo index buffer */
	float *GeneratedTexCoords[4];	/* triples (per triangle) of texture coords if there is no texCoord node */
	int ntexdim[4];  /* number of texture coordinate dimensions, normally 2 xy, 3 xyz, 4 xyzw */
	int ntcoord;		/* number of multitextureCoordinates */
	int tcoordtype; /* type of texture coord node - is this a NODE_TextureCoordGenerator... */
	int texgentype; /* if we do have a TextureCoordinateGenerator, what "TCGT_XXX" type is it? */
	GLfloat minVals[3];		/* for collision and default texture coord generation */
	GLfloat maxVals[3];		/* for collision and default texture coord generation */
	int isRGBAcolorNode;		/* color was originally an RGBA, DO NOT re-write if transparency changes */
	GLuint VBO_buffers[VBO_COUNT];		/* VBO indexen */
};

/* definitions to help scanning values in from a string */ 
#define SCANTONUMBER(value) while (isspace(*value) || (*value==',')) value++; 
#define SCANTOSTRING(value) while (isspace(*value) || (*value==',')) value++; 
#define OLDSCANTOSTRING(value) while ((*value==' ') || (*value==',')) value++; 
#define ISSTARTNUMBER(value) (isdigit(*value) \
		|| (*value == '+') || (*value == '-')) 
#define SCANPASTFLOATNUMBER(value) while (isdigit(*value) \
		|| (*value == '.') || \
		(*value == 'E') || (*value == 'e') || (*value == '+') || (*value == '-')) value++; 
#define SCANPASTINTNUMBER(value) if (isdigit(*value) || (*value == '-') || (*value == '+')) value++; \
		while (isdigit(*value) || \
		(*value == 'x') || (*value == 'X') ||\
		((*value >='a') && (*value <='f')) || \
		((*value >='A') && (*value <='F')) || \
		(*value == '-') || (*value == '+')) value++; 
/* Data type for index into ID-table. */
typedef int indexT;

#define NODE_AcousticProperties	0
#define NODE_AnalyserNode	1
#define NODE_Anchor	2
#define NODE_Appearance	3
#define NODE_Arc2D	4
#define NODE_ArcClose2D	5
#define NODE_AudioBuffer	6
#define NODE_AudioBufferSourceNode	7
#define NODE_AudioClip	8
#define NODE_AudioContext	9
#define NODE_AudioDestinationNode	10
#define NODE_AudioListener	11
#define NODE_AudioParam	12
#define NODE_BackdropBackground	13
#define NODE_Background	14
#define NODE_BallJoint	15
#define NODE_Billboard	16
#define NODE_BiquadFilterNode	17
#define NODE_BlendedVolumeStyle	18
#define NODE_BooleanFilter	19
#define NODE_BooleanSequencer	20
#define NODE_BooleanToggle	21
#define NODE_BooleanTrigger	22
#define NODE_BoundaryEnhancementVolumeStyle	23
#define NODE_BoundedPhysicsModel	24
#define NODE_Box	25
#define NODE_CADAssembly	26
#define NODE_CADFace	27
#define NODE_CADLayer	28
#define NODE_CADPart	29
#define NODE_CalibratedCameraSensor	30
#define NODE_CartoonVolumeStyle	31
#define NODE_ChannelMergerNode	32
#define NODE_ChannelSplitterNode	33
#define NODE_Circle2D	34
#define NODE_ClipPlane	35
#define NODE_CollidableOffset	36
#define NODE_CollidableShape	37
#define NODE_Collision	38
#define NODE_CollisionCollection	39
#define NODE_CollisionSensor	40
#define NODE_CollisionSpace	41
#define NODE_Color	42
#define NODE_ColorChaser	43
#define NODE_ColorDamper	44
#define NODE_ColorInterpolator	45
#define NODE_ColorRGBA	46
#define NODE_ComposedCubeMapTexture	47
#define NODE_ComposedShader	48
#define NODE_ComposedTexture3D	49
#define NODE_ComposedVolumeStyle	50
#define NODE_CompositeVolumeStyle	51
#define NODE_Cone	52
#define NODE_ConeEmitter	53
#define NODE_Contact	54
#define NODE_Contour2D	55
#define NODE_ContourPolyline2D	56
#define NODE_ConvolverNode	57
#define NODE_Coordinate	58
#define NODE_CoordinateChaser	59
#define NODE_CoordinateDamper	60
#define NODE_CoordinateDouble	61
#define NODE_CoordinateInterpolator	62
#define NODE_CoordinateInterpolator2D	63
#define NODE_Cylinder	64
#define NODE_CylinderSensor	65
#define NODE_DISEntityManager	66
#define NODE_DISEntityTypeMapping	67
#define NODE_DelayNode	68
#define NODE_DirectionalLight	69
#define NODE_Disk2D	70
#define NODE_DoubleAxisHingeJoint	71
#define NODE_DynamicsCompressorNode	72
#define NODE_EaseInEaseOut	73
#define NODE_EdgeEnhancementVolumeStyle	74
#define NODE_Effect	75
#define NODE_EffectPart	76
#define NODE_ElevationGrid	77
#define NODE_EnvironmentLight	78
#define NODE_EspduTransform	79
#define NODE_ExplosionEmitter	80
#define NODE_Extrusion	81
#define NODE_FillProperties	82
#define NODE_FloatVertexAttribute	83
#define NODE_Fog	84
#define NODE_FogCoordinate	85
#define NODE_FontStyle	86
#define NODE_ForcePhysicsModel	87
#define NODE_GainNode	88
#define NODE_GeneratedCubeMapTexture	89
#define NODE_GeoConvert	90
#define NODE_GeoCoordinate	91
#define NODE_GeoElevationGrid	92
#define NODE_GeoLOD	93
#define NODE_GeoLocation	94
#define NODE_GeoMetadata	95
#define NODE_GeoOrigin	96
#define NODE_GeoPlanet	97
#define NODE_GeoPositionInterpolator	98
#define NODE_GeoProximitySensor	99
#define NODE_GeoTouchSensor	100
#define NODE_GeoTransform	101
#define NODE_GeoViewpoint	102
#define NODE_Group	103
#define NODE_HAnimDisplacer	104
#define NODE_HAnimHumanoid	105
#define NODE_HAnimJoint	106
#define NODE_HAnimMotion	107
#define NODE_HAnimMotionData	108
#define NODE_HAnimMotionDataFile	109
#define NODE_HAnimMotionPlay	110
#define NODE_HAnimSegment	111
#define NODE_HAnimSite	112
#define NODE_ImageBackdropBackground	113
#define NODE_ImageCubeMapTexture	114
#define NODE_ImageTexture	115
#define NODE_ImageTexture3D	116
#define NODE_IndexedFaceSet	117
#define NODE_IndexedLineSet	118
#define NODE_IndexedQuadSet	119
#define NODE_IndexedTriangleFanSet	120
#define NODE_IndexedTriangleSet	121
#define NODE_IndexedTriangleStripSet	122
#define NODE_Inline	123
#define NODE_IntegerSequencer	124
#define NODE_IntegerTrigger	125
#define NODE_IsoSurfaceVolumeData	126
#define NODE_KeySensor	127
#define NODE_LOD	128
#define NODE_Layer	129
#define NODE_LayerSet	130
#define NODE_Layout	131
#define NODE_LayoutGroup	132
#define NODE_LayoutLayer	133
#define NODE_LinePickSensor	134
#define NODE_LineProperties	135
#define NODE_LineSensor	136
#define NODE_LineSet	137
#define NODE_LoadSensor	138
#define NODE_LocalFog	139
#define NODE_Material	140
#define NODE_Matrix3VertexAttribute	141
#define NODE_Matrix4VertexAttribute	142
#define NODE_MediaElementAudioSourceNode	143
#define NODE_MediaStreamAudioDestinationNode	144
#define NODE_MediaStreamTrack	145
#define NODE_MetadataBoolean	146
#define NODE_MetadataDouble	147
#define NODE_MetadataFloat	148
#define NODE_MetadataInteger	149
#define NODE_MetadataMFBool	150
#define NODE_MetadataMFColor	151
#define NODE_MetadataMFColorRGBA	152
#define NODE_MetadataMFDouble	153
#define NODE_MetadataMFFloat	154
#define NODE_MetadataMFInt32	155
#define NODE_MetadataMFMatrix3d	156
#define NODE_MetadataMFMatrix3f	157
#define NODE_MetadataMFMatrix4d	158
#define NODE_MetadataMFMatrix4f	159
#define NODE_MetadataMFNode	160
#define NODE_MetadataMFRotation	161
#define NODE_MetadataMFString	162
#define NODE_MetadataMFTime	163
#define NODE_MetadataMFVec2d	164
#define NODE_MetadataMFVec2f	165
#define NODE_MetadataMFVec3d	166
#define NODE_MetadataMFVec3f	167
#define NODE_MetadataMFVec4d	168
#define NODE_MetadataMFVec4f	169
#define NODE_MetadataSFBool	170
#define NODE_MetadataSFColor	171
#define NODE_MetadataSFColorRGBA	172
#define NODE_MetadataSFDouble	173
#define NODE_MetadataSFFloat	174
#define NODE_MetadataSFImage	175
#define NODE_MetadataSFInt32	176
#define NODE_MetadataSFMatrix3d	177
#define NODE_MetadataSFMatrix3f	178
#define NODE_MetadataSFMatrix4d	179
#define NODE_MetadataSFMatrix4f	180
#define NODE_MetadataSFNode	181
#define NODE_MetadataSFRotation	182
#define NODE_MetadataSFString	183
#define NODE_MetadataSFTime	184
#define NODE_MetadataSFVec2d	185
#define NODE_MetadataSFVec2f	186
#define NODE_MetadataSFVec3d	187
#define NODE_MetadataSFVec3f	188
#define NODE_MetadataSFVec4d	189
#define NODE_MetadataSFVec4f	190
#define NODE_MetadataSet	191
#define NODE_MetadataString	192
#define NODE_MotorJoint	193
#define NODE_MovieTexture	194
#define NODE_MultiTexture	195
#define NODE_MultiTextureCoordinate	196
#define NODE_MultiTextureTransform	197
#define NODE_NavigationInfo	198
#define NODE_Normal	199
#define NODE_NormalInterpolator	200
#define NODE_NurbsCurve	201
#define NODE_NurbsCurve2D	202
#define NODE_NurbsOrientationInterpolator	203
#define NODE_NurbsPatchSurface	204
#define NODE_NurbsPositionInterpolator	205
#define NODE_NurbsSet	206
#define NODE_NurbsSurfaceInterpolator	207
#define NODE_NurbsSweptSurface	208
#define NODE_NurbsSwungSurface	209
#define NODE_NurbsTextureCoordinate	210
#define NODE_NurbsTrimmedSurface	211
#define NODE_OSC_Sensor	212
#define NODE_OpacityMapVolumeStyle	213
#define NODE_OrientationChaser	214
#define NODE_OrientationDamper	215
#define NODE_OrientationInterpolator	216
#define NODE_OrthoViewpoint	217
#define NODE_OscillatorNode	218
#define NODE_PackagedShader	219
#define NODE_PannerNode	220
#define NODE_ParticleSystem	221
#define NODE_PeriodicWave	222
#define NODE_PhysicalMaterial	223
#define NODE_PickableGroup	224
#define NODE_PixelTexture	225
#define NODE_PixelTexture3D	226
#define NODE_PlaneSensor	227
#define NODE_PointEmitter	228
#define NODE_PointLight	229
#define NODE_PointPickSensor	230
#define NODE_PointProperties	231
#define NODE_PointSensor	232
#define NODE_PointSet	233
#define NODE_Polyline2D	234
#define NODE_PolylineEmitter	235
#define NODE_Polypoint2D	236
#define NODE_PositionChaser	237
#define NODE_PositionChaser2D	238
#define NODE_PositionDamper	239
#define NODE_PositionDamper2D	240
#define NODE_PositionInterpolator	241
#define NODE_PositionInterpolator2D	242
#define NODE_PrimitivePickSensor	243
#define NODE_ProgramShader	244
#define NODE_ProjectionVolumeStyle	245
#define NODE_Proto	246
#define NODE_ProximitySensor	247
#define NODE_Pyramid	248
#define NODE_QuadSet	249
#define NODE_ReceiverPdu	250
#define NODE_Rectangle2D	251
#define NODE_RigidBody	252
#define NODE_RigidBodyCollection	253
#define NODE_ScalarChaser	254
#define NODE_ScalarDamper	255
#define NODE_ScalarInterpolator	256
#define NODE_ScreenFontStyle	257
#define NODE_ScreenGroup	258
#define NODE_Script	259
#define NODE_SegmentedVolumeData	260
#define NODE_ShadedVolumeStyle	261
#define NODE_ShaderPart	262
#define NODE_ShaderProgram	263
#define NODE_Shape	264
#define NODE_SignalPdu	265
#define NODE_SilhouetteEnhancementVolumeStyle	266
#define NODE_SingleAxisHingeJoint	267
#define NODE_SliderJoint	268
#define NODE_Sound	269
#define NODE_Sphere	270
#define NODE_SphereSensor	271
#define NODE_SplinePositionInterpolator	272
#define NODE_SplinePositionInterpolator2D	273
#define NODE_SplineScalarInterpolator	274
#define NODE_SpotLight	275
#define NODE_SquadOrientationInterpolator	276
#define NODE_StaticGroup	277
#define NODE_StringSensor	278
#define NODE_SurfaceEmitter	279
#define NODE_Switch	280
#define NODE_Teapot	281
#define NODE_TexCoordChaser2D	282
#define NODE_TexCoordDamper2D	283
#define NODE_Text	284
#define NODE_TextureBackground	285
#define NODE_TextureCoordinate	286
#define NODE_TextureCoordinate3D	287
#define NODE_TextureCoordinate4D	288
#define NODE_TextureCoordinateGenerator	289
#define NODE_TextureProjectorParallel	290
#define NODE_TextureProjectorPerspective	291
#define NODE_TextureProperties	292
#define NODE_TextureTransform	293
#define NODE_TextureTransform3D	294
#define NODE_TextureTransformMatrix3D	295
#define NODE_TimeSensor	296
#define NODE_TimeTrigger	297
#define NODE_ToneMappedVolumeStyle	298
#define NODE_TouchSensor	299
#define NODE_TrackingSensor	300
#define NODE_Transform	301
#define NODE_TransformSensor	302
#define NODE_TransmitterPdu	303
#define NODE_TriangleFanSet	304
#define NODE_TriangleSet	305
#define NODE_TriangleSet2D	306
#define NODE_TriangleStripSet	307
#define NODE_TwoSidedMaterial	308
#define NODE_UniversalJoint	309
#define NODE_UnlitMaterial	310
#define NODE_Viewpoint	311
#define NODE_ViewpointGroup	312
#define NODE_Viewport	313
#define NODE_VisibilitySensor	314
#define NODE_VolumeData	315
#define NODE_VolumeEmitter	316
#define NODE_VolumePickSensor	317
#define NODE_WaveShaperNode	318
#define NODE_WindPhysicsModel	319
#define NODE_WorldInfo	320


/* Table of built-in fieldIds */
extern const char *FIELDNAMES[];
extern const int FIELDNAMES_COUNT;
#define FIELDNAMES_FIFOsize	0
#define FIELDNAMES_Q	1
#define FIELDNAMES__CPU_Routes_out	2
#define FIELDNAMES__GPU_Routes_out	3
#define FIELDNAMES__JT	4
#define FIELDNAMES__NV	5
#define FIELDNAMES__OK	6
#define FIELDNAMES__PVI	7
#define FIELDNAMES__PVW	8
#define FIELDNAMES__T0	9
#define FIELDNAMES__T1	10
#define FIELDNAMES___DEFnames	11
#define FIELDNAMES___EXPORTS	12
#define FIELDNAMES___GC	13
#define FIELDNAMES___IMPORTS	14
#define FIELDNAMES___IS	15
#define FIELDNAMES___ROUTES	16
#define FIELDNAMES___Samples	17
#define FIELDNAMES___SphereIndxVBO	18
#define FIELDNAMES___StartLoadTime	19
#define FIELDNAMES___VBO	20
#define FIELDNAMES___afterPound	21
#define FIELDNAMES___autoOffset	22
#define FIELDNAMES___backTexture	23
#define FIELDNAMES___botpoints	24
#define FIELDNAMES___bottomTexture	25
#define FIELDNAMES___child1Node	26
#define FIELDNAMES___child2Node	27
#define FIELDNAMES___child3Node	28
#define FIELDNAMES___child4Node	29
#define FIELDNAMES___childloadstatus	30
#define FIELDNAMES___children	31
#define FIELDNAMES___colours	32
#define FIELDNAMES___coneTriangles	33
#define FIELDNAMES___coneVBO	34
#define FIELDNAMES___counts	35
#define FIELDNAMES___ctex	36
#define FIELDNAMES___ctflag	37
#define FIELDNAMES___cylinderTriangles	38
#define FIELDNAMES___cylinderVBO	39
#define FIELDNAMES___do_anything	40
#define FIELDNAMES___do_center	41
#define FIELDNAMES___do_rotation	42
#define FIELDNAMES___do_scale	43
#define FIELDNAMES___do_scaleO	44
#define FIELDNAMES___do_trans	45
#define FIELDNAMES___externProtoDeclares	46
#define FIELDNAMES___finishedloading	47
#define FIELDNAMES___fogScale	48
#define FIELDNAMES___fogType	49
#define FIELDNAMES___frac	50
#define FIELDNAMES___frontTexture	51
#define FIELDNAMES___fw_movie	52
#define FIELDNAMES___geoSystem	53
#define FIELDNAMES___highest	54
#define FIELDNAMES___hit	55
#define FIELDNAMES___ifsnode	56
#define FIELDNAMES___inRange	57
#define FIELDNAMES___inittime	58
#define FIELDNAMES___isX3D	59
#define FIELDNAMES___lastlocation	60
#define FIELDNAMES___lasttime	61
#define FIELDNAMES___leftTexture	62
#define FIELDNAMES___level	63
#define FIELDNAMES___linerep	64
#define FIELDNAMES___loadResource	65
#define FIELDNAMES___loading	66
#define FIELDNAMES___loadstatus	67
#define FIELDNAMES___localOrient	68
#define FIELDNAMES___lowest	69
#define FIELDNAMES___movedCoords	70
#define FIELDNAMES___movedOrientation	71
#define FIELDNAMES___movedOrientationB	72
#define FIELDNAMES___movedPosition	73
#define FIELDNAMES___movedValue	74
#define FIELDNAMES___movedgd	75
#define FIELDNAMES___nodes	76
#define FIELDNAMES___normals	77
#define FIELDNAMES___numPoints	78
#define FIELDNAMES___occludeCheckCount	79
#define FIELDNAMES___offsetOrient	80
#define FIELDNAMES___oldChildren	81
#define FIELDNAMES___oldEnabled	82
#define FIELDNAMES___oldFieldOfView	83
#define FIELDNAMES___oldGeoCenter	84
#define FIELDNAMES___oldHeadlight	85
#define FIELDNAMES___oldJump	86
#define FIELDNAMES___oldKeyPtr	87
#define FIELDNAMES___oldKeyValuePtr	88
#define FIELDNAMES___oldMFString	89
#define FIELDNAMES___oldSFString	90
#define FIELDNAMES___oldSize	91
#define FIELDNAMES___old_anchorPoint	92
#define FIELDNAMES___old_angularVelocity	93
#define FIELDNAMES___old_axis	94
#define FIELDNAMES___old_axis1	95
#define FIELDNAMES___old_axis1Angle	96
#define FIELDNAMES___old_axis2	97
#define FIELDNAMES___old_axis2Angle	98
#define FIELDNAMES___old_axis3Angle	99
#define FIELDNAMES___old_body1	100
#define FIELDNAMES___old_body2	101
#define FIELDNAMES___old_centerOfMass	102
#define FIELDNAMES___old_finiteRotationAxis	103
#define FIELDNAMES___old_linearVelocity	104
#define FIELDNAMES___old_motor1Axis	105
#define FIELDNAMES___old_motor2Axis	106
#define FIELDNAMES___old_motor3Axis	107
#define FIELDNAMES___old_orientation	108
#define FIELDNAMES___old_position	109
#define FIELDNAMES___oldgcCoords	110
#define FIELDNAMES___oldgeoCoords	111
#define FIELDNAMES___oldload	112
#define FIELDNAMES___oldmetadata	113
#define FIELDNAMES___oldurl	114
#define FIELDNAMES___parentProto	115
#define FIELDNAMES___pindices	116
#define FIELDNAMES___planets	117
#define FIELDNAMES___points	118
#define FIELDNAMES___position	119
#define FIELDNAMES___protoDeclares	120
#define FIELDNAMES___protoDef	121
#define FIELDNAMES___protoFlags	122
#define FIELDNAMES___prototype	123
#define FIELDNAMES___proxNode	124
#define FIELDNAMES___quadcount	125
#define FIELDNAMES___regenSubTextures	126
#define FIELDNAMES___rightTexture	127
#define FIELDNAMES___rootUrl	128
#define FIELDNAMES___rooturlloadstatus	129
#define FIELDNAMES___rotyup	130
#define FIELDNAMES___scriptObj	131
#define FIELDNAMES___scripts	132
#define FIELDNAMES___segCount	133
#define FIELDNAMES___sibAffectors	134
#define FIELDNAMES___sidepoints	135
#define FIELDNAMES___simpleDisk	136
#define FIELDNAMES___solid	137
#define FIELDNAMES___sourceNumber	138
#define FIELDNAMES___specversion	139
#define FIELDNAMES___starts	140
#define FIELDNAMES___style16	141
#define FIELDNAMES___styleEnd	142
#define FIELDNAMES___styleStart	143
#define FIELDNAMES___subTextures	144
#define FIELDNAMES___subcontexts	145
#define FIELDNAMES___t1	146
#define FIELDNAMES___t2	147
#define FIELDNAMES___t3	148
#define FIELDNAMES___texCoords	149
#define FIELDNAMES___texture	150
#define FIELDNAMES___textureTableIndex	151
#define FIELDNAMES___textureright	152
#define FIELDNAMES___topTexture	153
#define FIELDNAMES___transparency	154
#define FIELDNAMES___typename	155
#define FIELDNAMES___unitlengthfactor	156
#define FIELDNAMES___vertArr	157
#define FIELDNAMES___vertIndx	158
#define FIELDNAMES___vertexCount	159
#define FIELDNAMES___vertices	160
#define FIELDNAMES___visible	161
#define FIELDNAMES___wireindices	162
#define FIELDNAMES___wireindicesVBO	163
#define FIELDNAMES___xcolours	164
#define FIELDNAMES___xfog	165
#define FIELDNAMES___xparams	166
#define FIELDNAMES__a0	167
#define FIELDNAMES__align	168
#define FIELDNAMES__amb	169
#define FIELDNAMES__angularVelocity	170
#define FIELDNAMES__appliedParameters	171
#define FIELDNAMES__appliedParametersMask	172
#define FIELDNAMES__attenuation	173
#define FIELDNAMES__backMaterial	174
#define FIELDNAMES__bboxCenter	175
#define FIELDNAMES__bboxSize	176
#define FIELDNAMES__body	177
#define FIELDNAMES__boxtris	178
#define FIELDNAMES__buffer	179
#define FIELDNAMES__bufferendtime	180
#define FIELDNAMES__change_count	181
#define FIELDNAMES__channelcount	182
#define FIELDNAMES__channels	183
#define FIELDNAMES__child	184
#define FIELDNAMES__class	185
#define FIELDNAMES__col	186
#define FIELDNAMES__colormode	187
#define FIELDNAMES__colourSize	188
#define FIELDNAMES__coloursVBO	189
#define FIELDNAMES__controlPoint	190
#define FIELDNAMES__coordIndex	191
#define FIELDNAMES__csensor	192
#define FIELDNAMES__destination	193
#define FIELDNAMES__dir	194
#define FIELDNAMES__donethispass	195
#define FIELDNAMES__dsock	196
#define FIELDNAMES__enabled	197
#define FIELDNAMES__fbohandles	198
#define FIELDNAMES__floatInpFIFO	199
#define FIELDNAMES__floatOutFIFO	200
#define FIELDNAMES__fogcoordVBO	201
#define FIELDNAMES__forceout	202
#define FIELDNAMES__frameSpeed	203
#define FIELDNAMES__framevalues	204
#define FIELDNAMES__fvalues	205
#define FIELDNAMES__geom	206
#define FIELDNAMES__geomIdentityTransform	207
#define FIELDNAMES__geometryType	208
#define FIELDNAMES__gridHeight	209
#define FIELDNAMES__group	210
#define FIELDNAMES__hatchScale	211
#define FIELDNAMES__ifs	212
#define FIELDNAMES__index	213
#define FIELDNAMES__initialRotation	214
#define FIELDNAMES__initialTranslation	215
#define FIELDNAMES__initialized	216
#define FIELDNAMES__initializedOnce	217
#define FIELDNAMES__input	218
#define FIELDNAMES__int32InpFIFO	219
#define FIELDNAMES__int32OutFIFO	220
#define FIELDNAMES__isActive	221
#define FIELDNAMES__isScreen	222
#define FIELDNAMES__joint	223
#define FIELDNAMES__keyVBO	224
#define FIELDNAMES__keyValueVBO	225
#define FIELDNAMES__knot	226
#define FIELDNAMES__knotrange	227
#define FIELDNAMES__lastEnabled	228
#define FIELDNAMES__lastenabled	229
#define FIELDNAMES__lastframetime	230
#define FIELDNAMES__lastp0	231
#define FIELDNAMES__lastp0time	232
#define FIELDNAMES__lastr0	233
#define FIELDNAMES__lasttick	234
#define FIELDNAMES__lasttime	235
#define FIELDNAMES__layerId	236
#define FIELDNAMES__loc	237
#define FIELDNAMES__ltex	238
#define FIELDNAMES__material	239
#define FIELDNAMES__method	240
#define FIELDNAMES__motor1	241
#define FIELDNAMES__motor2	242
#define FIELDNAMES__needs_gradient	243
#define FIELDNAMES__njoints	244
#define FIELDNAMES__normkey	245
#define FIELDNAMES__normkeyValue	246
#define FIELDNAMES__npoints	247
#define FIELDNAMES__nseg	248
#define FIELDNAMES__offsetUnits	249
#define FIELDNAMES__oldState	250
#define FIELDNAMES__oldhitNormal	251
#define FIELDNAMES__oldhitPoint	252
#define FIELDNAMES__oldhitTexCoord	253
#define FIELDNAMES__oldisActive	254
#define FIELDNAMES__oldpickTarget	255
#define FIELDNAMES__oldpickedGeometry	256
#define FIELDNAMES__oldpickedPoint	257
#define FIELDNAMES__oldrotation	258
#define FIELDNAMES__oldtrackPoint	259
#define FIELDNAMES__oldtranslation	260
#define FIELDNAMES__orientation	261
#define FIELDNAMES__origCoords	262
#define FIELDNAMES__origNormalizedPoint	263
#define FIELDNAMES__origNorms	264
#define FIELDNAMES__origPoint	265
#define FIELDNAMES__p	266
#define FIELDNAMES__p0	267
#define FIELDNAMES__parentResource	268
#define FIELDNAMES__particles	269
#define FIELDNAMES__patch	270
#define FIELDNAMES__pduchange_collision	271
#define FIELDNAMES__pduchange_create	272
#define FIELDNAMES__pduchange_detonation	273
#define FIELDNAMES__pduchange_em_info	274
#define FIELDNAMES__pduchange_es	275
#define FIELDNAMES__pduchange_fire	276
#define FIELDNAMES__pduchange_networksensor	277
#define FIELDNAMES__pduchange_receiver	278
#define FIELDNAMES__pduchange_remove	279
#define FIELDNAMES__pduchange_signal	280
#define FIELDNAMES__pduchange_transmitter	281
#define FIELDNAMES__phaseFunction	282
#define FIELDNAMES__pointMethod	283
#define FIELDNAMES__pointsVBO	284
#define FIELDNAMES__portions	285
#define FIELDNAMES__position	286
#define FIELDNAMES__prepped_planet	287
#define FIELDNAMES__previousvalue	288
#define FIELDNAMES__r0	289
#define FIELDNAMES__radius	290
#define FIELDNAMES__registered	291
#define FIELDNAMES__remainder	292
#define FIELDNAMES__resetRelativeHeight	293
#define FIELDNAMES__retrievedURLData	294
#define FIELDNAMES__rotationAngle	295
#define FIELDNAMES__scale	296
#define FIELDNAMES__scaleMode	297
#define FIELDNAMES__screendata	298
#define FIELDNAMES__segs	299
#define FIELDNAMES__selected	300
#define FIELDNAMES__sent	301
#define FIELDNAMES__shaderLoadThread	302
#define FIELDNAMES__shaderUserDefinedFields	303
#define FIELDNAMES__shaderUserNumber	304
#define FIELDNAMES__shaderflags_base	305
#define FIELDNAMES__shaderflags_effects	306
#define FIELDNAMES__shaderflags_usershaders	307
#define FIELDNAMES__sideVBO	308
#define FIELDNAMES__sizeUnits	309
#define FIELDNAMES__smoothingCount	310
#define FIELDNAMES__smoothingDelta	311
#define FIELDNAMES__sortedChildren	312
#define FIELDNAMES__space	313
#define FIELDNAMES__startTime	314
#define FIELDNAMES__status	315
#define FIELDNAMES__steptime	316
#define FIELDNAMES__stringInpFIFO	317
#define FIELDNAMES__stringOutFIFO	318
#define FIELDNAMES__t	319
#define FIELDNAMES__takefirstinput	320
#define FIELDNAMES__talkToNodes	321
#define FIELDNAMES__tau	322
#define FIELDNAMES__tris	323
#define FIELDNAMES__tscale	324
#define FIELDNAMES__ttex	325
#define FIELDNAMES__type	326
#define FIELDNAMES__uKnot	327
#define FIELDNAMES__upVec	328
#define FIELDNAMES__usingDisk	329
#define FIELDNAMES__v0	330
#define FIELDNAMES__vKnot	331
#define FIELDNAMES__values	332
#define FIELDNAMES__weightFunction1	333
#define FIELDNAMES__weightFunction2	334
#define FIELDNAMES__world	335
#define FIELDNAMES__xyzw	336
#define FIELDNAMES_absorption	337
#define FIELDNAMES_acousticProperties	338
#define FIELDNAMES_actionKeyPress	339
#define FIELDNAMES_actionKeyRelease	340
#define FIELDNAMES_activate	341
#define FIELDNAMES_activeLayer	342
#define FIELDNAMES_addChildren	343
#define FIELDNAMES_addEntities	344
#define FIELDNAMES_addGeometry	345
#define FIELDNAMES_addTrimmingContour	346
#define FIELDNAMES_addedEntities	347
#define FIELDNAMES_address	348
#define FIELDNAMES_align	349
#define FIELDNAMES_alpha	350
#define FIELDNAMES_altKey	351
#define FIELDNAMES_ambientIntensity	352
#define FIELDNAMES_ambientTexture	353
#define FIELDNAMES_ambientTextureChannel	354
#define FIELDNAMES_anchorPoint	355
#define FIELDNAMES_angle	356
#define FIELDNAMES_angleRate	357
#define FIELDNAMES_angularDampingFactor	358
#define FIELDNAMES_angularVelocity	359
#define FIELDNAMES_anisotropicDegree	360
#define FIELDNAMES_antennaLocation	361
#define FIELDNAMES_antennaPatternLength	362
#define FIELDNAMES_antennaPatternType	363
#define FIELDNAMES_appearance	364
#define FIELDNAMES_applicationID	365
#define FIELDNAMES_applied	366
#define FIELDNAMES_appliedParameters	367
#define FIELDNAMES_articulationParameterArray	368
#define FIELDNAMES_articulationParameterChangeIndicatorArr	369
#define FIELDNAMES_articulationParameterCount	370
#define FIELDNAMES_articulationParameterDesignatorArray	371
#define FIELDNAMES_articulationParameterIdPartAttachedToAr	372
#define FIELDNAMES_articulationParameterTypeArray	373
#define FIELDNAMES_articulationParameterValue0_changed	374
#define FIELDNAMES_articulationParameterValue1_changed	375
#define FIELDNAMES_articulationParameterValue2_changed	376
#define FIELDNAMES_articulationParameterValue3_changed	377
#define FIELDNAMES_articulationParameterValue4_changed	378
#define FIELDNAMES_articulationParameterValue5_changed	379
#define FIELDNAMES_articulationParameterValue6_changed	380
#define FIELDNAMES_articulationParameterValue7_changed	381
#define FIELDNAMES_aspectRatio	382
#define FIELDNAMES_attack	383
#define FIELDNAMES_attenuation	384
#define FIELDNAMES_attrib	385
#define FIELDNAMES_autoCalc	386
#define FIELDNAMES_autoDamp	387
#define FIELDNAMES_autoDisable	388
#define FIELDNAMES_autoOffset	389
#define FIELDNAMES_avatarSize	390
#define FIELDNAMES_axis	391
#define FIELDNAMES_axis1	392
#define FIELDNAMES_axis1Angle	393
#define FIELDNAMES_axis1Torque	394
#define FIELDNAMES_axis2	395
#define FIELDNAMES_axis2Angle	396
#define FIELDNAMES_axis2Torque	397
#define FIELDNAMES_axis3Angle	398
#define FIELDNAMES_axis3Torque	399
#define FIELDNAMES_axisOfRotation	400
#define FIELDNAMES_axisRotation	401
#define FIELDNAMES_back	402
#define FIELDNAMES_backAmbientIntensity	403
#define FIELDNAMES_backCull	404
#define FIELDNAMES_backDiffuseColor	405
#define FIELDNAMES_backEmissiveColor	406
#define FIELDNAMES_backMaterial	407
#define FIELDNAMES_backShininess	408
#define FIELDNAMES_backSpecularColor	409
#define FIELDNAMES_backTexture	410
#define FIELDNAMES_backTransparency	411
#define FIELDNAMES_backUrl	412
#define FIELDNAMES_baseColor	413
#define FIELDNAMES_baseTexture	414
#define FIELDNAMES_baseTextureChannel	415
#define FIELDNAMES_bboxCenter	416
#define FIELDNAMES_bboxDisplay	417
#define FIELDNAMES_bboxSize	418
#define FIELDNAMES_beamWidth	419
#define FIELDNAMES_beginCap	420
#define FIELDNAMES_bindTime	421
#define FIELDNAMES_bodies	422
#define FIELDNAMES_body1	423
#define FIELDNAMES_body1AnchorPoint	424
#define FIELDNAMES_body1Axis	425
#define FIELDNAMES_body2	426
#define FIELDNAMES_body2AnchorPoint	427
#define FIELDNAMES_body2Axis	428
#define FIELDNAMES_borderColor	429
#define FIELDNAMES_borderWidth	430
#define FIELDNAMES_bottom	431
#define FIELDNAMES_bottomRadius	432
#define FIELDNAMES_bottomTexture	433
#define FIELDNAMES_bottomUrl	434
#define FIELDNAMES_bounce	435
#define FIELDNAMES_boundaryModeR	436
#define FIELDNAMES_boundaryModeS	437
#define FIELDNAMES_boundaryModeT	438
#define FIELDNAMES_boundaryOpacity	439
#define FIELDNAMES_buffer	440
#define FIELDNAMES_category	441
#define FIELDNAMES_ccw	442
#define FIELDNAMES_center	443
#define FIELDNAMES_centerOfMass	444
#define FIELDNAMES_centerOfRotation	445
#define FIELDNAMES_centerOfRotation_changed	446
#define FIELDNAMES_channelCount	447
#define FIELDNAMES_channelCountMode	448
#define FIELDNAMES_channelInterpretation	449
#define FIELDNAMES_channels	450
#define FIELDNAMES_channelsEnabled	451
#define FIELDNAMES_child1Url	452
#define FIELDNAMES_child2Url	453
#define FIELDNAMES_child3Url	454
#define FIELDNAMES_child4Url	455
#define FIELDNAMES_children	456
#define FIELDNAMES_choice	457
#define FIELDNAMES_clipBoundary	458
#define FIELDNAMES_closed	459
#define FIELDNAMES_closureType	460
#define FIELDNAMES_collidable	461
#define FIELDNAMES_collidables	462
#define FIELDNAMES_collide	463
#define FIELDNAMES_collideTime	464
#define FIELDNAMES_collider	465
#define FIELDNAMES_collisionType	466
#define FIELDNAMES_color	467
#define FIELDNAMES_colorIndex	468
#define FIELDNAMES_colorKey	469
#define FIELDNAMES_colorMode	470
#define FIELDNAMES_colorPerVertex	471
#define FIELDNAMES_colorRamp	472
#define FIELDNAMES_colorSteps	473
#define FIELDNAMES_coneGain	474
#define FIELDNAMES_coneInnerAngle	475
#define FIELDNAMES_coneOuterAngle	476
#define FIELDNAMES_connect	477
#define FIELDNAMES_constantForceMix	478
#define FIELDNAMES_contactNormal	479
#define FIELDNAMES_contactSurfaceThickness	480
#define FIELDNAMES_contacts	481
#define FIELDNAMES_context	482
#define FIELDNAMES_contourStepSize	483
#define FIELDNAMES_controlKey	484
#define FIELDNAMES_controlPoint	485
#define FIELDNAMES_convex	486
#define FIELDNAMES_coolColor	487
#define FIELDNAMES_coord	488
#define FIELDNAMES_coordIndex	489
#define FIELDNAMES_country	490
#define FIELDNAMES_creaseAngle	491
#define FIELDNAMES_createParticles	492
#define FIELDNAMES_crossSection	493
#define FIELDNAMES_crossSectionCurve	494
#define FIELDNAMES_cryptoKeyID	495
#define FIELDNAMES_cryptoSystem	496
#define FIELDNAMES_currentTime	497
#define FIELDNAMES_curve	498
#define FIELDNAMES_cutOffAngle	499
#define FIELDNAMES_cycleInterval	500
#define FIELDNAMES_cycleTime	501
#define FIELDNAMES_data	502
#define FIELDNAMES_dataLength	503
#define FIELDNAMES_deadReckoning	504
#define FIELDNAMES_defaultValue	505
#define FIELDNAMES_delayTime	506
#define FIELDNAMES_deletionAllowed	507
#define FIELDNAMES_depth	508
#define FIELDNAMES_description	509
#define FIELDNAMES_desiredAngularVelocity1	510
#define FIELDNAMES_desiredAngularVelocity2	511
#define FIELDNAMES_destination	512
#define FIELDNAMES_detonateTime	513
#define FIELDNAMES_detonationLocation	514
#define FIELDNAMES_detonationRelativeLocation	515
#define FIELDNAMES_detonationResult	516
#define FIELDNAMES_detune	517
#define FIELDNAMES_diffuse	518
#define FIELDNAMES_diffuseColor	519
#define FIELDNAMES_diffuseTexture	520
#define FIELDNAMES_diffuseTextureChannel	521
#define FIELDNAMES_dimensions	522
#define FIELDNAMES_directOutput	523
#define FIELDNAMES_direction	524
#define FIELDNAMES_disableAngularSpeed	525
#define FIELDNAMES_disableLinearSpeed	526
#define FIELDNAMES_disableTime	527
#define FIELDNAMES_diskAngle	528
#define FIELDNAMES_displacements	529
#define FIELDNAMES_displacers	530
#define FIELDNAMES_displayed	531
#define FIELDNAMES_distanceModel	532
#define FIELDNAMES_domain	533
#define FIELDNAMES_dopplerFactor	534
#define FIELDNAMES_duration	535
#define FIELDNAMES_duration_changed	536
#define FIELDNAMES_easeInEaseOut	537
#define FIELDNAMES_edgeColor	538
#define FIELDNAMES_effects	539
#define FIELDNAMES_elapsedTime	540
#define FIELDNAMES_emissiveColor	541
#define FIELDNAMES_emissiveTexture	542
#define FIELDNAMES_emissiveTextureChannel	543
#define FIELDNAMES_emitter	544
#define FIELDNAMES_enabled	545
#define FIELDNAMES_enabledAxes	546
#define FIELDNAMES_encodingScheme	547
#define FIELDNAMES_endAngle	548
#define FIELDNAMES_endCap	549
#define FIELDNAMES_endFrame	550
#define FIELDNAMES_enterTime	551
#define FIELDNAMES_enteredText	552
#define FIELDNAMES_entities	553
#define FIELDNAMES_entityCategory	554
#define FIELDNAMES_entityCountry	555
#define FIELDNAMES_entityDomain	556
#define FIELDNAMES_entityExtra	557
#define FIELDNAMES_entityID	558
#define FIELDNAMES_entityKind	559
#define FIELDNAMES_entitySpecific	560
#define FIELDNAMES_entitySubCategory	561
#define FIELDNAMES_errorCorrection	562
#define FIELDNAMES_eventApplicationID	563
#define FIELDNAMES_eventEntityID	564
#define FIELDNAMES_eventNumber	565
#define FIELDNAMES_eventSiteID	566
#define FIELDNAMES_exitTime	567
#define FIELDNAMES_extra	568
#define FIELDNAMES_family	569
#define FIELDNAMES_fanCount	570
#define FIELDNAMES_farDistance	571
#define FIELDNAMES_fftSize	572
#define FIELDNAMES_fieldOfView	573
#define FIELDNAMES_fillProperties	574
#define FIELDNAMES_filled	575
#define FIELDNAMES_filter	576
#define FIELDNAMES_finalText	577
#define FIELDNAMES_finiteRotationAxis	578
#define FIELDNAMES_fireMissionIndex	579
#define FIELDNAMES_fired1	580
#define FIELDNAMES_fired2	581
#define FIELDNAMES_firedTime	582
#define FIELDNAMES_firingRange	583
#define FIELDNAMES_firingRate	584
#define FIELDNAMES_fixed	585
#define FIELDNAMES_floatInp	586
#define FIELDNAMES_focalPoint	587
#define FIELDNAMES_fogCoord	588
#define FIELDNAMES_fogType	589
#define FIELDNAMES_fontStyle	590
#define FIELDNAMES_force	591
#define FIELDNAMES_forceID	592
#define FIELDNAMES_forceOutput	593
#define FIELDNAMES_forceTransitions	594
#define FIELDNAMES_forces	595
#define FIELDNAMES_fovMode	596
#define FIELDNAMES_fraction_changed	597
#define FIELDNAMES_frameCount	598
#define FIELDNAMES_frameDuration	599
#define FIELDNAMES_frameIncrement	600
#define FIELDNAMES_frameIndex	601
#define FIELDNAMES_frequency	602
#define FIELDNAMES_frequencyBinCount	603
#define FIELDNAMES_frictionCoefficients	604
#define FIELDNAMES_frictionDirection	605
#define FIELDNAMES_front	606
#define FIELDNAMES_frontTexture	607
#define FIELDNAMES_frontUrl	608
#define FIELDNAMES_function	609
#define FIELDNAMES_fuse	610
#define FIELDNAMES_gain	611
#define FIELDNAMES_gcCoords_changed	612
#define FIELDNAMES_generateMipMaps	613
#define FIELDNAMES_geoCenter	614
#define FIELDNAMES_geoCoord_changed	615
#define FIELDNAMES_geoCoords	616
#define FIELDNAMES_geoCoords_changed	617
#define FIELDNAMES_geoGridOrigin	618
#define FIELDNAMES_geoOrigin	619
#define FIELDNAMES_geoSystem	620
#define FIELDNAMES_geometry	621
#define FIELDNAMES_geometry1	622
#define FIELDNAMES_geometry2	623
#define FIELDNAMES_geometryType	624
#define FIELDNAMES_geovalue_changed	625
#define FIELDNAMES_global	626
#define FIELDNAMES_gotEvents	627
#define FIELDNAMES_gradientThreshold	628
#define FIELDNAMES_gradients	629
#define FIELDNAMES_gravity	630
#define FIELDNAMES_groundAngle	631
#define FIELDNAMES_groundColor	632
#define FIELDNAMES_gustiness	633
#define FIELDNAMES_handler	634
#define FIELDNAMES_hatchColor	635
#define FIELDNAMES_hatchStyle	636
#define FIELDNAMES_hatched	637
#define FIELDNAMES_headlight	638
#define FIELDNAMES_height	639
#define FIELDNAMES_hinge1Angle	640
#define FIELDNAMES_hinge1AngleRate	641
#define FIELDNAMES_hinge2Angle	642
#define FIELDNAMES_hinge2AngleRate	643
#define FIELDNAMES_hitGeoCoord_changed	644
#define FIELDNAMES_hitNormal_changed	645
#define FIELDNAMES_hitPoint_changed	646
#define FIELDNAMES_hitTexCoord_changed	647
#define FIELDNAMES_horizontal	648
#define FIELDNAMES_image	649
#define FIELDNAMES_index	650
#define FIELDNAMES_inertia	651
#define FIELDNAMES_info	652
#define FIELDNAMES_initialDestination	653
#define FIELDNAMES_initialValue	654
#define FIELDNAMES_innerRadius	655
#define FIELDNAMES_inputFalse	656
#define FIELDNAMES_inputNegate	657
#define FIELDNAMES_inputSource	658
#define FIELDNAMES_inputTrue	659
#define FIELDNAMES_int32Inp	660
#define FIELDNAMES_integerKey	661
#define FIELDNAMES_intensity	662
#define FIELDNAMES_intensityThreshold	663
#define FIELDNAMES_internal	664
#define FIELDNAMES_intersectionType	665
#define FIELDNAMES_intersections	666
#define FIELDNAMES_isActive	667
#define FIELDNAMES_isBound	668
#define FIELDNAMES_isCollided	669
#define FIELDNAMES_isDetonated	670
#define FIELDNAMES_isLoaded	671
#define FIELDNAMES_isNetworkReader	672
#define FIELDNAMES_isNetworkWriter	673
#define FIELDNAMES_isOver	674
#define FIELDNAMES_isPaused	675
#define FIELDNAMES_isPickable	676
#define FIELDNAMES_isPositionAvailable	677
#define FIELDNAMES_isRotationAvailable	678
#define FIELDNAMES_isRtpHeaderHeard	679
#define FIELDNAMES_isSelected	680
#define FIELDNAMES_isStandAlone	681
#define FIELDNAMES_isValid	682
#define FIELDNAMES_iterations	683
#define FIELDNAMES_joints	684
#define FIELDNAMES_jump	685
#define FIELDNAMES_justify	686
#define FIELDNAMES_key	687
#define FIELDNAMES_keyPress	688
#define FIELDNAMES_keyRelease	689
#define FIELDNAMES_keyValue	690
#define FIELDNAMES_keyVelocity	691
#define FIELDNAMES_kind	692
#define FIELDNAMES_knee	693
#define FIELDNAMES_knot	694
#define FIELDNAMES_language	695
#define FIELDNAMES_layers	696
#define FIELDNAMES_layout	697
#define FIELDNAMES_left	698
#define FIELDNAMES_leftTexture	699
#define FIELDNAMES_leftToRight	700
#define FIELDNAMES_leftUrl	701
#define FIELDNAMES_length	702
#define FIELDNAMES_lengthOfModulationParameters	703
#define FIELDNAMES_level	704
#define FIELDNAMES_level_changed	705
#define FIELDNAMES_lifetimeVariation	706
#define FIELDNAMES_lighting	707
#define FIELDNAMES_limitOrientation	708
#define FIELDNAMES_lineBounds	709
#define FIELDNAMES_lineProperties	710
#define FIELDNAMES_lineSegments	711
#define FIELDNAMES_linearAcceleration	712
#define FIELDNAMES_linearDampingFactor	713
#define FIELDNAMES_linearVelocity	714
#define FIELDNAMES_linetype	715
#define FIELDNAMES_linewidthScaleFactor	716
#define FIELDNAMES_listener	717
#define FIELDNAMES_listenfor	718
#define FIELDNAMES_llimit	719
#define FIELDNAMES_loa	720
#define FIELDNAMES_load	721
#define FIELDNAMES_loadTime	722
#define FIELDNAMES_location	723
#define FIELDNAMES_loop	724
#define FIELDNAMES_loopEnd	725
#define FIELDNAMES_loopStart	726
#define FIELDNAMES_magnificationFilter	727
#define FIELDNAMES_mapping	728
#define FIELDNAMES_marking	729
#define FIELDNAMES_mass	730
#define FIELDNAMES_massDensityModel	731
#define FIELDNAMES_matchCriterion	732
#define FIELDNAMES_material	733
#define FIELDNAMES_matrix	734
#define FIELDNAMES_maxAngle	735
#define FIELDNAMES_maxAngle1	736
#define FIELDNAMES_maxBack	737
#define FIELDNAMES_maxChannelCount	738
#define FIELDNAMES_maxCorrectionSpeed	739
#define FIELDNAMES_maxDecibels	740
#define FIELDNAMES_maxDistance	741
#define FIELDNAMES_maxExtent	742
#define FIELDNAMES_maxFront	743
#define FIELDNAMES_maxParticles	744
#define FIELDNAMES_maxPosition	745
#define FIELDNAMES_maxSeparation	746
#define FIELDNAMES_maxTorque1	747
#define FIELDNAMES_maxTorque2	748
#define FIELDNAMES_metadata	749
#define FIELDNAMES_metallic	750
#define FIELDNAMES_metallicRoughnessTexture	751
#define FIELDNAMES_metallicRoughnessTextureChannel	752
#define FIELDNAMES_method	753
#define FIELDNAMES_minAngle	754
#define FIELDNAMES_minAngle1	755
#define FIELDNAMES_minBack	756
#define FIELDNAMES_minBounceSpeed	757
#define FIELDNAMES_minDecibels	758
#define FIELDNAMES_minFront	759
#define FIELDNAMES_minPosition	760
#define FIELDNAMES_minSeparation	761
#define FIELDNAMES_minificationFilter	762
#define FIELDNAMES_mode	763
#define FIELDNAMES_modifiedFraction_changed	764
#define FIELDNAMES_modulationTypeDetail	765
#define FIELDNAMES_modulationTypeMajor	766
#define FIELDNAMES_modulationTypeSpreadSpectrum	767
#define FIELDNAMES_modulationTypeSystem	768
#define FIELDNAMES_momentsOfInertia	769
#define FIELDNAMES_motions	770
#define FIELDNAMES_motionsEnabled	771
#define FIELDNAMES_motor1Angle	772
#define FIELDNAMES_motor1AngleRate	773
#define FIELDNAMES_motor1Axis	774
#define FIELDNAMES_motor2Angle	775
#define FIELDNAMES_motor2AngleRate	776
#define FIELDNAMES_motor2Axis	777
#define FIELDNAMES_motor3Angle	778
#define FIELDNAMES_motor3AngleRate	779
#define FIELDNAMES_motor3Axis	780
#define FIELDNAMES_mozAudioChannelType	781
#define FIELDNAMES_multicastRelayHost	782
#define FIELDNAMES_multicastRelayPort	783
#define FIELDNAMES_munitionApplicationID	784
#define FIELDNAMES_munitionEndPoint	785
#define FIELDNAMES_munitionEntityID	786
#define FIELDNAMES_munitionQuantity	787
#define FIELDNAMES_munitionSiteID	788
#define FIELDNAMES_munitionStartPoint	789
#define FIELDNAMES_mustEvaluate	790
#define FIELDNAMES_name	791
#define FIELDNAMES_navType	792
#define FIELDNAMES_nearDistance	793
#define FIELDNAMES_networkMode	794
#define FIELDNAMES_next	795
#define FIELDNAMES_normal	796
#define FIELDNAMES_normalIndex	797
#define FIELDNAMES_normalPerVertex	798
#define FIELDNAMES_normalTexture	799
#define FIELDNAMES_normalTextureChannel	800
#define FIELDNAMES_normal_changed	801
#define FIELDNAMES_normalize	802
#define FIELDNAMES_normalizeVelocity	803
#define FIELDNAMES_numComponents	804
#define FIELDNAMES_numberOfChannels	805
#define FIELDNAMES_numberOfInputs	806
#define FIELDNAMES_numberOfOutputs	807
#define FIELDNAMES_objectType	808
#define FIELDNAMES_offset	809
#define FIELDNAMES_offsetUnits	810
#define FIELDNAMES_on	811
#define FIELDNAMES_opacityFactor	812
#define FIELDNAMES_order	813
#define FIELDNAMES_orientation	814
#define FIELDNAMES_orientation_changed	815
#define FIELDNAMES_origin	816
#define FIELDNAMES_orthogonalColor	817
#define FIELDNAMES_outerRadius	818
#define FIELDNAMES_oversample	819
#define FIELDNAMES_panningModel	820
#define FIELDNAMES_parallelColor	821
#define FIELDNAMES_parameter	822
#define FIELDNAMES_particleLifetime	823
#define FIELDNAMES_particleSize	824
#define FIELDNAMES_parts	825
#define FIELDNAMES_pauseTime	826
#define FIELDNAMES_phaseFunction	827
#define FIELDNAMES_physics	828
#define FIELDNAMES_pickTarget	829
#define FIELDNAMES_pickable	830
#define FIELDNAMES_pickedGeometry	831
#define FIELDNAMES_pickedNormal	832
#define FIELDNAMES_pickedPoint	833
#define FIELDNAMES_pickedTextureCoordinate	834
#define FIELDNAMES_pickingGeometry	835
#define FIELDNAMES_pitch	836
#define FIELDNAMES_plane	837
#define FIELDNAMES_planetId	838
#define FIELDNAMES_playbackRate	839
#define FIELDNAMES_point	840
#define FIELDNAMES_pointProperties	841
#define FIELDNAMES_pointSize	842
#define FIELDNAMES_pointSizeAttenuation	843
#define FIELDNAMES_pointSizeMaxValue	844
#define FIELDNAMES_pointSizeMinValue	845
#define FIELDNAMES_pointSizeScaleFactor	846
#define FIELDNAMES_port	847
#define FIELDNAMES_position	848
#define FIELDNAMES_position_changed	849
#define FIELDNAMES_power	850
#define FIELDNAMES_preferAccuracy	851
#define FIELDNAMES_previous	852
#define FIELDNAMES_priority	853
#define FIELDNAMES_profileCurve	854
#define FIELDNAMES_programs	855
#define FIELDNAMES_progress	856
#define FIELDNAMES_protocol	857
#define FIELDNAMES_proxy	858
#define FIELDNAMES_radioEntityTypeCategory	859
#define FIELDNAMES_radioEntityTypeCountry	860
#define FIELDNAMES_radioEntityTypeDomain	861
#define FIELDNAMES_radioEntityTypeKind	862
#define FIELDNAMES_radioEntityTypeNomenclature	863
#define FIELDNAMES_radioEntityTypeNomenclatureVersion	864
#define FIELDNAMES_radioID	865
#define FIELDNAMES_radius	866
#define FIELDNAMES_range	867
#define FIELDNAMES_ratio	868
#define FIELDNAMES_readInterval	869
#define FIELDNAMES_receivedPower	870
#define FIELDNAMES_receiverState	871
#define FIELDNAMES_reduction	872
#define FIELDNAMES_refDistance	873
#define FIELDNAMES_reference	874
#define FIELDNAMES_refraction	875
#define FIELDNAMES_relativeAntennaLocation	876
#define FIELDNAMES_relativeHeight	877
#define FIELDNAMES_release	878
#define FIELDNAMES_removeChildren	879
#define FIELDNAMES_removeEntities	880
#define FIELDNAMES_removeGeometry	881
#define FIELDNAMES_removeTrimmingContour	882
#define FIELDNAMES_removedEntities	883
#define FIELDNAMES_renderStyle	884
#define FIELDNAMES_repeatR	885
#define FIELDNAMES_repeatS	886
#define FIELDNAMES_repeatT	887
#define FIELDNAMES_resumeTime	888
#define FIELDNAMES_retainUserOffsets	889
#define FIELDNAMES_retainedOpacity	890
#define FIELDNAMES_right	891
#define FIELDNAMES_rightTexture	892
#define FIELDNAMES_rightUrl	893
#define FIELDNAMES_rolloffFactor	894
#define FIELDNAMES_rootNode	895
#define FIELDNAMES_rootUrl	896
#define FIELDNAMES_rotateYUp	897
#define FIELDNAMES_rotation	898
#define FIELDNAMES_rotation_changed	899
#define FIELDNAMES_roughness	900
#define FIELDNAMES_rtpHeaderExpected	901
#define FIELDNAMES_sampleRate	902
#define FIELDNAMES_samples	903
#define FIELDNAMES_scale	904
#define FIELDNAMES_scaleMode	905
#define FIELDNAMES_scaleOrientation	906
#define FIELDNAMES_segmentEnabled	907
#define FIELDNAMES_segmentIdentifiers	908
#define FIELDNAMES_segments	909
#define FIELDNAMES_sensorLocalOutput	910
#define FIELDNAMES_separateBackColor	911
#define FIELDNAMES_separation	912
#define FIELDNAMES_separationRate	913
#define FIELDNAMES_setValue	914
#define FIELDNAMES_set_articulationParameterValue0	915
#define FIELDNAMES_set_articulationParameterValue1	916
#define FIELDNAMES_set_articulationParameterValue2	917
#define FIELDNAMES_set_articulationParameterValue3	918
#define FIELDNAMES_set_articulationParameterValue4	919
#define FIELDNAMES_set_articulationParameterValue5	920
#define FIELDNAMES_set_articulationParameterValue6	921
#define FIELDNAMES_set_articulationParameterValue7	922
#define FIELDNAMES_set_bind	923
#define FIELDNAMES_set_boolean	924
#define FIELDNAMES_set_colorIndex	925
#define FIELDNAMES_set_contacts	926
#define FIELDNAMES_set_coordIndex	927
#define FIELDNAMES_set_coordinate	928
#define FIELDNAMES_set_crossSection	929
#define FIELDNAMES_set_destination	930
#define FIELDNAMES_set_fraction	931
#define FIELDNAMES_set_gcCoords	932
#define FIELDNAMES_set_geoCoords	933
#define FIELDNAMES_set_height	934
#define FIELDNAMES_set_index	935
#define FIELDNAMES_set_intersectionType	936
#define FIELDNAMES_set_normalIndex	937
#define FIELDNAMES_set_orientation	938
#define FIELDNAMES_set_scale	939
#define FIELDNAMES_set_sortOrder	940
#define FIELDNAMES_set_spine	941
#define FIELDNAMES_set_texCoordIndex	942
#define FIELDNAMES_set_triggerTime	943
#define FIELDNAMES_set_value	944
#define FIELDNAMES_shaders	945
#define FIELDNAMES_shadows	946
#define FIELDNAMES_shape	947
#define FIELDNAMES_shiftKey	948
#define FIELDNAMES_shininess	949
#define FIELDNAMES_side	950
#define FIELDNAMES_silhouetteBoundaryOpacity	951
#define FIELDNAMES_silhouetteRetainedOpacity	952
#define FIELDNAMES_silhouetteSharpness	953
#define FIELDNAMES_singleton	954
#define FIELDNAMES_siteID	955
#define FIELDNAMES_sites	956
#define FIELDNAMES_size	957
#define FIELDNAMES_sizeUnits	958
#define FIELDNAMES_skeletalConfiguration	959
#define FIELDNAMES_skeleton	960
#define FIELDNAMES_skin	961
#define FIELDNAMES_skinCoord	962
#define FIELDNAMES_skinCoordIndex	963
#define FIELDNAMES_skinCoordWeight	964
#define FIELDNAMES_skinNormal	965
#define FIELDNAMES_skyAngle	966
#define FIELDNAMES_skyColor	967
#define FIELDNAMES_sliderForce	968
#define FIELDNAMES_slipCoefficients	969
#define FIELDNAMES_slipFactors	970
#define FIELDNAMES_smoothingTimeConstant	971
#define FIELDNAMES_softnessConstantForceMix	972
#define FIELDNAMES_softnessErrorCorrection	973
#define FIELDNAMES_solid	974
#define FIELDNAMES_sortOrder	975
#define FIELDNAMES_source	976
#define FIELDNAMES_spacing	977
#define FIELDNAMES_spatialize	978
#define FIELDNAMES_specific	979
#define FIELDNAMES_specular	980
#define FIELDNAMES_specularColor	981
#define FIELDNAMES_specularShininessTexture	982
#define FIELDNAMES_specularShininessTextureChannel	983
#define FIELDNAMES_specularTexture	984
#define FIELDNAMES_speed	985
#define FIELDNAMES_speedFactor	986
#define FIELDNAMES_speedOfSound	987
#define FIELDNAMES_spine	988
#define FIELDNAMES_startAngle	989
#define FIELDNAMES_startFrame	990
#define FIELDNAMES_startTime	991
#define FIELDNAMES_stiffness	992
#define FIELDNAMES_stop1Bounce	993
#define FIELDNAMES_stop1ErrorCorrection	994
#define FIELDNAMES_stop2Bounce	995
#define FIELDNAMES_stop2ErrorCorrection	996
#define FIELDNAMES_stop3Bounce	997
#define FIELDNAMES_stop3ErrorCorrection	998
#define FIELDNAMES_stopBounce	999
#define FIELDNAMES_stopBounce1	1000
#define FIELDNAMES_stopConstantForceMix1	1001
#define FIELDNAMES_stopErrorCorrection	1002
#define FIELDNAMES_stopErrorCorrection1	1003
#define FIELDNAMES_stopTime	1004
#define FIELDNAMES_stream	1005
#define FIELDNAMES_string	1006
#define FIELDNAMES_stringInp	1007
#define FIELDNAMES_stripCount	1008
#define FIELDNAMES_style	1009
#define FIELDNAMES_styleEnd	1010
#define FIELDNAMES_styleStart	1011
#define FIELDNAMES_subcategory	1012
#define FIELDNAMES_summary	1013
#define FIELDNAMES_surface	1014
#define FIELDNAMES_surfaceArea	1015
#define FIELDNAMES_surfaceNormals	1016
#define FIELDNAMES_surfaceSpeed	1017
#define FIELDNAMES_surfaceTolerance	1018
#define FIELDNAMES_surfaceValues	1019
#define FIELDNAMES_suspensionErrorCorrection	1020
#define FIELDNAMES_suspensionForce	1021
#define FIELDNAMES_talksTo	1022
#define FIELDNAMES_targetObject	1023
#define FIELDNAMES_tau	1024
#define FIELDNAMES_tdlType	1025
#define FIELDNAMES_tessellation	1026
#define FIELDNAMES_tessellationScale	1027
#define FIELDNAMES_texCoord	1028
#define FIELDNAMES_texCoordIndex	1029
#define FIELDNAMES_texCoordKey	1030
#define FIELDNAMES_texCoordRamp	1031
#define FIELDNAMES_textBounds	1032
#define FIELDNAMES_texture	1033
#define FIELDNAMES_textureCompression	1034
#define FIELDNAMES_texturePriority	1035
#define FIELDNAMES_textureProperties	1036
#define FIELDNAMES_textureTransform	1037
#define FIELDNAMES_threshold	1038
#define FIELDNAMES_tickTime	1039
#define FIELDNAMES_time	1040
#define FIELDNAMES_timeOut	1041
#define FIELDNAMES_timestamp	1042
#define FIELDNAMES_title	1043
#define FIELDNAMES_toggle	1044
#define FIELDNAMES_tolerance	1045
#define FIELDNAMES_top	1046
#define FIELDNAMES_topTexture	1047
#define FIELDNAMES_topToBottom	1048
#define FIELDNAMES_topUrl	1049
#define FIELDNAMES_torques	1050
#define FIELDNAMES_touchTime	1051
#define FIELDNAMES_trackPoint_changed	1052
#define FIELDNAMES_trajectoryCurve	1053
#define FIELDNAMES_transferFunction	1054
#define FIELDNAMES_transitionComplete	1055
#define FIELDNAMES_transitionTime	1056
#define FIELDNAMES_transitionType	1057
#define FIELDNAMES_translation	1058
#define FIELDNAMES_translation_changed	1059
#define FIELDNAMES_transmitFrequencyBandwidth	1060
#define FIELDNAMES_transmitState	1061
#define FIELDNAMES_transmitterApplicationID	1062
#define FIELDNAMES_transmitterEntityID	1063
#define FIELDNAMES_transmitterRadioID	1064
#define FIELDNAMES_transmitterSiteID	1065
#define FIELDNAMES_transparency	1066
#define FIELDNAMES_triggerTime	1067
#define FIELDNAMES_triggerTrue	1068
#define FIELDNAMES_triggerValue	1069
#define FIELDNAMES_trimmingContour	1070
#define FIELDNAMES_turbulence	1071
#define FIELDNAMES_type	1072
#define FIELDNAMES_type16dashes	1073
#define FIELDNAMES_type16wiggles	1074
#define FIELDNAMES_uClosed	1075
#define FIELDNAMES_uDimension	1076
#define FIELDNAMES_uKnot	1077
#define FIELDNAMES_uOrder	1078
#define FIELDNAMES_uTessellation	1079
#define FIELDNAMES_ulimit	1080
#define FIELDNAMES_upVector	1081
#define FIELDNAMES_update	1082
#define FIELDNAMES_url	1083
#define FIELDNAMES_useFiniteRotation	1084
#define FIELDNAMES_useGeometry	1085
#define FIELDNAMES_useGlobalGravity	1086
#define FIELDNAMES_vClosed	1087
#define FIELDNAMES_vDimension	1088
#define FIELDNAMES_vKnot	1089
#define FIELDNAMES_vOrder	1090
#define FIELDNAMES_vTessellation	1091
#define FIELDNAMES_value	1092
#define FIELDNAMES_valueChanged	1093
#define FIELDNAMES_value_changed	1094
#define FIELDNAMES_values	1095
#define FIELDNAMES_variation	1096
#define FIELDNAMES_vector	1097
#define FIELDNAMES_velocity	1098
#define FIELDNAMES_version	1099
#define FIELDNAMES_vertexCount	1100
#define FIELDNAMES_vertices	1101
#define FIELDNAMES_viewpoints	1102
#define FIELDNAMES_viewport	1103
#define FIELDNAMES_visibilityLimit	1104
#define FIELDNAMES_visibilityRange	1105
#define FIELDNAMES_visible	1106
#define FIELDNAMES_visibles	1107
#define FIELDNAMES_voxels	1108
#define FIELDNAMES_warhead	1109
#define FIELDNAMES_warmColor	1110
#define FIELDNAMES_watchList	1111
#define FIELDNAMES_weight	1112
#define FIELDNAMES_weightConstant1	1113
#define FIELDNAMES_weightConstant2	1114
#define FIELDNAMES_weightFunction1	1115
#define FIELDNAMES_weightFunction2	1116
#define FIELDNAMES_weightTransferFunction1	1117
#define FIELDNAMES_weightTransferFunction2	1118
#define FIELDNAMES_whichChoice	1119
#define FIELDNAMES_whichGeometry	1120
#define FIELDNAMES_writeInterval	1121
#define FIELDNAMES_xDimension	1122
#define FIELDNAMES_xSpacing	1123
#define FIELDNAMES_yScale	1124
#define FIELDNAMES_zDimension	1125
#define FIELDNAMES_zSpacing	1126

const char *stringFieldType(int st);

/* Table of built-in fieldIds */
extern const char *EVENT_OUT[];
extern const int EVENT_OUT_COUNT;
#define EVENT_OUT_actionKeyPress	0
#define EVENT_OUT_actionKeyRelease	1
#define EVENT_OUT_addedEntities	2
#define EVENT_OUT_altKey	3
#define EVENT_OUT_angle	4
#define EVENT_OUT_angleRate	5
#define EVENT_OUT_articulationParameterValue0_changed	6
#define EVENT_OUT_articulationParameterValue1_changed	7
#define EVENT_OUT_articulationParameterValue2_changed	8
#define EVENT_OUT_articulationParameterValue3_changed	9
#define EVENT_OUT_articulationParameterValue4_changed	10
#define EVENT_OUT_articulationParameterValue5_changed	11
#define EVENT_OUT_articulationParameterValue6_changed	12
#define EVENT_OUT_articulationParameterValue7_changed	13
#define EVENT_OUT_aspectRatio	14
#define EVENT_OUT_bindTime	15
#define EVENT_OUT_body1AnchorPoint	16
#define EVENT_OUT_body1Axis	17
#define EVENT_OUT_body2AnchorPoint	18
#define EVENT_OUT_body2Axis	19
#define EVENT_OUT_centerOfRotation_changed	20
#define EVENT_OUT_children	21
#define EVENT_OUT_collideTime	22
#define EVENT_OUT_contacts	23
#define EVENT_OUT_controlKey	24
#define EVENT_OUT_cycleTime	25
#define EVENT_OUT_detonateTime	26
#define EVENT_OUT_duration_changed	27
#define EVENT_OUT_elapsedTime	28
#define EVENT_OUT_enterTime	29
#define EVENT_OUT_enteredText	30
#define EVENT_OUT_exitTime	31
#define EVENT_OUT_fieldOfView	32
#define EVENT_OUT_finalText	33
#define EVENT_OUT_firedTime	34
#define EVENT_OUT_focalPoint	35
#define EVENT_OUT_fovMode	36
#define EVENT_OUT_fraction_changed	37
#define EVENT_OUT_frameCount	38
#define EVENT_OUT_gcCoords_changed	39
#define EVENT_OUT_geoCoord_changed	40
#define EVENT_OUT_geoCoords_changed	41
#define EVENT_OUT_geovalue_changed	42
#define EVENT_OUT_hinge1Angle	43
#define EVENT_OUT_hinge1AngleRate	44
#define EVENT_OUT_hinge2Angle	45
#define EVENT_OUT_hinge2AngleRate	46
#define EVENT_OUT_hitGeoCoord_changed	47
#define EVENT_OUT_hitNormal_changed	48
#define EVENT_OUT_hitPoint_changed	49
#define EVENT_OUT_hitTexCoord_changed	50
#define EVENT_OUT_image	51
#define EVENT_OUT_inputFalse	52
#define EVENT_OUT_inputNegate	53
#define EVENT_OUT_inputTrue	54
#define EVENT_OUT_intersections	55
#define EVENT_OUT_isActive	56
#define EVENT_OUT_isBound	57
#define EVENT_OUT_isCollided	58
#define EVENT_OUT_isDetonated	59
#define EVENT_OUT_isLoaded	60
#define EVENT_OUT_isNetworkReader	61
#define EVENT_OUT_isNetworkWriter	62
#define EVENT_OUT_isOver	63
#define EVENT_OUT_isPaused	64
#define EVENT_OUT_isPositionAvailable	65
#define EVENT_OUT_isRotationAvailable	66
#define EVENT_OUT_isRtpHeaderHeard	67
#define EVENT_OUT_isSelected	68
#define EVENT_OUT_isStandAlone	69
#define EVENT_OUT_isValid	70
#define EVENT_OUT_keyPress	71
#define EVENT_OUT_keyRelease	72
#define EVENT_OUT_level_changed	73
#define EVENT_OUT_lineBounds	74
#define EVENT_OUT_loadTime	75
#define EVENT_OUT_modifiedFraction_changed	76
#define EVENT_OUT_motor1Angle	77
#define EVENT_OUT_motor1AngleRate	78
#define EVENT_OUT_motor1Axis	79
#define EVENT_OUT_motor2Angle	80
#define EVENT_OUT_motor2AngleRate	81
#define EVENT_OUT_motor2Axis	82
#define EVENT_OUT_motor3Angle	83
#define EVENT_OUT_motor3AngleRate	84
#define EVENT_OUT_motor3Axis	85
#define EVENT_OUT_normal_changed	86
#define EVENT_OUT_orientation_changed	87
#define EVENT_OUT_origin	88
#define EVENT_OUT_pickedGeometry	89
#define EVENT_OUT_pickedNormal	90
#define EVENT_OUT_pickedPoint	91
#define EVENT_OUT_pickedTextureCoordinate	92
#define EVENT_OUT_position	93
#define EVENT_OUT_position_changed	94
#define EVENT_OUT_progress	95
#define EVENT_OUT_removedEntities	96
#define EVENT_OUT_rotation	97
#define EVENT_OUT_rotation_changed	98
#define EVENT_OUT_separation	99
#define EVENT_OUT_separationRate	100
#define EVENT_OUT_shiftKey	101
#define EVENT_OUT_textBounds	102
#define EVENT_OUT_time	103
#define EVENT_OUT_timestamp	104
#define EVENT_OUT_toggle	105
#define EVENT_OUT_touchTime	106
#define EVENT_OUT_trackPoint_changed	107
#define EVENT_OUT_transitionComplete	108
#define EVENT_OUT_translation_changed	109
#define EVENT_OUT_triggerTime	110
#define EVENT_OUT_triggerTrue	111
#define EVENT_OUT_triggerValue	112
#define EVENT_OUT_valueChanged	113
#define EVENT_OUT_value_changed	114


/* Table of built-in fieldIds */
extern const char *EVENT_IN[];
extern const int EVENT_IN_COUNT;
#define EVENT_IN_activate	0
#define EVENT_IN_addChildren	1
#define EVENT_IN_addEntities	2
#define EVENT_IN_addGeometry	3
#define EVENT_IN_addTrimmingContour	4
#define EVENT_IN_method	5
#define EVENT_IN_next	6
#define EVENT_IN_previous	7
#define EVENT_IN_removeChildren	8
#define EVENT_IN_removeEntities	9
#define EVENT_IN_removeGeometry	10
#define EVENT_IN_removeTrimmingContour	11
#define EVENT_IN_retainUserOffsets	12
#define EVENT_IN_setValue	13
#define EVENT_IN_set_articulationParameterValue0	14
#define EVENT_IN_set_articulationParameterValue1	15
#define EVENT_IN_set_articulationParameterValue2	16
#define EVENT_IN_set_articulationParameterValue3	17
#define EVENT_IN_set_articulationParameterValue4	18
#define EVENT_IN_set_articulationParameterValue5	19
#define EVENT_IN_set_articulationParameterValue6	20
#define EVENT_IN_set_articulationParameterValue7	21
#define EVENT_IN_set_bind	22
#define EVENT_IN_set_boolean	23
#define EVENT_IN_set_colorIndex	24
#define EVENT_IN_set_contacts	25
#define EVENT_IN_set_coordIndex	26
#define EVENT_IN_set_coordinate	27
#define EVENT_IN_set_crossSection	28
#define EVENT_IN_set_destination	29
#define EVENT_IN_set_fraction	30
#define EVENT_IN_set_gcCoords	31
#define EVENT_IN_set_geoCoords	32
#define EVENT_IN_set_height	33
#define EVENT_IN_set_index	34
#define EVENT_IN_set_intersectionType	35
#define EVENT_IN_set_normalIndex	36
#define EVENT_IN_set_orientation	37
#define EVENT_IN_set_scale	38
#define EVENT_IN_set_sortOrder	39
#define EVENT_IN_set_spine	40
#define EVENT_IN_set_texCoordIndex	41
#define EVENT_IN_set_triggerTime	42
#define EVENT_IN_set_value	43
#define EVENT_IN_tickTime	44


/* Table of built-in fieldIds */
extern const char *EXPOSED_FIELD[];
extern const int EXPOSED_FIELD_COUNT;
#define EXPOSED_FIELD_FIFOsize	0
#define EXPOSED_FIELD_Q	1
#define EXPOSED_FIELD_absorption	2
#define EXPOSED_FIELD_acousticProperties	3
#define EXPOSED_FIELD_activeLayer	4
#define EXPOSED_FIELD_address	5
#define EXPOSED_FIELD_align	6
#define EXPOSED_FIELD_alpha	7
#define EXPOSED_FIELD_ambientIntensity	8
#define EXPOSED_FIELD_ambientTexture	9
#define EXPOSED_FIELD_ambientTextureChannel	10
#define EXPOSED_FIELD_anchorPoint	11
#define EXPOSED_FIELD_angle	12
#define EXPOSED_FIELD_angularDampingFactor	13
#define EXPOSED_FIELD_angularVelocity	14
#define EXPOSED_FIELD_anisotropicDegree	15
#define EXPOSED_FIELD_antennaLocation	16
#define EXPOSED_FIELD_antennaPatternLength	17
#define EXPOSED_FIELD_antennaPatternType	18
#define EXPOSED_FIELD_appearance	19
#define EXPOSED_FIELD_applicationID	20
#define EXPOSED_FIELD_applied	21
#define EXPOSED_FIELD_appliedParameters	22
#define EXPOSED_FIELD_articulationParameterArray	23
#define EXPOSED_FIELD_articulationParameterChangeIndicatorArr	24
#define EXPOSED_FIELD_articulationParameterCount	25
#define EXPOSED_FIELD_articulationParameterDesignatorArray	26
#define EXPOSED_FIELD_articulationParameterIdPartAttachedToAr	27
#define EXPOSED_FIELD_articulationParameterTypeArray	28
#define EXPOSED_FIELD_aspectRatio	29
#define EXPOSED_FIELD_attack	30
#define EXPOSED_FIELD_attenuation	31
#define EXPOSED_FIELD_attrib	32
#define EXPOSED_FIELD_autoDamp	33
#define EXPOSED_FIELD_autoDisable	34
#define EXPOSED_FIELD_autoOffset	35
#define EXPOSED_FIELD_avatarSize	36
#define EXPOSED_FIELD_axis	37
#define EXPOSED_FIELD_axis1	38
#define EXPOSED_FIELD_axis1Angle	39
#define EXPOSED_FIELD_axis1Torque	40
#define EXPOSED_FIELD_axis2	41
#define EXPOSED_FIELD_axis2Angle	42
#define EXPOSED_FIELD_axis2Torque	43
#define EXPOSED_FIELD_axis3Angle	44
#define EXPOSED_FIELD_axis3Torque	45
#define EXPOSED_FIELD_axisOfRotation	46
#define EXPOSED_FIELD_axisRotation	47
#define EXPOSED_FIELD_back	48
#define EXPOSED_FIELD_backAmbientIntensity	49
#define EXPOSED_FIELD_backCull	50
#define EXPOSED_FIELD_backDiffuseColor	51
#define EXPOSED_FIELD_backEmissiveColor	52
#define EXPOSED_FIELD_backMaterial	53
#define EXPOSED_FIELD_backShininess	54
#define EXPOSED_FIELD_backSpecularColor	55
#define EXPOSED_FIELD_backTexture	56
#define EXPOSED_FIELD_backTransparency	57
#define EXPOSED_FIELD_backUrl	58
#define EXPOSED_FIELD_baseColor	59
#define EXPOSED_FIELD_baseTexture	60
#define EXPOSED_FIELD_baseTextureChannel	61
#define EXPOSED_FIELD_bboxDisplay	62
#define EXPOSED_FIELD_beamWidth	63
#define EXPOSED_FIELD_bodies	64
#define EXPOSED_FIELD_body1	65
#define EXPOSED_FIELD_body2	66
#define EXPOSED_FIELD_borderColor	67
#define EXPOSED_FIELD_borderWidth	68
#define EXPOSED_FIELD_bottom	69
#define EXPOSED_FIELD_bottomTexture	70
#define EXPOSED_FIELD_bottomUrl	71
#define EXPOSED_FIELD_bounce	72
#define EXPOSED_FIELD_boundaryModeR	73
#define EXPOSED_FIELD_boundaryModeS	74
#define EXPOSED_FIELD_boundaryModeT	75
#define EXPOSED_FIELD_boundaryOpacity	76
#define EXPOSED_FIELD_buffer	77
#define EXPOSED_FIELD_center	78
#define EXPOSED_FIELD_centerOfMass	79
#define EXPOSED_FIELD_centerOfRotation	80
#define EXPOSED_FIELD_channelCount	81
#define EXPOSED_FIELD_channelCountMode	82
#define EXPOSED_FIELD_channelInterpretation	83
#define EXPOSED_FIELD_channels	84
#define EXPOSED_FIELD_channelsEnabled	85
#define EXPOSED_FIELD_children	86
#define EXPOSED_FIELD_choice	87
#define EXPOSED_FIELD_clipBoundary	88
#define EXPOSED_FIELD_closed	89
#define EXPOSED_FIELD_collidables	90
#define EXPOSED_FIELD_collide	91
#define EXPOSED_FIELD_collider	92
#define EXPOSED_FIELD_collisionType	93
#define EXPOSED_FIELD_color	94
#define EXPOSED_FIELD_colorMode	95
#define EXPOSED_FIELD_colorSteps	96
#define EXPOSED_FIELD_coneGain	97
#define EXPOSED_FIELD_coneInnerAngle	98
#define EXPOSED_FIELD_coneOuterAngle	99
#define EXPOSED_FIELD_connect	100
#define EXPOSED_FIELD_constantForceMix	101
#define EXPOSED_FIELD_contactNormal	102
#define EXPOSED_FIELD_contactSurfaceThickness	103
#define EXPOSED_FIELD_context	104
#define EXPOSED_FIELD_contourStepSize	105
#define EXPOSED_FIELD_controlPoint	106
#define EXPOSED_FIELD_coolColor	107
#define EXPOSED_FIELD_coord	108
#define EXPOSED_FIELD_coordIndex	109
#define EXPOSED_FIELD_createParticles	110
#define EXPOSED_FIELD_crossSectionCurve	111
#define EXPOSED_FIELD_cryptoKeyID	112
#define EXPOSED_FIELD_cryptoSystem	113
#define EXPOSED_FIELD_currentTime	114
#define EXPOSED_FIELD_curve	115
#define EXPOSED_FIELD_cutOffAngle	116
#define EXPOSED_FIELD_cycleInterval	117
#define EXPOSED_FIELD_data	118
#define EXPOSED_FIELD_dataLength	119
#define EXPOSED_FIELD_deadReckoning	120
#define EXPOSED_FIELD_defaultValue	121
#define EXPOSED_FIELD_delayTime	122
#define EXPOSED_FIELD_deletionAllowed	123
#define EXPOSED_FIELD_depth	124
#define EXPOSED_FIELD_description	125
#define EXPOSED_FIELD_desiredAngularVelocity1	126
#define EXPOSED_FIELD_desiredAngularVelocity2	127
#define EXPOSED_FIELD_destination	128
#define EXPOSED_FIELD_detonationLocation	129
#define EXPOSED_FIELD_detonationRelativeLocation	130
#define EXPOSED_FIELD_detonationResult	131
#define EXPOSED_FIELD_detune	132
#define EXPOSED_FIELD_diffuse	133
#define EXPOSED_FIELD_diffuseColor	134
#define EXPOSED_FIELD_diffuseTexture	135
#define EXPOSED_FIELD_diffuseTextureChannel	136
#define EXPOSED_FIELD_dimensions	137
#define EXPOSED_FIELD_direction	138
#define EXPOSED_FIELD_disableAngularSpeed	139
#define EXPOSED_FIELD_disableLinearSpeed	140
#define EXPOSED_FIELD_disableTime	141
#define EXPOSED_FIELD_diskAngle	142
#define EXPOSED_FIELD_displacements	143
#define EXPOSED_FIELD_displacers	144
#define EXPOSED_FIELD_displayed	145
#define EXPOSED_FIELD_distanceModel	146
#define EXPOSED_FIELD_dopplerFactor	147
#define EXPOSED_FIELD_duration	148
#define EXPOSED_FIELD_easeInEaseOut	149
#define EXPOSED_FIELD_edgeColor	150
#define EXPOSED_FIELD_effects	151
#define EXPOSED_FIELD_emissiveColor	152
#define EXPOSED_FIELD_emissiveTexture	153
#define EXPOSED_FIELD_emissiveTextureChannel	154
#define EXPOSED_FIELD_enabled	155
#define EXPOSED_FIELD_enabledAxes	156
#define EXPOSED_FIELD_encodingScheme	157
#define EXPOSED_FIELD_endFrame	158
#define EXPOSED_FIELD_entities	159
#define EXPOSED_FIELD_entityCategory	160
#define EXPOSED_FIELD_entityCountry	161
#define EXPOSED_FIELD_entityDomain	162
#define EXPOSED_FIELD_entityExtra	163
#define EXPOSED_FIELD_entityID	164
#define EXPOSED_FIELD_entityKind	165
#define EXPOSED_FIELD_entitySpecific	166
#define EXPOSED_FIELD_entitySubCategory	167
#define EXPOSED_FIELD_errorCorrection	168
#define EXPOSED_FIELD_eventApplicationID	169
#define EXPOSED_FIELD_eventEntityID	170
#define EXPOSED_FIELD_eventNumber	171
#define EXPOSED_FIELD_eventSiteID	172
#define EXPOSED_FIELD_farDistance	173
#define EXPOSED_FIELD_fftSize	174
#define EXPOSED_FIELD_fieldOfView	175
#define EXPOSED_FIELD_fillProperties	176
#define EXPOSED_FIELD_filled	177
#define EXPOSED_FIELD_filter	178
#define EXPOSED_FIELD_finiteRotationAxis	179
#define EXPOSED_FIELD_fireMissionIndex	180
#define EXPOSED_FIELD_fired1	181
#define EXPOSED_FIELD_fired2	182
#define EXPOSED_FIELD_firingRange	183
#define EXPOSED_FIELD_firingRate	184
#define EXPOSED_FIELD_fixed	185
#define EXPOSED_FIELD_floatInp	186
#define EXPOSED_FIELD_fogCoord	187
#define EXPOSED_FIELD_fogType	188
#define EXPOSED_FIELD_fontStyle	189
#define EXPOSED_FIELD_force	190
#define EXPOSED_FIELD_forceID	191
#define EXPOSED_FIELD_forceOutput	192
#define EXPOSED_FIELD_forces	193
#define EXPOSED_FIELD_fovMode	194
#define EXPOSED_FIELD_frameDuration	195
#define EXPOSED_FIELD_frameIncrement	196
#define EXPOSED_FIELD_frameIndex	197
#define EXPOSED_FIELD_frequency	198
#define EXPOSED_FIELD_frequencyBinCount	199
#define EXPOSED_FIELD_frictionCoefficients	200
#define EXPOSED_FIELD_frictionDirection	201
#define EXPOSED_FIELD_front	202
#define EXPOSED_FIELD_frontTexture	203
#define EXPOSED_FIELD_frontUrl	204
#define EXPOSED_FIELD_function	205
#define EXPOSED_FIELD_fuse	206
#define EXPOSED_FIELD_gain	207
#define EXPOSED_FIELD_geoCenter	208
#define EXPOSED_FIELD_geoCoords	209
#define EXPOSED_FIELD_geometry	210
#define EXPOSED_FIELD_geometry1	211
#define EXPOSED_FIELD_geometry2	212
#define EXPOSED_FIELD_global	213
#define EXPOSED_FIELD_gotEvents	214
#define EXPOSED_FIELD_gradientThreshold	215
#define EXPOSED_FIELD_gradients	216
#define EXPOSED_FIELD_gravity	217
#define EXPOSED_FIELD_groundAngle	218
#define EXPOSED_FIELD_groundColor	219
#define EXPOSED_FIELD_gustiness	220
#define EXPOSED_FIELD_handler	221
#define EXPOSED_FIELD_hatchColor	222
#define EXPOSED_FIELD_hatchStyle	223
#define EXPOSED_FIELD_hatched	224
#define EXPOSED_FIELD_headlight	225
#define EXPOSED_FIELD_image	226
#define EXPOSED_FIELD_index	227
#define EXPOSED_FIELD_inertia	228
#define EXPOSED_FIELD_inputSource	229
#define EXPOSED_FIELD_int32Inp	230
#define EXPOSED_FIELD_integerKey	231
#define EXPOSED_FIELD_intensity	232
#define EXPOSED_FIELD_intensityThreshold	233
#define EXPOSED_FIELD_internal	234
#define EXPOSED_FIELD_isPickable	235
#define EXPOSED_FIELD_iterations	236
#define EXPOSED_FIELD_joints	237
#define EXPOSED_FIELD_jump	238
#define EXPOSED_FIELD_key	239
#define EXPOSED_FIELD_keyValue	240
#define EXPOSED_FIELD_keyVelocity	241
#define EXPOSED_FIELD_knee	242
#define EXPOSED_FIELD_knot	243
#define EXPOSED_FIELD_layers	244
#define EXPOSED_FIELD_layout	245
#define EXPOSED_FIELD_left	246
#define EXPOSED_FIELD_leftTexture	247
#define EXPOSED_FIELD_leftUrl	248
#define EXPOSED_FIELD_length	249
#define EXPOSED_FIELD_lengthOfModulationParameters	250
#define EXPOSED_FIELD_level	251
#define EXPOSED_FIELD_lifetimeVariation	252
#define EXPOSED_FIELD_lighting	253
#define EXPOSED_FIELD_limitOrientation	254
#define EXPOSED_FIELD_lineProperties	255
#define EXPOSED_FIELD_linearAcceleration	256
#define EXPOSED_FIELD_linearDampingFactor	257
#define EXPOSED_FIELD_linearVelocity	258
#define EXPOSED_FIELD_linetype	259
#define EXPOSED_FIELD_linewidthScaleFactor	260
#define EXPOSED_FIELD_listener	261
#define EXPOSED_FIELD_listenfor	262
#define EXPOSED_FIELD_llimit	263
#define EXPOSED_FIELD_loa	264
#define EXPOSED_FIELD_load	265
#define EXPOSED_FIELD_location	266
#define EXPOSED_FIELD_loop	267
#define EXPOSED_FIELD_loopEnd	268
#define EXPOSED_FIELD_loopStart	269
#define EXPOSED_FIELD_magnificationFilter	270
#define EXPOSED_FIELD_mapping	271
#define EXPOSED_FIELD_marking	272
#define EXPOSED_FIELD_mass	273
#define EXPOSED_FIELD_massDensityModel	274
#define EXPOSED_FIELD_matchCriterion	275
#define EXPOSED_FIELD_material	276
#define EXPOSED_FIELD_matrix	277
#define EXPOSED_FIELD_maxAngle	278
#define EXPOSED_FIELD_maxAngle1	279
#define EXPOSED_FIELD_maxBack	280
#define EXPOSED_FIELD_maxChannelCount	281
#define EXPOSED_FIELD_maxCorrectionSpeed	282
#define EXPOSED_FIELD_maxDecibels	283
#define EXPOSED_FIELD_maxDistance	284
#define EXPOSED_FIELD_maxExtent	285
#define EXPOSED_FIELD_maxFront	286
#define EXPOSED_FIELD_maxParticles	287
#define EXPOSED_FIELD_maxPosition	288
#define EXPOSED_FIELD_maxSeparation	289
#define EXPOSED_FIELD_maxTorque1	290
#define EXPOSED_FIELD_maxTorque2	291
#define EXPOSED_FIELD_metadata	292
#define EXPOSED_FIELD_metallic	293
#define EXPOSED_FIELD_metallicRoughnessTexture	294
#define EXPOSED_FIELD_metallicRoughnessTextureChannel	295
#define EXPOSED_FIELD_minAngle	296
#define EXPOSED_FIELD_minAngle1	297
#define EXPOSED_FIELD_minBack	298
#define EXPOSED_FIELD_minBounceSpeed	299
#define EXPOSED_FIELD_minDecibels	300
#define EXPOSED_FIELD_minFront	301
#define EXPOSED_FIELD_minPosition	302
#define EXPOSED_FIELD_minSeparation	303
#define EXPOSED_FIELD_minificationFilter	304
#define EXPOSED_FIELD_mode	305
#define EXPOSED_FIELD_modulationTypeDetail	306
#define EXPOSED_FIELD_modulationTypeMajor	307
#define EXPOSED_FIELD_modulationTypeSpreadSpectrum	308
#define EXPOSED_FIELD_modulationTypeSystem	309
#define EXPOSED_FIELD_momentsOfInertia	310
#define EXPOSED_FIELD_motions	311
#define EXPOSED_FIELD_motionsEnabled	312
#define EXPOSED_FIELD_mozAudioChannelType	313
#define EXPOSED_FIELD_multicastRelayHost	314
#define EXPOSED_FIELD_multicastRelayPort	315
#define EXPOSED_FIELD_munitionApplicationID	316
#define EXPOSED_FIELD_munitionEndPoint	317
#define EXPOSED_FIELD_munitionEntityID	318
#define EXPOSED_FIELD_munitionQuantity	319
#define EXPOSED_FIELD_munitionSiteID	320
#define EXPOSED_FIELD_munitionStartPoint	321
#define EXPOSED_FIELD_name	322
#define EXPOSED_FIELD_navType	323
#define EXPOSED_FIELD_nearDistance	324
#define EXPOSED_FIELD_networkMode	325
#define EXPOSED_FIELD_normal	326
#define EXPOSED_FIELD_normalTexture	327
#define EXPOSED_FIELD_normalTextureChannel	328
#define EXPOSED_FIELD_normalize	329
#define EXPOSED_FIELD_normalizeVelocity	330
#define EXPOSED_FIELD_numberOfChannels	331
#define EXPOSED_FIELD_numberOfInputs	332
#define EXPOSED_FIELD_numberOfOutputs	333
#define EXPOSED_FIELD_objectType	334
#define EXPOSED_FIELD_offset	335
#define EXPOSED_FIELD_offsetUnits	336
#define EXPOSED_FIELD_on	337
#define EXPOSED_FIELD_opacityFactor	338
#define EXPOSED_FIELD_order	339
#define EXPOSED_FIELD_orientation	340
#define EXPOSED_FIELD_orthogonalColor	341
#define EXPOSED_FIELD_oversample	342
#define EXPOSED_FIELD_panningModel	343
#define EXPOSED_FIELD_parallelColor	344
#define EXPOSED_FIELD_parameter	345
#define EXPOSED_FIELD_particleLifetime	346
#define EXPOSED_FIELD_particleSize	347
#define EXPOSED_FIELD_parts	348
#define EXPOSED_FIELD_pauseTime	349
#define EXPOSED_FIELD_pickTarget	350
#define EXPOSED_FIELD_pickable	351
#define EXPOSED_FIELD_pickingGeometry	352
#define EXPOSED_FIELD_pitch	353
#define EXPOSED_FIELD_plane	354
#define EXPOSED_FIELD_playbackRate	355
#define EXPOSED_FIELD_point	356
#define EXPOSED_FIELD_pointProperties	357
#define EXPOSED_FIELD_pointSize	358
#define EXPOSED_FIELD_pointSizeAttenuation	359
#define EXPOSED_FIELD_pointSizeMaxValue	360
#define EXPOSED_FIELD_pointSizeMinValue	361
#define EXPOSED_FIELD_pointSizeScaleFactor	362
#define EXPOSED_FIELD_port	363
#define EXPOSED_FIELD_position	364
#define EXPOSED_FIELD_power	365
#define EXPOSED_FIELD_preferAccuracy	366
#define EXPOSED_FIELD_priority	367
#define EXPOSED_FIELD_profileCurve	368
#define EXPOSED_FIELD_programs	369
#define EXPOSED_FIELD_protocol	370
#define EXPOSED_FIELD_radioEntityTypeCategory	371
#define EXPOSED_FIELD_radioEntityTypeCountry	372
#define EXPOSED_FIELD_radioEntityTypeDomain	373
#define EXPOSED_FIELD_radioEntityTypeKind	374
#define EXPOSED_FIELD_radioEntityTypeNomenclature	375
#define EXPOSED_FIELD_radioEntityTypeNomenclatureVersion	376
#define EXPOSED_FIELD_radioID	377
#define EXPOSED_FIELD_radius	378
#define EXPOSED_FIELD_ratio	379
#define EXPOSED_FIELD_readInterval	380
#define EXPOSED_FIELD_receivedPower	381
#define EXPOSED_FIELD_receiverState	382
#define EXPOSED_FIELD_reduction	383
#define EXPOSED_FIELD_refDistance	384
#define EXPOSED_FIELD_refraction	385
#define EXPOSED_FIELD_relativeAntennaLocation	386
#define EXPOSED_FIELD_release	387
#define EXPOSED_FIELD_renderStyle	388
#define EXPOSED_FIELD_resumeTime	389
#define EXPOSED_FIELD_retainUserOffsets	390
#define EXPOSED_FIELD_retainedOpacity	391
#define EXPOSED_FIELD_right	392
#define EXPOSED_FIELD_rightTexture	393
#define EXPOSED_FIELD_rightUrl	394
#define EXPOSED_FIELD_rolloffFactor	395
#define EXPOSED_FIELD_rotation	396
#define EXPOSED_FIELD_roughness	397
#define EXPOSED_FIELD_sampleRate	398
#define EXPOSED_FIELD_samples	399
#define EXPOSED_FIELD_scale	400
#define EXPOSED_FIELD_scaleMode	401
#define EXPOSED_FIELD_scaleOrientation	402
#define EXPOSED_FIELD_segmentEnabled	403
#define EXPOSED_FIELD_segmentIdentifiers	404
#define EXPOSED_FIELD_segments	405
#define EXPOSED_FIELD_separateBackColor	406
#define EXPOSED_FIELD_shaders	407
#define EXPOSED_FIELD_shadows	408
#define EXPOSED_FIELD_shape	409
#define EXPOSED_FIELD_shininess	410
#define EXPOSED_FIELD_silhouetteBoundaryOpacity	411
#define EXPOSED_FIELD_silhouetteRetainedOpacity	412
#define EXPOSED_FIELD_silhouetteSharpness	413
#define EXPOSED_FIELD_singleton	414
#define EXPOSED_FIELD_siteID	415
#define EXPOSED_FIELD_sites	416
#define EXPOSED_FIELD_size	417
#define EXPOSED_FIELD_sizeUnits	418
#define EXPOSED_FIELD_skeletalConfiguration	419
#define EXPOSED_FIELD_skeleton	420
#define EXPOSED_FIELD_skin	421
#define EXPOSED_FIELD_skinCoord	422
#define EXPOSED_FIELD_skinCoordIndex	423
#define EXPOSED_FIELD_skinCoordWeight	424
#define EXPOSED_FIELD_skinNormal	425
#define EXPOSED_FIELD_skyAngle	426
#define EXPOSED_FIELD_skyColor	427
#define EXPOSED_FIELD_sliderForce	428
#define EXPOSED_FIELD_slipCoefficients	429
#define EXPOSED_FIELD_slipFactors	430
#define EXPOSED_FIELD_smoothingTimeConstant	431
#define EXPOSED_FIELD_softnessConstantForceMix	432
#define EXPOSED_FIELD_softnessErrorCorrection	433
#define EXPOSED_FIELD_source	434
#define EXPOSED_FIELD_specular	435
#define EXPOSED_FIELD_specularColor	436
#define EXPOSED_FIELD_specularShininessTexture	437
#define EXPOSED_FIELD_specularShininessTextureChannel	438
#define EXPOSED_FIELD_specularTexture	439
#define EXPOSED_FIELD_speed	440
#define EXPOSED_FIELD_speedOfSound	441
#define EXPOSED_FIELD_startFrame	442
#define EXPOSED_FIELD_startTime	443
#define EXPOSED_FIELD_stiffness	444
#define EXPOSED_FIELD_stop1Bounce	445
#define EXPOSED_FIELD_stop1ErrorCorrection	446
#define EXPOSED_FIELD_stop2Bounce	447
#define EXPOSED_FIELD_stop2ErrorCorrection	448
#define EXPOSED_FIELD_stop3Bounce	449
#define EXPOSED_FIELD_stop3ErrorCorrection	450
#define EXPOSED_FIELD_stopBounce	451
#define EXPOSED_FIELD_stopBounce1	452
#define EXPOSED_FIELD_stopConstantForceMix1	453
#define EXPOSED_FIELD_stopErrorCorrection	454
#define EXPOSED_FIELD_stopErrorCorrection1	455
#define EXPOSED_FIELD_stopTime	456
#define EXPOSED_FIELD_stream	457
#define EXPOSED_FIELD_string	458
#define EXPOSED_FIELD_stringInp	459
#define EXPOSED_FIELD_styleEnd	460
#define EXPOSED_FIELD_styleStart	461
#define EXPOSED_FIELD_summary	462
#define EXPOSED_FIELD_surface	463
#define EXPOSED_FIELD_surfaceNormals	464
#define EXPOSED_FIELD_surfaceSpeed	465
#define EXPOSED_FIELD_surfaceTolerance	466
#define EXPOSED_FIELD_surfaceValues	467
#define EXPOSED_FIELD_suspensionErrorCorrection	468
#define EXPOSED_FIELD_suspensionForce	469
#define EXPOSED_FIELD_talksTo	470
#define EXPOSED_FIELD_targetObject	471
#define EXPOSED_FIELD_tau	472
#define EXPOSED_FIELD_tdlType	473
#define EXPOSED_FIELD_tessellation	474
#define EXPOSED_FIELD_tessellationScale	475
#define EXPOSED_FIELD_texCoord	476
#define EXPOSED_FIELD_texture	477
#define EXPOSED_FIELD_textureCompression	478
#define EXPOSED_FIELD_texturePriority	479
#define EXPOSED_FIELD_textureTransform	480
#define EXPOSED_FIELD_threshold	481
#define EXPOSED_FIELD_timeOut	482
#define EXPOSED_FIELD_tolerance	483
#define EXPOSED_FIELD_top	484
#define EXPOSED_FIELD_topTexture	485
#define EXPOSED_FIELD_topUrl	486
#define EXPOSED_FIELD_torques	487
#define EXPOSED_FIELD_trajectoryCurve	488
#define EXPOSED_FIELD_transferFunction	489
#define EXPOSED_FIELD_transitionTime	490
#define EXPOSED_FIELD_transitionType	491
#define EXPOSED_FIELD_translation	492
#define EXPOSED_FIELD_transmitFrequencyBandwidth	493
#define EXPOSED_FIELD_transmitState	494
#define EXPOSED_FIELD_transmitterApplicationID	495
#define EXPOSED_FIELD_transmitterEntityID	496
#define EXPOSED_FIELD_transmitterRadioID	497
#define EXPOSED_FIELD_transmitterSiteID	498
#define EXPOSED_FIELD_transparency	499
#define EXPOSED_FIELD_trimmingContour	500
#define EXPOSED_FIELD_turbulence	501
#define EXPOSED_FIELD_type	502
#define EXPOSED_FIELD_type16dashes	503
#define EXPOSED_FIELD_type16wiggles	504
#define EXPOSED_FIELD_uDimension	505
#define EXPOSED_FIELD_uOrder	506
#define EXPOSED_FIELD_uTessellation	507
#define EXPOSED_FIELD_ulimit	508
#define EXPOSED_FIELD_upVector	509
#define EXPOSED_FIELD_update	510
#define EXPOSED_FIELD_url	511
#define EXPOSED_FIELD_useFiniteRotation	512
#define EXPOSED_FIELD_useGeometry	513
#define EXPOSED_FIELD_useGlobalGravity	514
#define EXPOSED_FIELD_vDimension	515
#define EXPOSED_FIELD_vOrder	516
#define EXPOSED_FIELD_vTessellation	517
#define EXPOSED_FIELD_value	518
#define EXPOSED_FIELD_values	519
#define EXPOSED_FIELD_variation	520
#define EXPOSED_FIELD_vector	521
#define EXPOSED_FIELD_velocity	522
#define EXPOSED_FIELD_version	523
#define EXPOSED_FIELD_vertexCount	524
#define EXPOSED_FIELD_vertices	525
#define EXPOSED_FIELD_viewpoints	526
#define EXPOSED_FIELD_viewport	527
#define EXPOSED_FIELD_visibilityLimit	528
#define EXPOSED_FIELD_visibilityRange	529
#define EXPOSED_FIELD_visible	530
#define EXPOSED_FIELD_visibles	531
#define EXPOSED_FIELD_voxels	532
#define EXPOSED_FIELD_warhead	533
#define EXPOSED_FIELD_warmColor	534
#define EXPOSED_FIELD_watchList	535
#define EXPOSED_FIELD_weight	536
#define EXPOSED_FIELD_weightConstant1	537
#define EXPOSED_FIELD_weightConstant2	538
#define EXPOSED_FIELD_weightTransferFunction1	539
#define EXPOSED_FIELD_weightTransferFunction2	540
#define EXPOSED_FIELD_whichChoice	541
#define EXPOSED_FIELD_whichGeometry	542
#define EXPOSED_FIELD_writeInterval	543
#define EXPOSED_FIELD_yScale	544


/* Table of built-in fieldIds */
extern const char *FIELD[];
extern const int FIELD_COUNT;
#define FIELD_autoCalc	0
#define FIELD_bboxCenter	1
#define FIELD_bboxSize	2
#define FIELD_beginCap	3
#define FIELD_bottomRadius	4
#define FIELD_category	5
#define FIELD_ccw	6
#define FIELD_child1Url	7
#define FIELD_child2Url	8
#define FIELD_child3Url	9
#define FIELD_child4Url	10
#define FIELD_closed	11
#define FIELD_closureType	12
#define FIELD_collidable	13
#define FIELD_colorIndex	14
#define FIELD_colorKey	15
#define FIELD_colorPerVertex	16
#define FIELD_colorRamp	17
#define FIELD_convex	18
#define FIELD_coordIndex	19
#define FIELD_country	20
#define FIELD_creaseAngle	21
#define FIELD_crossSection	22
#define FIELD_directOutput	23
#define FIELD_domain	24
#define FIELD_duration	25
#define FIELD_emitter	26
#define FIELD_endAngle	27
#define FIELD_endCap	28
#define FIELD_extra	29
#define FIELD_family	30
#define FIELD_fanCount	31
#define FIELD_forceTransitions	32
#define FIELD_generateMipMaps	33
#define FIELD_geoGridOrigin	34
#define FIELD_geoOrigin	35
#define FIELD_geoSystem	36
#define FIELD_geometryType	37
#define FIELD_height	38
#define FIELD_horizontal	39
#define FIELD_index	40
#define FIELD_info	41
#define FIELD_initialDestination	42
#define FIELD_initialValue	43
#define FIELD_innerRadius	44
#define FIELD_intersectionType	45
#define FIELD_justify	46
#define FIELD_kind	47
#define FIELD_knot	48
#define FIELD_language	49
#define FIELD_leftToRight	50
#define FIELD_lineSegments	51
#define FIELD_mass	52
#define FIELD_mustEvaluate	53
#define FIELD_normalIndex	54
#define FIELD_normalPerVertex	55
#define FIELD_numComponents	56
#define FIELD_order	57
#define FIELD_outerRadius	58
#define FIELD_phaseFunction	59
#define FIELD_physics	60
#define FIELD_planetId	61
#define FIELD_proxy	62
#define FIELD_range	63
#define FIELD_reference	64
#define FIELD_relativeHeight	65
#define FIELD_repeatR	66
#define FIELD_repeatS	67
#define FIELD_repeatT	68
#define FIELD_rootNode	69
#define FIELD_rootUrl	70
#define FIELD_rotateYUp	71
#define FIELD_rtpHeaderExpected	72
#define FIELD_sensorLocalOutput	73
#define FIELD_shape	74
#define FIELD_side	75
#define FIELD_size	76
#define FIELD_solid	77
#define FIELD_sortOrder	78
#define FIELD_spacing	79
#define FIELD_spatialize	80
#define FIELD_specific	81
#define FIELD_speedFactor	82
#define FIELD_spine	83
#define FIELD_startAngle	84
#define FIELD_stripCount	85
#define FIELD_style	86
#define FIELD_subcategory	87
#define FIELD_surfaceArea	88
#define FIELD_texCoordIndex	89
#define FIELD_texCoordKey	90
#define FIELD_texCoordRamp	91
#define FIELD_textureProperties	92
#define FIELD_title	93
#define FIELD_topToBottom	94
#define FIELD_type	95
#define FIELD_uClosed	96
#define FIELD_uKnot	97
#define FIELD_vClosed	98
#define FIELD_vKnot	99
#define FIELD_weightFunction1	100
#define FIELD_weightFunction2	101
#define FIELD_xDimension	102
#define FIELD_xSpacing	103
#define FIELD_zDimension	104
#define FIELD_zSpacing	105


/* Table of built-in keywords */
extern const char *KEYWORDS[];
extern const int KEYWORDS_COUNT;
#define KW_BROTO	0
#define KW_COMPONENT	1
#define KW_DEF	2
#define KW_EXPORT	3
#define KW_EXTERNBROTO	4
#define KW_EXTERNPROTO	5
#define KW_FALSE	6
#define KW_IMPORT	7
#define KW_IS	8
#define KW_META	9
#define KW_NULL	10
#define KW_PROFILE	11
#define KW_PROTO	12
#define KW_ROUTE	13
#define KW_TO	14
#define KW_TRUE	15
#define KW_UNIT	16
#define KW_USE	17
#define KW_eventIn	18
#define KW_eventOut	19
#define KW_exposedField	20
#define KW_false	21
#define KW_field	22
#define KW_initializeOnly	23
#define KW_inputOnly	24
#define KW_inputOutput	25
#define KW_outputOnly	26
#define KW_true	27

const char *stringKeywordType(int st);

/* Table of built-in profiles */
extern const char *PROFILES[];
extern const int PROFILES_COUNT;
#define PRO_CADInterchange	0
#define PRO_Core	1
#define PRO_Full	2
#define PRO_Immersive	3
#define PRO_Interactive	4
#define PRO_Interchange	5
#define PRO_MPEG4	6

const char *stringProfileType(int st);

/* Table of built-in components */
extern const char *COMPONENTS[];
extern const int COMPONENTS_COUNT;
#define COM_CADGeometry	0
#define COM_Core	1
#define COM_CubeMapTexturing	2
#define COM_DIS	3
#define COM_EnvironmentalEffects	4
#define COM_EnvironmentalSensor	5
#define COM_EventUtilities	6
#define COM_Followers	7
#define COM_Geometry2D	8
#define COM_Geometry3D	9
#define COM_Geospatial	10
#define COM_Grouping	11
#define COM_HAnim	12
#define COM_Interpolation	13
#define COM_KeyDeviceSensor	14
#define COM_Layering	15
#define COM_Layout	16
#define COM_Lighting	17
#define COM_NURBS	18
#define COM_Navigation	19
#define COM_Networking	20
#define COM_ParticleSystems	21
#define COM_Picking	22
#define COM_PointDeviceSensor	23
#define COM_ProjectiveTextureMapping	24
#define COM_Rendering	25
#define COM_RigidBodyPhysics	26
#define COM_Scripting	27
#define COM_Shaders	28
#define COM_Shape	29
#define COM_Sound	30
#define COM_Text	31
#define COM_Texturing	32
#define COM_Texturing3D	33
#define COM_Time	34
#define COM_VolumeRendering	35

const char *stringComponentType(int st);

/* Table of built-in PROTO keywords */
extern const char *PROTOKEYWORDS[];
extern const int PROTOKEYWORDS_COUNT;
#define PKW_initializeOnly	0
#define PKW_inputOnly	1
#define PKW_outputOnly	2
#define PKW_inputOutput	3
#define PKW_field	4
#define PKW_eventIn	5
#define PKW_eventOut	6
#define PKW_exposedField	7

const char *stringPROTOKeywordType(int st);

/* Table of built-in MULTITEXTUREMODE keywords */
extern const char *MULTITEXTUREMODE[];
extern const int MULTITEXTUREMODE_COUNT;
#define MTMODE_ADD	1
#define MTMODE_ADDSIGNED	2
#define MTMODE_ADDSIGNED2X	3
#define MTMODE_ADDSMOOTH	4
#define MTMODE_BLENDCURRENTALPHA	5
#define MTMODE_BLENDDIFFUSEALPHA	6
#define MTMODE_BLENDFACTORALPHA	7
#define MTMODE_BLENDTEXTUREALPHA	8
#define MTMODE_DOTPRODUCT3	9
#define MTMODE_MODULATE	10
#define MTMODE_MODULATE2X	11
#define MTMODE_MODULATE4X	12
#define MTMODE_MODULATEALPHA_ADDCOLOR	13
#define MTMODE_MODULATEINVALPHA_ADDCOLOR	14
#define MTMODE_MODULATEINVCOLOR_ADDALPHA	15
#define MTMODE_OFF	16
#define MTMODE_REPLACE	17
#define MTMODE_SELECTARG1	18
#define MTMODE_SELECTARG2	19
#define MTMODE_SUBTRACT	20

const char *stringMULTITEXTUREMODEType(int st);
#define MULTITEXTUREDefs " \
#define MTMODE_ADD	1\n \
#define MTMODE_ADDSIGNED	2\n \
#define MTMODE_ADDSIGNED2X	3\n \
#define MTMODE_ADDSMOOTH	4\n \
#define MTMODE_BLENDCURRENTALPHA	5\n \
#define MTMODE_BLENDDIFFUSEALPHA	6\n \
#define MTMODE_BLENDFACTORALPHA	7\n \
#define MTMODE_BLENDTEXTUREALPHA	8\n \
#define MTMODE_DOTPRODUCT3	9\n \
#define MTMODE_MODULATE	10\n \
#define MTMODE_MODULATE2X	11\n \
#define MTMODE_MODULATE4X	12\n \
#define MTMODE_MODULATEALPHA_ADDCOLOR	13\n \
#define MTMODE_MODULATEINVALPHA_ADDCOLOR	14\n \
#define MTMODE_MODULATEINVCOLOR_ADDALPHA	15\n \
#define MTMODE_OFF	16\n \
#define MTMODE_REPLACE	17\n \
#define MTMODE_SELECTARG1	18\n \
#define MTMODE_SELECTARG2	19\n \
#define MTMODE_SUBTRACT	20\n \
";

/* Table of built-in MULTITEXTURESOURCE keywords */
extern const char *MULTITEXTURESOURCE[];
extern const int MULTITEXTURESOURCE_COUNT;
#define MTSRC_DIFFUSE	1
#define MTSRC_FACTOR	2
#define MTSRC_SPECULAR	3


/* Table of built-in TEXTURECOORDINATEGENERATOR keywords */
extern const char *TEXTURECOORDINATEGENERATOR[];
extern const int TEXTURECOORDINATEGENERATOR_COUNT;
#define TCGT_CAMERASPACENORMAL    0
#define TCGT_CAMERASPACEPOSITION    1
#define TCGT_CAMERASPACEREFLECTION    2
#define TCGT_COORD    3
#define TCGT_COORD_EYE    4
#define TCGT_NOISE    5
#define TCGT_NOISE_EYE    6
#define TCGT_SPHERE    7
#define TCGT_SPHERE_LOCAL    8
#define TCGT_SPHERE_REFLECT    9
#define TCGT_SPHERE_REFLECT_LOCAL    10

#define TEXTURECOORDINATEGENERATORDefs " \
#define TCGT_CAMERASPACENORMAL    0\n \
#define TCGT_CAMERASPACEPOSITION    1\n \
#define TCGT_CAMERASPACEREFLECTION    2\n \
#define TCGT_COORD    3\n \
#define TCGT_COORD_EYE    4\n \
#define TCGT_NOISE    5\n \
#define TCGT_NOISE_EYE    6\n \
#define TCGT_SPHERE    7\n \
#define TCGT_SPHERE_LOCAL    8\n \
#define TCGT_SPHERE_REFLECT    9\n \
#define TCGT_SPHERE_REFLECT_LOCAL    10\n \
";
const char *stringMULTITEXTURESOURCEType(int st);

/* Table of built-in MULTITEXTUREFUNCTION keywords */
extern const char *MULTITEXTUREFUNCTION[];
extern const int MULTITEXTUREFUNCTION_COUNT;
#define MTFN_ALPHAREPLICATE	0
#define MTFN_COMPLEMENT	1

const char *stringMULTITEXTUREFUNCTIONType(int st);

/* Table of built-in X3DSPECIAL keywords */
extern const char *X3DSPECIAL[];
extern const int X3DSPECIAL_COUNT;
#define X3DSP_EXPORT	0
#define X3DSP_ExternProtoDeclare	1
#define X3DSP_Header	2
#define X3DSP_IMPORT	3
#define X3DSP_IS	4
#define X3DSP_ProtoBody	5
#define X3DSP_ProtoDeclare	6
#define X3DSP_ProtoInstance	7
#define X3DSP_ProtoInterface	8
#define X3DSP_ROUTE	9
#define X3DSP_Scene	10
#define X3DSP_X3D	11
#define X3DSP_component	12
#define X3DSP_connect	13
#define X3DSP_field	14
#define X3DSP_fieldValue	15
#define X3DSP_head	16
#define X3DSP_meta	17
#define X3DSP_unit	18

const char *stringX3DSPECIALType(int st);

/* Table of built-in TEXTUREBOUNDARY keywords */
extern const char *TEXTUREBOUNDARYKEYWORDS[];
extern const int TEXTUREBOUNDARYKEYWORDS_COUNT;
#define TB_CLAMP	0
#define TB_CLAMP_TO_BOUNDARY	1
#define TB_CLAMP_TO_EDGE	2
#define TB_MIRRORED_REPEAT	3
#define TB_REPEAT	4

const char *stringTEXTUREBOUNDARYKeywordType(int st);

/* Table of built-in TEXTUREMAGNIFICATION keywords */
extern const char *TEXTUREMAGNIFICATIONKEYWORDS[];
extern const int TEXTUREMAGNIFICATIONKEYWORDS_COUNT;
#define TMAG_AVG_PIXEL	0
#define TMAG_DEFAULT	1
#define TMAG_FASTEST	2
#define TMAG_NEAREST_PIXEL	3
#define TMAG_NICEST	4

const char *stringTEXTUREMAGNIFICATIONKeywordType(int st);

/* Table of built-in TEXTUREMINIFICATION keywords */
extern const char *TEXTUREMINIFICATIONKEYWORDS[];
extern const int TEXTUREMINIFICATIONKEYWORDS_COUNT;
#define TMIN_AVG_PIXEL	0
#define TMIN_AVG_PIXEL_AVG_MIPMAP	1
#define TMIN_AVG_PIXEL_NEAREST_MIPMAP	2
#define TMIN_DEFAULT	3
#define TMIN_FASTEST	4
#define TMIN_NEAREST_PIXEL	5
#define TMIN_NEAREST_PIXEL_AVG_MIPMAP	6
#define TMIN_NEAREST_PIXEL_NEAREST_MIPMAP	7
#define TMIN_NICEST	8

const char *stringTEXTUREMINIFICATIONKeywordType(int st);

/* Table of built-in TEXTURECOMPRESSION keywords */
extern const char *TEXTURECOMPRESSIONKEYWORDS[];
extern const int TEXTURECOMPRESSIONKEYWORDS_COUNT;
#define TC_DEFAULT	0
#define TC_FASTEST	1
#define TC_HIGH	2
#define TC_LOW	3
#define TC_MEDIUM	4
#define TC_NICEST	5

const char *stringTEXTURECOMPRESSIONKeywordType(int st);

/* Table of built-in GEOELLIPSOID keywords */
extern const char *GEOELLIPSOID[];
extern const int GEOELLIPSOID_COUNT;
#define GEOEL_AA	0
#define GEOEL_AM	1
#define GEOEL_AN	2
#define GEOEL_BN	3
#define GEOEL_BR	4
#define GEOEL_CC	5
#define GEOEL_CD	6
#define GEOEL_EA	7
#define GEOEL_EB	8
#define GEOEL_EC	9
#define GEOEL_ED	10
#define GEOEL_EE	11
#define GEOEL_EF	12
#define GEOEL_FA	13
#define GEOEL_HE	14
#define GEOEL_HO	15
#define GEOEL_ID	16
#define GEOEL_IN	17
#define GEOEL_KA	18
#define GEOEL_RF	19
#define GEOEL_SA	20
#define GEOEL_WD	21
#define GEOEL_WE	22

const char *stringGEOELLIPSOIDType(int st);

/* Table of built-in GEOSPATIAL keywords */
extern const char *GEOSPATIAL[];
extern const int GEOSPATIAL_COUNT;
#define GEOSP_3TM	0
#define GEOSP_A	1
#define GEOSP_B	2
#define GEOSP_F	3
#define GEOSP_GC	4
#define GEOSP_GD	5
#define GEOSP_IF	6
#define GEOSP_P	7
#define GEOSP_R	8
#define GEOSP_UTM	9
#define GEOSP_WGS84	10
#define GEOSP_WM	11
#define GEOSP_coordinateSystem	12
#define GEOSP_copyright	13
#define GEOSP_dataFormat	14
#define GEOSP_dataUrl	15
#define GEOSP_date	16
#define GEOSP_description	17
#define GEOSP_ellipsoid	18
#define GEOSP_extent	19
#define GEOSP_horizontalDatum	20
#define GEOSP_metadataFormat	21
#define GEOSP_originator	22
#define GEOSP_resolution	23
#define GEOSP_title	24
#define GEOSP_verticalDatum	25

const char *stringGEOSPATIALType(int st);
char mapFieldTypeToEAItype (int st);
int mapEAItypeToFieldType (char st);
int convertToSFType (int st);

/* Table of built-in fieldIds */
extern const char *FIELDTYPES[];
extern const int FIELDTYPES_COUNT;
#define FIELDTYPE_SFFloat	0
#define FIELDTYPE_MFFloat	1
#define FIELDTYPE_SFRotation	2
#define FIELDTYPE_MFRotation	3
#define FIELDTYPE_SFVec3f	4
#define FIELDTYPE_MFVec3f	5
#define FIELDTYPE_SFBool	6
#define FIELDTYPE_MFBool	7
#define FIELDTYPE_SFInt32	8
#define FIELDTYPE_MFInt32	9
#define FIELDTYPE_SFNode	10
#define FIELDTYPE_MFNode	11
#define FIELDTYPE_SFColor	12
#define FIELDTYPE_MFColor	13
#define FIELDTYPE_SFColorRGBA	14
#define FIELDTYPE_MFColorRGBA	15
#define FIELDTYPE_SFTime	16
#define FIELDTYPE_MFTime	17
#define FIELDTYPE_SFString	18
#define FIELDTYPE_MFString	19
#define FIELDTYPE_SFVec2f	20
#define FIELDTYPE_MFVec2f	21
#define FIELDTYPE_FreeWRLPTR	22
#define FIELDTYPE_SFImage	23
#define FIELDTYPE_SFVec3d	24
#define FIELDTYPE_MFVec3d	25
#define FIELDTYPE_SFDouble	26
#define FIELDTYPE_MFDouble	27
#define FIELDTYPE_SFMatrix3f	28
#define FIELDTYPE_MFMatrix3f	29
#define FIELDTYPE_SFMatrix3d	30
#define FIELDTYPE_MFMatrix3d	31
#define FIELDTYPE_SFMatrix4f	32
#define FIELDTYPE_MFMatrix4f	33
#define FIELDTYPE_SFMatrix4d	34
#define FIELDTYPE_MFMatrix4d	35
#define FIELDTYPE_SFVec2d	36
#define FIELDTYPE_MFVec2d	37
#define FIELDTYPE_SFVec4f	38
#define FIELDTYPE_MFVec4f	39
#define FIELDTYPE_SFVec4d	40
#define FIELDTYPE_MFVec4d	41
#define FIELDTYPE_FreeWRLThread	42

/*cstruct*/
struct Multi_Float { int n; float  *p; };
struct SFRotation { float c[4]; };
struct Multi_Rotation { int n; struct SFRotation  *p; };
struct SFVec3f { float c[3]; };
struct Multi_Vec3f { int n; struct SFVec3f  *p; };
/*cstruct*/
struct Multi_Bool { int n; int  *p; };
/*cstruct*/
struct Multi_Int32 { int n; int  *p; };

struct Multi_Node { int n; struct X3D_Node * *p; };
struct SFColor { float c[3]; };
struct Multi_Color { int n; struct SFColor  *p; };
struct SFColorRGBA { float c[4]; };
struct Multi_ColorRGBA { int n; struct SFColorRGBA  *p; };
/*cstruct*/
struct Multi_Time { int n; double  *p; };
/*cstruct*/
struct Multi_String { int n; struct Uni_String * *p; };
struct SFVec2f { float c[2]; };
struct Multi_Vec2f { int n; struct SFVec2f  *p; };
/*cstruct*/
/*cstruct*/
struct SFVec3d { double c[3]; };
struct Multi_Vec3d { int n; struct SFVec3d  *p; };
/*cstruct*/
struct Multi_Double { int n; double  *p; };
struct SFMatrix3f { float c[9]; };
struct Multi_Matrix3f { int n; struct SFMatrix3f  *p; };
struct SFMatrix3d { double c[9]; };
struct Multi_Matrix3d { int n; struct SFMatrix3d  *p; };
struct SFMatrix4f { float c[16]; };
struct Multi_Matrix4f { int n; struct SFMatrix4f  *p; };
struct SFMatrix4d { double c[16]; };
struct Multi_Matrix4d { int n; struct SFMatrix4d  *p; };
struct SFVec2d { double c[2]; };
struct Multi_Vec2d { int n; struct SFVec2d  *p; };
struct SFVec4f { float c[4]; };
struct Multi_Vec4f { int n; struct SFVec4f  *p; };
struct SFVec4d { double c[4]; };
struct Multi_Vec4d { int n; struct SFVec4d  *p; };
/*cstruct*/
const char *stringFieldtypeType(int st);

/* Table of built-in nodeIds */
extern const char *NODES[];
extern const int NODES_COUNT;

/* and now the structs for the nodetypes */ 
const char *stringNodeType(int st);

/* Table of defaultContainerFields for x3d parsing */
extern const short NODE_DEFAULT_CONTAINER[][7];

/* First, a generic struct, contains only the common elements - nicknames for convenience */
struct X3D_Node {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
};
#define X3D_POINTPROPERTIES(node) ((struct X3D_PointProperties*)node)
#define X3D_LINEPROPERTIES(node) ((struct X3D_LineProperties*)node)
#define X3D_FILLPROPERTIES(node) ((struct X3D_FillProperties*)node)
#define X3D_TEXTURE_TRANSFORM(node) ((struct X3D_TextureTransform*)node)
#define X3D_NODE(node) ((struct X3D_Node*)node)
#define X3D_APPEARANCE(node) ((struct X3D_Appearance*)node)
#define X3D_MATERIAL(node) ((struct X3D_Material*)node)
#define X3D_TWOSIDEDMATERIAL(node) ((struct X3D_TwoSidedMaterial*)node)
#define X3D_GROUP(node) ((struct X3D_Group*)node)
#define X3D_PROTO(node) ((struct X3D_Proto*)node)
#define X3D_PICKABLEGROUP(node) ((struct X3D_PickableGroup*)node)
#define X3D_POINTPICKSENSOR(node) ((struct X3D_PointPickSensor*)node)
#define X3D_STATICGROUP(node) ((struct X3D_StaticGroup*)node)
#define X3D_ANCHOR(node) ((struct X3D_Anchor*)node)
#define X3D_COLLISION(node) ((struct X3D_Collision*)node)
#define X3D_COMPOSEDSHADER(node) ((struct X3D_ComposedShader*)node)
#define X3D_EFFECT(node) ((struct X3D_Effect*)node)
#define X3D_PACKAGEDSHADER(node) ((struct X3D_PackagedShader*)node)
#define X3D_PROGRAMSHADER(node) ((struct X3D_ProgramShader*)node)
#define X3D_SHADERPROGRAM(node) ((struct X3D_ShaderProgram*)node)
#define X3D_SHAPE(node) ((struct X3D_Shape*)node)
#define X3D_VISIBILITYSENSOR(node) ((struct X3D_VisibilitySensor*)node)
#define X3D_BILLBOARD(node) ((struct X3D_Billboard*)node)
#define X3D_NAVIGATIONINFO(node) ((struct X3D_NavigationInfo*)node)
#define X3D_BACKGROUND(node) ((struct X3D_Background*)node)
#define X3D_TEXTUREBACKGROUND(node) ((struct X3D_TextureBackground*)node)
#define X3D_FOG(node) ((struct X3D_Fog*)node)
#define X3D_INLINE(node) ((struct X3D_Inline*)node)
#define X3D_SWITCH(node) ((struct X3D_Switch*)node)
#define X3D_CADASSEMBLY(node) ((struct X3D_CADAssembly*)node)
#define X3D_CADFACE(node) ((struct X3D_CADFace*)node)
#define X3D_CADLAYER(node) ((struct X3D_CADLayer*)node)
#define X3D_CADPART(node) ((struct X3D_CADPart*)node)
#define X3D_SCRIPT(node) ((struct X3D_Script*)node)
#define X3D_VIEWPOINT(node) ((struct X3D_Viewpoint*)node)
#define X3D_ORTHOVIEWPOINT(node) ((struct X3D_OrthoViewpoint*)node)
#define X3D_LODNODE(node) ((struct X3D_LOD*)node)
#define X3D_TRANSFORM(node) ((struct X3D_Transform*)node)
#define X3D_PROXIMITYSENSOR(node) ((struct X3D_ProximitySensor*)node)
#define X3D_POINTLIGHT(node) ((struct X3D_PointLight*)node)
#define X3D_SPOTLIGHT(node) ((struct X3D_SpotLight*)node)
#define X3D_DIRECTIONALLIGHT(node) ((struct X3D_DirectionalLight*)node)
#define X3D_ENVIRONMENTLIGHT(node) ((struct X3D_EnvironmentLight*)node)
#define X3D_INDEXEDFACESET(node) ((struct X3D_IndexedFaceSet*)node)
#define X3D_INDEXEDLINESET(node) ((struct X3D_IndexedLineSet*)node)
#define X3D_ELEVATIONGRID(node) ((struct X3D_ElevationGrid*)node)
#define X3D_INDEXEDTRIANGLEFANSET(node) ((struct X3D_IndexedTriangleFanSet*)node)
#define X3D_INDEXEDTRIANGLESET(node) ((struct X3D_IndexedTriangleSet*)node)
#define X3D_INDEXEDTRIANGLESTRIPSET(node) ((struct X3D_IndexedTriangleStripSet*)node)
#define X3D_TRIANGLEFANSET(node) ((struct X3D_TriangleFanSet*)node)
#define X3D_TRIANGLESET(node) ((struct X3D_TriangleSet*)node)
#define X3D_TRIANGLESTRIPSET(node) ((struct X3D_TriangleStripSet*)node)
#define X3D_QUADSET(node) ((struct X3D_QuadSet*)node)
#define X3D_INDEXEDQUADSET(node) ((struct X3D_IndexedQuadSet*)node)
#define X3D_ORTHOTEXTURE(node) ((struct X3D_OrthoTexture*)node)
#define X3D_VIEWPORT(node) ((struct X3D_Viewport*)node)
#define X3D_LAYOUT(node) ((struct X3D_Layout*)node)
#define X3D_LAYERSET(node) ((struct X3D_LayerSet*)node)
#define X3D_GEOORIGIN(node) ((struct X3D_GeoOrigin*)node)
#define X3D_GEOLOD(node) ((struct X3D_GeoLOD*)node)
#define X3D_GEOCOORD(node) ((struct X3D_GeoCoordinate*)node)
#define X3D_GEOVIEWPOINT(node) ((struct X3D_GeoViewpoint*)node)
#define X3D_GEOELEVATIONGRID(node) ((struct X3D_GeoElevationGrid*)node)
#define X3D_GEOLOCATION(node) ((struct X3D_GeoLocation*)node)
#define X3D_GEOTRANSFORM(node) ((struct X3D_GeoTransform*)node)
#define X3D_GEOPROXIMITYSENSOR(node) ((struct X3D_GeoProximitySensor*)node)
#define X3D_COLOR(node) ((struct X3D_Color*)node)
#define X3D_COORDINATE(node) ((struct X3D_Coordinate*)node)
#define X3D_COORDINATEINTERPOLATOR(node) ((struct X3D_CoordinateInterpolator*)node)
#define X3D_NORMAL(node) ((struct X3D_Normal*)node)
#define X3D_TEXTURECOORDINATE(node) ((struct X3D_TextureCoordinate*)node)
#define X3D_IMAGETEXTURE(node) ((struct X3D_ImageTexture*)node)
#define X3D_TEXTUREPROPERTIES(node) ((struct X3D_TextureProperties*)node)
#define X3D_PIXELTEXTURE(node) ((struct X3D_PixelTexture*)node)
#define X3D_TEXTUREPROJECTORPERSPECTIVE(node) ((struct X3D_TextureProjectorPerspective*)node)
#define X3D_TEXTUREPROJECTORPARALLEL(node) ((struct X3D_TextureProjectorParallel*)node)
void mark_event (struct X3D_Node *from, int totalptr);
#undef DEBUG_VALIDNODE
#ifdef DEBUG_VALIDNODE	
#define X3D_NODE_CHECK(node) checkNode(node,__FILE__,__LINE__)
#define MARK_EVENT(node,offset) mark_event_check(node,(int) offset,__FILE__,__LINE__)
#else
#define X3D_NODE_CHECK(node)
#define MARK_EVENT(node,offset)	mark_event(node,(int) offset)
#endif
#define COPY_SFVEC3F_TO_POINT_XYZ(too,from) { too.x = from[0]; too.y = from[1]; too.z = from[2];}
#define COPY_POINT_XYZ_TO_SFVEC3F(too,from) { too[0] = (float) from.x; too[1] = (float) from.y; too[2] = (float) from.z;}
#define offsetPointer_deref(t, me, offs) ((t)(((char*)(me))+offs))

/* now, generated structures for each VRML/X3D Node*/
/***********************/
struct X3D_AcousticProperties {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float absorption;
	float specular;
	float diffuse;
	float refraction;
};
extern struct X3D_Virt virt_AcousticProperties;
/***********************/
struct X3D_AnalyserNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int fftSize;
	int frequencyBinCount;
	float minDecibels;
	float maxDecibels;
	float smoothingTimeConstant;
};
extern struct X3D_Virt virt_AnalyserNode;
/***********************/
struct X3D_Anchor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct Uni_String *description;
	struct X3D_Node *metadata;
	struct Multi_String parameter;
	struct Multi_String url;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	void * _parentResource;
};
extern struct X3D_Virt virt_Anchor;
/***********************/
struct X3D_Appearance {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *fillProperties;
	struct X3D_Node *lineProperties;
	struct X3D_Node *pointProperties;
	struct X3D_Node *acousticProperties;
	struct X3D_Node *material;
	struct X3D_Node *backMaterial;
	struct X3D_Node *metadata;
	struct Multi_Node shaders;
	struct Multi_Node effects;
	struct X3D_Node *texture;
	struct X3D_Node *textureTransform;
};
extern struct X3D_Virt virt_Appearance;
/***********************/
struct X3D_Arc2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	float endAngle;
	float radius;
	float startAngle;
	struct Multi_Vec2f __points;
	int __numPoints;
	void * __linerep;
};
extern struct X3D_Virt virt_Arc2D;
/***********************/
struct X3D_ArcClose2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *closureType;
	float endAngle;
	float radius;
	int solid;
	float startAngle;
	struct Multi_Vec2f __points;
	struct Multi_Vec2f __texCoords;
	int __numPoints;
	int __simpleDisk;
	void * __wireindices;
	void * __linerep;
};
extern struct X3D_Virt virt_ArcClose2D;
/***********************/
struct X3D_AudioBuffer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	float sampleRate;
	int length;
	float duration;
	int numberOfChannels;
};
extern struct X3D_Virt virt_AudioBuffer;
/***********************/
struct X3D_AudioBufferSourceNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *buffer;
	float playbackRate;
	int loop;
	float loopStart;
	float loopEnd;
};
extern struct X3D_Virt virt_AudioBufferSourceNode;
/***********************/
struct X3D_AudioClip {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct Uni_String *description;
	int loop;
	struct X3D_Node *metadata;
	double pauseTime;
	float pitch;
	double resumeTime;
	double startTime;
	double stopTime;
	struct Multi_String url;
	double duration_changed;
	double elapsedTime;
	int isActive;
	int isPaused;
	void * _parentResource;
	int __loadstatus;
	void * __loadResource;
	int __sourceNumber;
	double __inittime;
	double __lasttime;
};
extern struct X3D_Virt virt_AudioClip;
/***********************/
struct X3D_AudioContext {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	float currentTime;
	struct X3D_Node *destination;
	struct X3D_Node *listener;
	float sampleRate;
	struct X3D_Node *mozAudioChannelType;
};
extern struct X3D_Virt virt_AudioContext;
/***********************/
struct X3D_AudioDestinationNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int maxChannelCount;
};
extern struct X3D_Virt virt_AudioDestinationNode;
/***********************/
struct X3D_AudioListener {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	float dopplerFactor;
	float speedOfSound;
};
extern struct X3D_Virt virt_AudioListener;
/***********************/
struct X3D_AudioParam {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int value;
	int defaultValue;
};
extern struct X3D_Virt virt_AudioParam;
/***********************/
struct X3D_BackdropBackground {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_bind;
	double bindTime;
	int isBound;
	float transparency;
	struct SFColor color;
	struct X3D_Node *metadata;
	int __texture;
	int __VBO;
	struct Multi_String url;
};
extern struct X3D_Virt virt_BackdropBackground;
/***********************/
struct X3D_Background {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_bind;
	struct Multi_Float groundAngle;
	struct Multi_Color groundColor;
	struct Multi_Float skyAngle;
	struct Multi_Color skyColor;
	double bindTime;
	int isBound;
	int _layerId;
	void * _parentResource;
	struct Multi_Vec3f __points;
	struct Multi_Color __colours;
	int __quadcount;
	float transparency;
	struct Multi_String frontUrl;
	struct Multi_String backUrl;
	struct Multi_String topUrl;
	struct Multi_String bottomUrl;
	struct Multi_String leftUrl;
	struct Multi_String rightUrl;
	struct X3D_Node *metadata;
	int __textureright;
	struct X3D_Node *__frontTexture;
	struct X3D_Node *__backTexture;
	struct X3D_Node *__topTexture;
	struct X3D_Node *__bottomTexture;
	struct X3D_Node *__leftTexture;
	struct X3D_Node *__rightTexture;
	int __VBO;
};
extern struct X3D_Virt virt_Background;
/***********************/
struct X3D_BallJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f anchorPoint;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	struct Multi_String forceOutput;
	struct X3D_Node *metadata;
	struct SFVec3f body1AnchorPoint;
	struct SFVec3f body2AnchorPoint;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_anchorPoint;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
};
extern struct X3D_Virt virt_BallJoint;
/***********************/
struct X3D_Billboard {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct SFVec3f axisOfRotation;
	struct Multi_Node children;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *metadata;
	double _rotationAngle;
};
extern struct X3D_Virt virt_Billboard;
/***********************/
struct X3D_BiquadFilterNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int frequency;
	float detune;
	float Q;
	float gain;
	struct Uni_String *type;
};
extern struct X3D_Virt virt_BiquadFilterNode;
/***********************/
struct X3D_BlendedVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	struct X3D_Node *renderStyle;
	struct X3D_Node *voxels;
	float weightConstant1;
	float weightConstant2;
	struct Uni_String *weightFunction1;
	struct Uni_String *weightFunction2;
	struct X3D_Node *weightTransferFunction1;
	struct X3D_Node *weightTransferFunction2;
	struct Multi_Int32 _fbohandles;
	int _weightFunction1;
	int _weightFunction2;
};
extern struct X3D_Virt virt_BlendedVolumeStyle;
/***********************/
struct X3D_BooleanFilter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_boolean;
	int inputFalse;
	int inputNegate;
	int inputTrue;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_BooleanFilter;
/***********************/
struct X3D_BooleanSequencer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int next;
	int previous;
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Bool keyValue;
	int value_changed;
	struct X3D_Node *metadata;
	int _index;
};
extern struct X3D_Virt virt_BooleanSequencer;
/***********************/
struct X3D_BooleanToggle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_boolean;
	int toggle;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_BooleanToggle;
/***********************/
struct X3D_BooleanTrigger {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	double set_triggerTime;
	int triggerTrue;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_BooleanTrigger;
/***********************/
struct X3D_BoundaryEnhancementVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	float boundaryOpacity;
	float opacityFactor;
	float retainedOpacity;
};
extern struct X3D_Virt virt_BoundaryEnhancementVolumeStyle;
/***********************/
struct X3D_BoundedPhysicsModel {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *geometry;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_BoundedPhysicsModel;
/***********************/
struct X3D_Box {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFVec3f size;
	int solid;
	struct Multi_Vec3f __points;
};
extern struct X3D_Virt virt_Box;
/***********************/
struct X3D_CADAssembly {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_CADAssembly;
/***********************/
struct X3D_CADFace {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct X3D_Node *shape;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_CADFace;
/***********************/
struct X3D_CADLayer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Multi_Bool visibles;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_CADLayer;
/***********************/
struct X3D_CADPart {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct SFVec3f center;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_CADPart;
/***********************/
struct X3D_CalibratedCameraSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	struct X3D_Node *metadata;
	struct Uni_String *description;
	struct Multi_Int32 image;
	struct SFVec2f focalPoint;
	float fieldOfView;
	struct Uni_String *fovMode;
	float aspectRatio;
};
extern struct X3D_Virt virt_CalibratedCameraSensor;
/***********************/
struct X3D_CartoonVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	struct SFColorRGBA orthogonalColor;
	struct SFColorRGBA parallelColor;
	int colorSteps;
};
extern struct X3D_Virt virt_CartoonVolumeStyle;
/***********************/
struct X3D_ChannelMergerNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *context;
	int numberOfInputs;
	int numberOfOutputs;
	int channelCount;
	struct Uni_String *channelCountMode;
	struct Uni_String *channelInterpretation;
};
extern struct X3D_Virt virt_ChannelMergerNode;
/***********************/
struct X3D_ChannelSplitterNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *context;
	int numberOfInputs;
	int numberOfOutputs;
	int channelCount;
	struct Uni_String *channelCountMode;
	struct Uni_String *channelInterpretation;
};
extern struct X3D_Virt virt_ChannelSplitterNode;
/***********************/
struct X3D_Circle2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	float radius;
	struct Multi_Vec2f __points;
	int __numPoints;
	void * __linerep;
};
extern struct X3D_Virt virt_Circle2D;
/***********************/
struct X3D_ClipPlane {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct SFVec4f plane;
};
extern struct X3D_Virt virt_ClipPlane;
/***********************/
struct X3D_CollidableOffset {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __do_trans;
	int __do_rotation;
	struct X3D_Node *collidable;
	void * _geom;
	struct SFRotation _initialRotation;
	struct SFVec3f _initialTranslation;
	int _initialized;
	void * _csensor;
};
extern struct X3D_Virt virt_CollidableOffset;
/***********************/
struct X3D_CollidableShape {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __do_trans;
	int __do_rotation;
	struct X3D_Node *shape;
	void * _geom;
	struct SFRotation _initialRotation;
	struct SFVec3f _initialTranslation;
	int _initialized;
	void * _csensor;
};
extern struct X3D_Virt virt_CollidableShape;
/***********************/
struct X3D_Collision {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	int enabled;
	int collide;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *proxy;
	double collideTime;
	struct X3D_Node *metadata;
	int __hit;
};
extern struct X3D_Virt virt_Collision;
/***********************/
struct X3D_CollisionCollection {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String appliedParameters;
	float bounce;
	struct Multi_Node collidables;
	int enabled;
	struct SFVec2f frictionCoefficients;
	struct X3D_Node *metadata;
	float minBounceSpeed;
	struct SFVec2f slipFactors;
	float softnessConstantForceMix;
	float softnessErrorCorrection;
	struct SFVec2f surfaceSpeed;
	void * _class;
	void * _csensor;
	int _appliedParametersMask;
};
extern struct X3D_Virt virt_CollisionCollection;
/***********************/
struct X3D_CollisionSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *collider;
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_Node intersections;
	struct Multi_Node contacts;
	int isActive;
};
extern struct X3D_Virt virt_CollisionSensor;
/***********************/
struct X3D_CollisionSpace {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node collidables;
	int enabled;
	struct X3D_Node *metadata;
	int useGeometry;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	void * _space;
};
extern struct X3D_Virt virt_CollisionSpace;
/***********************/
struct X3D_Color {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Color color;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_Color;
/***********************/
struct X3D_ColorChaser {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct SFColor value_changed;
	struct SFColor initialDestination;
	struct SFColor initialValue;
	struct SFColor set_destination;
	struct SFColor set_value;
	void * _buffer;
	struct SFColor _previousvalue;
	struct SFColor _destination;
};
extern struct X3D_Virt virt_ColorChaser;
/***********************/
struct X3D_ColorDamper {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct SFColor value_changed;
	struct SFColor initialDestination;
	struct SFColor initialValue;
	struct SFColor set_destination;
	struct SFColor set_value;
	void * _values;
	struct SFColor _input;
};
extern struct X3D_Virt virt_ColorDamper;
/***********************/
struct X3D_ColorInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Color keyValue;
	struct X3D_Node *metadata;
	struct SFColor value_changed;
};
extern struct X3D_Virt virt_ColorInterpolator;
/***********************/
struct X3D_ColorRGBA {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_ColorRGBA color;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_ColorRGBA;
/***********************/
struct X3D_ComposedCubeMapTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *back;
	struct X3D_Node *bottom;
	struct X3D_Node *front;
	struct X3D_Node *left;
	struct X3D_Node *top;
	struct X3D_Node *right;
	void * _parentResource;
};
extern struct X3D_Virt virt_ComposedCubeMapTexture;
/***********************/
struct X3D_ComposedShader {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int activate;
	struct Multi_Node parts;
	int isSelected;
	int isValid;
	struct Uni_String *language;
	struct X3D_Node *metadata;
	int _initialized;
	struct X3D_Node *_shaderUserDefinedFields;
	int _shaderUserNumber;
	pthread_t _shaderLoadThread;
	int _retrievedURLData;
};
extern struct X3D_Virt virt_ComposedShader;
/***********************/
struct X3D_ComposedTexture3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Node texture;
	struct X3D_Node *textureProperties;
	int repeatS;
	int repeatT;
	int repeatR;
	int __textureTableIndex;
	void * _parentResource;
};
extern struct X3D_Virt virt_ComposedTexture3D;
/***********************/
struct X3D_ComposedVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_Node renderStyle;
};
extern struct X3D_Virt virt_ComposedVolumeStyle;
/***********************/
struct X3D_CompositeVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_Node renderStyle;
};
extern struct X3D_Virt virt_CompositeVolumeStyle;
/***********************/
struct X3D_Cone {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int bottom;
	float bottomRadius;
	float height;
	int side;
	int solid;
	struct Multi_Vec3f __sidepoints;
	struct Multi_Vec3f __botpoints;
	struct Multi_Vec3f __normals;
	int __coneVBO;
	int __coneTriangles;
	void * __wireindices;
};
extern struct X3D_Virt virt_Cone;
/***********************/
struct X3D_ConeEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float angle;
	struct SFVec3f direction;
	struct X3D_Node *metadata;
	struct SFVec3f position;
	float speed;
	float variation;
	float mass;
	float surfaceArea;
};
extern struct X3D_Virt virt_ConeEmitter;
/***********************/
struct X3D_Contact {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String appliedParameters;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	float bounce;
	struct SFVec3f contactNormal;
	float depth;
	struct SFVec2f frictionCoefficients;
	struct SFVec3f frictionDirection;
	struct X3D_Node *geometry1;
	struct X3D_Node *geometry2;
	struct X3D_Node *metadata;
	float minBounceSpeed;
	struct SFVec3f position;
	struct SFVec2f slipCoefficients;
	float softnessConstantForceMix;
	float softnessErrorCorrection;
	struct SFVec2f surfaceSpeed;
	int _appliedParameters;
};
extern struct X3D_Virt virt_Contact;
/***********************/
struct X3D_Contour2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_Contour2D;
/***********************/
struct X3D_ContourPolyline2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2d controlPoint;
	struct Multi_Vec2f point;
};
extern struct X3D_Virt virt_ContourPolyline2D;
/***********************/
struct X3D_ConvolverNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *buffer;
	int normalize;
};
extern struct X3D_Virt virt_ConvolverNode;
/***********************/
struct X3D_Coordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec3f point;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_Coordinate;
/***********************/
struct X3D_CoordinateChaser {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct Multi_Vec3f value_changed;
	struct Multi_Vec3f initialDestination;
	struct Multi_Vec3f initialValue;
	struct Multi_Vec3f set_destination;
	struct Multi_Vec3f set_value;
	void * _buffer;
	struct Multi_Vec3f _previousvalue;
	struct Multi_Vec3f _destination;
};
extern struct X3D_Virt virt_CoordinateChaser;
/***********************/
struct X3D_CoordinateDamper {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct Multi_Vec3f value_changed;
	struct Multi_Vec3f initialDestination;
	struct Multi_Vec3f initialValue;
	struct Multi_Vec3f set_destination;
	struct Multi_Vec3f set_value;
	void * _values;
	struct Multi_Vec3f _input;
};
extern struct X3D_Virt virt_CoordinateDamper;
/***********************/
struct X3D_CoordinateDouble {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec3d point;
};
extern struct X3D_Virt virt_CoordinateDouble;
/***********************/
struct X3D_CoordinateInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec3f keyValue;
	struct X3D_Node *metadata;
	struct Multi_Vec3f value_changed;
	int _GPU_Routes_out;
	int _CPU_Routes_out;
	int _keyVBO;
	int _keyValueVBO;
};
extern struct X3D_Virt virt_CoordinateInterpolator;
/***********************/
struct X3D_CoordinateInterpolator2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec2f keyValue;
	struct X3D_Node *metadata;
	struct Multi_Vec2f value_changed;
};
extern struct X3D_Virt virt_CoordinateInterpolator2D;
/***********************/
struct X3D_Cylinder {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int bottom;
	float height;
	float radius;
	int side;
	int solid;
	int top;
	struct Multi_Vec3f __points;
	struct Multi_Vec3f __normals;
	int __cylinderVBO;
	int __cylinderTriangles;
	void * __wireindices;
};
extern struct X3D_Virt virt_Cylinder;
/***********************/
struct X3D_CylinderSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int autoOffset;
	struct SFRotation axisRotation;
	float diskAngle;
	int enabled;
	float maxAngle;
	float minAngle;
	float offset;
	int isActive;
	int isOver;
	struct Uni_String *description;
	struct SFRotation rotation_changed;
	struct SFVec3f trackPoint_changed;
	struct X3D_Node *metadata;
	int sensorLocalOutput;
	struct SFVec3f _oldtrackPoint;
	struct SFRotation _oldrotation;
	struct SFVec3f _origPoint;
	float _radius;
	int _usingDisk;
	int __oldEnabled;
};
extern struct X3D_Virt virt_CylinderSensor;
/***********************/
struct X3D_DISEntityManager {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	double timestamp;
	struct Uni_String *address;
	int port;
	struct Uni_String *multicastRelayHost;
	int multicastRelayPort;
	struct Uni_String *networkMode;
	int isNetworkReader;
	int isNetworkWriter;
	int isStandAlone;
	double readInterval;
	double writeInterval;
	int rtpHeaderExpected;
	int isRtpHeaderHeard;
	int _registered;
	struct X3D_Node *_dsock;
	double _lasttime;
	int _pduchange_networksensor;
	struct X3D_Node *_oldState;
	int entityID;
	int applicationID;
	int siteID;
	struct Multi_Node mapping;
	struct X3D_Node *metadata;
	struct Multi_Node addedEntities;
	struct Multi_Node removedEntities;
	struct Multi_Node addEntities;
	struct Multi_Node removeEntities;
	struct Multi_Node entities;
	int _pduchange_create;
	int _pduchange_remove;
	int _pduchange_em_info;
};
extern struct X3D_Virt virt_DISEntityManager;
/***********************/
struct X3D_DISEntityTypeMapping {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	int kind;
	int domain;
	int country;
	int category;
	int subcategory;
	int specific;
	int extra;
	struct X3D_Node *_child;
};
extern struct X3D_Virt virt_DISEntityTypeMapping;
/***********************/
struct X3D_DelayNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int delayTime;
};
extern struct X3D_Virt virt_DelayNode;
/***********************/
struct X3D_DirectionalLight {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int global;
	int on;
	float ambientIntensity;
	float intensity;
	struct SFColor color;
	struct SFVec3f direction;
	struct SFVec4f _dir;
	struct SFVec4f _col;
	struct SFVec4f _amb;
};
extern struct X3D_Virt virt_DirectionalLight;
/***********************/
struct X3D_Disk2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	float innerRadius;
	float outerRadius;
	int solid;
	struct Multi_Vec2f __points;
	struct Multi_Vec2f __texCoords;
	int __numPoints;
	int __simpleDisk;
	void * __wireindices;
};
extern struct X3D_Virt virt_Disk2D;
/***********************/
struct X3D_DoubleAxisHingeJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f anchorPoint;
	struct SFVec3f axis1;
	struct SFVec3f axis2;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	float desiredAngularVelocity1;
	float desiredAngularVelocity2;
	struct Multi_String forceOutput;
	float maxAngle1;
	float maxTorque1;
	float maxTorque2;
	struct X3D_Node *metadata;
	float minAngle1;
	float stopBounce1;
	float stopConstantForceMix1;
	float stopErrorCorrection1;
	float suspensionErrorCorrection;
	float suspensionForce;
	struct SFVec3f body1AnchorPoint;
	struct SFVec3f body1Axis;
	struct SFVec3f body2AnchorPoint;
	struct SFVec3f body2Axis;
	float hinge1Angle;
	float hinge1AngleRate;
	float hinge2Angle;
	float hinge2AngleRate;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_anchorPoint;
	struct SFVec3f __old_axis1;
	struct SFVec3f __old_axis2;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
	void * _motor1;
	void * _motor2;
	float axis1Angle;
};
extern struct X3D_Virt virt_DoubleAxisHingeJoint;
/***********************/
struct X3D_DynamicsCompressorNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	float threshold;
	int knee;
	int ratio;
	float reduction;
	float attack;
	int release;
};
extern struct X3D_Virt virt_DynamicsCompressorNode;
/***********************/
struct X3D_EaseInEaseOut {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Vec2f easeInEaseOut;
	struct Multi_Float key;
	struct X3D_Node *metadata;
	float modifiedFraction_changed;
};
extern struct X3D_Virt virt_EaseInEaseOut;
/***********************/
struct X3D_EdgeEnhancementVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	struct SFColorRGBA edgeColor;
	float gradientThreshold;
};
extern struct X3D_Virt virt_EdgeEnhancementVolumeStyle;
/***********************/
struct X3D_Effect {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int activate;
	struct Multi_Node parts;
	int isSelected;
	int isValid;
	struct Uni_String *language;
	struct X3D_Node *metadata;
	int _initialized;
	struct X3D_Node *_shaderUserDefinedFields;
	int _shaderUserNumber;
	pthread_t _shaderLoadThread;
	int _retrievedURLData;
};
extern struct X3D_Virt virt_Effect;
/***********************/
struct X3D_EffectPart {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	struct Uni_String *type;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
	struct X3D_Node *_shaderUserDefinedFields;
};
extern struct X3D_Virt virt_EffectPart;
/***********************/
struct X3D_ElevationGrid {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Float set_height;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	float creaseAngle;
	struct Multi_Float height;
	int normalPerVertex;
	int solid;
	int xDimension;
	float xSpacing;
	int zDimension;
	float zSpacing;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_ElevationGrid;
/***********************/
struct X3D_EnvironmentLight {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int global;
	int on;
	float ambientIntensity;
	float intensity;
	struct SFColor color;
	struct X3D_Node *diffuseTexture;
	struct X3D_Node *specularTexture;
};
extern struct X3D_Virt virt_EnvironmentLight;
/***********************/
struct X3D_EspduTransform {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	double timestamp;
	struct Uni_String *address;
	int port;
	struct Uni_String *multicastRelayHost;
	int multicastRelayPort;
	struct Uni_String *networkMode;
	int isNetworkReader;
	int isNetworkWriter;
	int isStandAlone;
	double readInterval;
	double writeInterval;
	int rtpHeaderExpected;
	int isRtpHeaderHeard;
	int _registered;
	struct X3D_Node *_dsock;
	double _lasttime;
	int _pduchange_networksensor;
	struct X3D_Node *_oldState;
	int entityID;
	int applicationID;
	int siteID;
	struct Multi_String geoSystem;
	struct SFVec3d geoCoords;
	struct X3D_Node *__geoSystem;
	int entityKind;
	int entityDomain;
	int entityCountry;
	int entityCategory;
	int entitySubCategory;
	int entitySpecific;
	int entityExtra;
	int forceID;
	struct Uni_String *marking;
	int deadReckoning;
	struct SFVec3f linearVelocity;
	struct SFVec3f linearAcceleration;
	struct SFVec3f _p0;
	struct SFVec3f _v0;
	struct SFVec3f _a0;
	struct SFRotation _angularVelocity;
	struct SFRotation _r0;
	int _change_count;
	int _sent;
	struct SFVec3f _lastp0;
	struct SFRotation _lastr0;
	double _lastp0time;
	double _lastframetime;
	struct SFVec3f _smoothingDelta;
	int _smoothingCount;
	float set_articulationParameterValue0;
	float set_articulationParameterValue1;
	float set_articulationParameterValue2;
	float set_articulationParameterValue3;
	float set_articulationParameterValue4;
	float set_articulationParameterValue5;
	float set_articulationParameterValue6;
	float set_articulationParameterValue7;
	int articulationParameterCount;
	struct Multi_Int32 articulationParameterDesignatorArray;
	struct Multi_Int32 articulationParameterChangeIndicatorArr;
	struct Multi_Int32 articulationParameterIdPartAttachedToAr;
	struct Multi_Int32 articulationParameterTypeArray;
	struct Multi_Float articulationParameterArray;
	float articulationParameterValue0_changed;
	float articulationParameterValue1_changed;
	float articulationParameterValue2_changed;
	float articulationParameterValue3_changed;
	float articulationParameterValue4_changed;
	float articulationParameterValue5_changed;
	float articulationParameterValue6_changed;
	float articulationParameterValue7_changed;
	int _pduchange_es;
	int collisionType;
	double collideTime;
	int isCollided;
	int _pduchange_collision;
	int eventEntityID;
	int eventApplicationID;
	int eventSiteID;
	int eventNumber;
	int fired1;
	int fired2;
	int fireMissionIndex;
	float firingRange;
	double firedTime;
	int _pduchange_fire;
	struct SFVec3f detonationLocation;
	struct SFVec3f detonationRelativeLocation;
	int detonationResult;
	double detonateTime;
	int isDetonated;
	int _pduchange_detonation;
	int munitionEntityID;
	int munitionApplicationID;
	int munitionSiteID;
	struct SFVec3f munitionStartPoint;
	struct SFVec3f munitionEndPoint;
	int munitionQuantity;
	int firingRate;
	int fuse;
	int warhead;
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct SFVec3f center;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_EspduTransform;
/***********************/
struct X3D_ExplosionEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFVec3f position;
	float speed;
	float variation;
	float mass;
	float surfaceArea;
};
extern struct X3D_Virt virt_ExplosionEmitter;
/***********************/
struct X3D_Extrusion {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec2f set_crossSection;
	struct Multi_Rotation set_orientation;
	struct Multi_Vec2f set_scale;
	struct Multi_Vec3f set_spine;
	struct X3D_Node *metadata;
	int beginCap;
	int ccw;
	int convex;
	float creaseAngle;
	struct Multi_Vec2f crossSection;
	int endCap;
	struct Multi_Rotation orientation;
	struct Multi_Vec2f scale;
	int solid;
	struct Multi_Vec3f spine;
};
extern struct X3D_Virt virt_Extrusion;
/***********************/
struct X3D_FillProperties {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int filled;
	struct SFColor hatchColor;
	int hatched;
	int hatchStyle;
	struct X3D_Node *metadata;
	int _enabled;
	struct SFVec2f _hatchScale;
};
extern struct X3D_Virt virt_FillProperties;
/***********************/
struct X3D_FloatVertexAttribute {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Float value;
	struct Uni_String *name;
	int numComponents;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_FloatVertexAttribute;
/***********************/
struct X3D_Fog {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFColor color;
	struct Uni_String *fogType;
	float visibilityRange;
	float __fogScale;
	int __fogType;
	int set_bind;
	double bindTime;
	int isBound;
	int _layerId;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_Fog;
/***********************/
struct X3D_FogCoordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Float depth;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_FogCoordinate;
/***********************/
struct X3D_FontStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String family;
	int horizontal;
	struct Multi_String justify;
	struct Uni_String *language;
	int leftToRight;
	float size;
	float spacing;
	struct Uni_String *style;
	int topToBottom;
};
extern struct X3D_Virt virt_FontStyle;
/***********************/
struct X3D_ForcePhysicsModel {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct SFVec3f force;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_ForcePhysicsModel;
/***********************/
struct X3D_GainNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int gain;
};
extern struct X3D_Virt virt_GainNode;
/***********************/
struct X3D_GeneratedCubeMapTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	void * _parentResource;
	struct Multi_Node __subTextures;
	int __regenSubTextures;
	struct Uni_String *update;
	int size;
};
extern struct X3D_Virt virt_GeneratedCubeMapTexture;
/***********************/
struct X3D_GeoConvert {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3d set_geoCoords;
	struct SFVec3d set_gcCoords;
	struct Multi_String geoSystem;
	struct X3D_Node *metadata;
	struct SFVec3d gcCoords_changed;
	struct SFVec3d geoCoords_changed;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __oldgeoCoords;
	struct SFVec3d __oldgcCoords;
};
extern struct X3D_Virt virt_GeoConvert;
/***********************/
struct X3D_GeoCoordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec3d point;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	struct X3D_Node *__geoSystem;
	struct Multi_Vec3f __movedCoords;
};
extern struct X3D_Virt virt_GeoCoordinate;
/***********************/
struct X3D_GeoElevationGrid {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Double set_height;
	struct X3D_Node *color;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	float yScale;
	int ccw;
	int colorPerVertex;
	double creaseAngle;
	struct SFVec3d geoGridOrigin;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	struct Multi_Double height;
	int normalPerVertex;
	int solid;
	int xDimension;
	double xSpacing;
	int zDimension;
	double zSpacing;
	struct Multi_Int32 _coordIndex;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __autoOffset;
	struct SFVec4d __localOrient;
	struct Multi_Int32 __planets;
};
extern struct X3D_Virt virt_GeoElevationGrid;
/***********************/
struct X3D_GeoLOD {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Node children;
	int level_changed;
	struct SFVec3d center;
	struct Multi_String child1Url;
	struct Multi_String child2Url;
	struct Multi_String child3Url;
	struct Multi_String child4Url;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	float range;
	struct Multi_String rootUrl;
	struct Multi_Node rootNode;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __movedCoords;
	int __inRange;
	struct X3D_Node *__child1Node;
	struct X3D_Node *__child2Node;
	struct X3D_Node *__child3Node;
	struct X3D_Node *__child4Node;
	struct X3D_Node *__rootUrl;
	int __childloadstatus;
	int __rooturlloadstatus;
	int __level;
};
extern struct X3D_Virt virt_GeoLOD;
/***********************/
struct X3D_GeoLocation {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct SFVec3d geoCoords;
	struct X3D_Node *metadata;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int relativeHeight;
	double _gridHeight;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __position;
	struct SFVec3d __movedCoords;
	struct SFVec3d __movedgd;
	struct SFVec4d __localOrient;
	struct SFVec4d __offsetOrient;
	struct SFVec3d __oldgeoCoords;
	struct Multi_Node __oldChildren;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_GeoLocation;
/***********************/
struct X3D_GeoMetadata {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node data;
	struct Multi_String summary;
	struct Multi_String url;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_GeoMetadata;
/***********************/
struct X3D_GeoOrigin {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3d geoCoords;
	struct Multi_String geoSystem;
	struct X3D_Node *metadata;
	int rotateYUp;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __movedCoords;
	struct SFVec3d __movedgd;
	struct SFVec3d __oldgeoCoords;
	struct Multi_String __oldMFString;
	struct SFVec4d __rotyup;
};
extern struct X3D_Virt virt_GeoOrigin;
/***********************/
struct X3D_GeoPlanet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Uni_String *description;
	int planetId;
	struct Multi_Node __oldChildren;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_GeoPlanet;
/***********************/
struct X3D_GeoPositionInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec3d keyValue;
	struct X3D_Node *metadata;
	struct SFVec3d geovalue_changed;
	struct SFVec3f value_changed;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	struct X3D_Node *__geoSystem;
	struct Multi_Vec3f __movedValue;
	struct Multi_Float __oldKeyPtr;
	struct Multi_Vec3d __oldKeyValuePtr;
};
extern struct X3D_Virt virt_GeoPositionInterpolator;
/***********************/
struct X3D_GeoProximitySensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct SFVec3d geoCenter;
	struct SFVec3d center;
	struct X3D_Node *metadata;
	struct SFVec3f size;
	struct SFVec3f centerOfRotation_changed;
	double enterTime;
	double exitTime;
	struct SFVec3d geoCoord_changed;
	int isActive;
	struct SFRotation orientation_changed;
	struct SFVec3f position_changed;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	int __hit;
	struct SFVec3f __t1;
	struct SFRotation __t2;
	struct SFVec3d __t3;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __movedCoords;
	struct SFVec4d __localOrient;
	int __oldEnabled;
	struct SFVec3d __oldGeoCenter;
	struct SFVec3f __oldSize;
};
extern struct X3D_Virt virt_GeoProximitySensor;
/***********************/
struct X3D_GeoTouchSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *description;
	int enabled;
	struct X3D_Node *metadata;
	struct SFVec3f hitNormal_changed;
	struct SFVec3f hitPoint_changed;
	struct SFVec2f hitTexCoord_changed;
	struct SFVec3d hitGeoCoord_changed;
	int isActive;
	int isOver;
	double touchTime;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	struct X3D_Node *__geoSystem;
	struct SFVec3f _oldhitNormal;
	struct SFVec3f _oldhitPoint;
	struct SFVec2f _oldhitTexCoord;
	int __oldEnabled;
};
extern struct X3D_Virt virt_GeoTouchSensor;
/***********************/
struct X3D_GeoTransform {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct SFVec3f center;
	struct Multi_Node children;
	struct SFVec3d geoCenter;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __movedCoords;
	struct SFVec4d __localOrient;
	struct SFVec3d __oldGeoCenter;
	struct Multi_Node __oldChildren;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_GeoTransform;
/***********************/
struct X3D_GeoViewpoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int _layerId;
	int _donethispass;
	int set_bind;
	double bindTime;
	int isBound;
	struct Uni_String *description;
	int jump;
	float fieldOfView;
	struct X3D_Node *metadata;
	struct SFRotation orientation;
	struct SFVec3d position;
	struct SFVec3d centerOfRotation;
	int headlight;
	struct Multi_String navType;
	struct X3D_Node *geoOrigin;
	struct Multi_String geoSystem;
	float speedFactor;
	int retainUserOffsets;
	int _initializedOnce;
	struct SFRotation _orientation;
	struct SFVec3d _position;
	int relativeHeight;
	int _resetRelativeHeight;
	int _prepped_planet;
	struct X3D_Node *__geoSystem;
	struct SFVec3d __movedPosition;
	struct SFRotation __movedOrientation;
	struct SFRotation __movedOrientationB;
	struct SFVec3d __movedgd;
	struct Uni_String *__oldSFString;
	float __oldFieldOfView;
	int __oldHeadlight;
	int __oldJump;
	struct Multi_String __oldMFString;
};
extern struct X3D_Virt virt_GeoViewpoint;
/***********************/
struct X3D_Group {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_Group;
/***********************/
struct X3D_HAnimDisplacer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 coordIndex;
	struct Multi_Vec3f displacements;
	struct Uni_String *name;
	float weight;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_HAnimDisplacer;
/***********************/
struct X3D_HAnimHumanoid {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	struct Multi_String info;
	struct Multi_Node joints;
	struct Uni_String *name;
	struct Uni_String *skeletalConfiguration;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct Multi_Node segments;
	struct Multi_Node sites;
	struct Multi_Node skeleton;
	struct Multi_Node skin;
	struct X3D_Node *skinCoord;
	struct X3D_Node *skinNormal;
	struct Multi_Node __sibAffectors;
	struct SFVec3f translation;
	struct Uni_String *version;
	struct Multi_Node viewpoints;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *metadata;
	struct Multi_Node motions;
	struct Multi_Bool motionsEnabled;
	int loa;
	void * _JT;
	void * _PVI;
	void * _PVW;
	int _NV;
	void * _origCoords;
	void * _origNorms;
};
extern struct X3D_Virt virt_HAnimHumanoid;
/***********************/
struct X3D_HAnimJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct SFVec3f center;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct Multi_Node displacers;
	struct SFRotation limitOrientation;
	struct Multi_Float llimit;
	struct Uni_String *name;
	struct Multi_Int32 skinCoordIndex;
	struct Multi_Float skinCoordWeight;
	struct Multi_Float stiffness;
	struct Multi_Float ulimit;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *metadata;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
};
extern struct X3D_Virt virt_HAnimJoint;
/***********************/
struct X3D_HAnimMotion {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *channels;
	struct Multi_Bool channelsEnabled;
	double cycleTime;
	struct Uni_String *description;
	double elapsedTime;
	double _startTime;
	int enabled;
	int _lastenabled;
	int _isActive;
	int frameCount;
	double frameDuration;
	int frameIncrement;
	int frameIndex;
	int startFrame;
	int endFrame;
	struct Uni_String *joints;
	int loa;
	int loop;
	struct X3D_Node *metadata;
	int next;
	int previous;
	struct Uni_String *values;
	void * _fvalues;
	void * _framevalues;
	int _channelcount;
	int _njoints;
	void * _channels;
};
extern struct X3D_Virt virt_HAnimMotion;
/***********************/
struct X3D_HAnimMotionData {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *channels;
	struct Uni_String *description;
	int frameCount;
	double frameDuration;
	struct Uni_String *joints;
	int loa;
	struct X3D_Node *metadata;
	struct Uni_String *values;
	void * _fvalues;
	int _channelcount;
	int _njoints;
	void * _channels;
};
extern struct X3D_Virt virt_HAnimMotionData;
/***********************/
struct X3D_HAnimMotionDataFile {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *channels;
	struct Uni_String *description;
	int frameCount;
	double frameDuration;
	struct Uni_String *joints;
	int loa;
	struct X3D_Node *metadata;
	struct Uni_String *values;
	void * _fvalues;
	int _channelcount;
	int _njoints;
	void * _channels;
	struct Multi_String url;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
};
extern struct X3D_Virt virt_HAnimMotionDataFile;
/***********************/
struct X3D_HAnimMotionPlay {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *data;
	struct Multi_Bool channelsEnabled;
	double cycleTime;
	struct Uni_String *description;
	double elapsedTime;
	double _startTime;
	int enabled;
	int _lastenabled;
	int _isActive;
	int frameIncrement;
	int frameIndex;
	int startFrame;
	int endFrame;
	int loop;
	struct X3D_Node *metadata;
	int next;
	int previous;
	void * _framevalues;
};
extern struct X3D_Virt virt_HAnimMotionPlay;
/***********************/
struct X3D_HAnimSegment {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct Uni_String *name;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct SFVec3f centerOfMass;
	struct X3D_Node *coord;
	struct Multi_Node displacers;
	float mass;
	struct Multi_Float momentsOfInertia;
	struct X3D_Node *metadata;
	void * _origCoords;
};
extern struct X3D_Virt virt_HAnimSegment;
/***********************/
struct X3D_HAnimSite {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct Uni_String *name;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct SFVec3f center;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct X3D_Node *metadata;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
};
extern struct X3D_Virt virt_HAnimSite;
/***********************/
struct X3D_ImageBackdropBackground {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_bind;
	double bindTime;
	int isBound;
	float transparency;
	struct SFColor color;
	struct X3D_Node *metadata;
	int __texture;
	int __VBO;
	struct Multi_Int32 image;
};
extern struct X3D_Virt virt_ImageBackdropBackground;
/***********************/
struct X3D_ImageCubeMapTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	void * _parentResource;
	struct Multi_Node __subTextures;
	int __regenSubTextures;
	struct Multi_String url;
};
extern struct X3D_Virt virt_ImageCubeMapTexture;
/***********************/
struct X3D_ImageTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	int repeatS;
	int repeatT;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	void * _parentResource;
};
extern struct X3D_Virt virt_ImageTexture;
/***********************/
struct X3D_ImageTexture3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	int repeatS;
	int repeatT;
	int repeatR;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	void * _parentResource;
	int _needs_gradient;
};
extern struct X3D_Virt virt_ImageTexture3D;
/***********************/
struct X3D_IndexedFaceSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_colorIndex;
	struct Multi_Int32 set_coordIndex;
	struct Multi_Int32 set_normalIndex;
	struct Multi_Int32 set_texCoordIndex;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	struct Multi_Int32 colorIndex;
	int colorPerVertex;
	int convex;
	struct Multi_Int32 coordIndex;
	float creaseAngle;
	struct Multi_Int32 normalIndex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 texCoordIndex;
};
extern struct X3D_Virt virt_IndexedFaceSet;
/***********************/
struct X3D_IndexedLineSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_colorIndex;
	struct Multi_Int32 set_coordIndex;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct Multi_Int32 colorIndex;
	int colorPerVertex;
	struct Multi_Int32 coordIndex;
	void * __vertArr;
	void * __vertIndx;
	void * __starts;
	void * __counts;
	int __segCount;
	void * __xcolours;
	void * __xfog;
	void * __vertices;
	void * __vertexCount;
	void * __linerep;
};
extern struct X3D_Virt virt_IndexedLineSet;
/***********************/
struct X3D_IndexedQuadSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_index;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	struct Multi_Int32 index;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_IndexedQuadSet;
/***********************/
struct X3D_IndexedTriangleFanSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_index;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 index;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_IndexedTriangleFanSet;
/***********************/
struct X3D_IndexedTriangleSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_index;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 index;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_IndexedTriangleSet;
/***********************/
struct X3D_IndexedTriangleStripSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_index;
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 index;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_IndexedTriangleStripSet;
/***********************/
struct X3D_Inline {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node __children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Multi_Node _sortedChildren;
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	void * __protoDeclares;
	void * __externProtoDeclares;
	void * __nodes;
	void * __subcontexts;
	void * __GC;
	void * __protoDef;
	int __protoFlags;
	struct X3D_Node *__prototype;
	struct X3D_Node *__parentProto;
	void * __ROUTES;
	void * __EXPORTS;
	void * __IMPORTS;
	void * __DEFnames;
	void * __IS;
	void * __scripts;
	struct Multi_String url;
	struct Multi_String __oldurl;
	void * __afterPound;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
	void * __typename;
	int load;
	int __oldload;
	double __unitlengthfactor;
	int __specversion;
};
extern struct X3D_Virt virt_Inline;
/***********************/
struct X3D_IntegerSequencer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int next;
	int previous;
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Int32 keyValue;
	int value_changed;
	struct X3D_Node *metadata;
	int _index;
};
extern struct X3D_Virt virt_IntegerSequencer;
/***********************/
struct X3D_IntegerTrigger {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_boolean;
	int integerKey;
	int triggerValue;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_IntegerTrigger;
/***********************/
struct X3D_IsoSurfaceVolumeData {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f dimensions;
	struct X3D_Node *metadata;
	struct X3D_Node *voxels;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	void * _boxtris;
	struct Multi_Node renderStyle;
	float contourStepSize;
	struct X3D_Node *gradients;
	float surfaceTolerance;
	struct Multi_Float surfaceValues;
};
extern struct X3D_Virt virt_IsoSurfaceVolumeData;
/***********************/
struct X3D_KeySensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int actionKeyPress;
	int actionKeyRelease;
	int altKey;
	int controlKey;
	int isActive;
	struct Uni_String *keyPress;
	struct Uni_String *keyRelease;
	int shiftKey;
	struct X3D_Node *metadata;
	int __oldEnabled;
};
extern struct X3D_Virt virt_KeySensor;
/***********************/
struct X3D_LOD {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node level;
	struct Multi_Node children;
	struct SFVec3f center;
	struct Multi_Float range;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *metadata;
	int level_changed;
	int forceTransitions;
	int __isX3D;
	void * _selected;
};
extern struct X3D_Virt virt_LOD;
/***********************/
struct X3D_Layer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	int isPickable;
	struct X3D_Node *viewport;
};
extern struct X3D_Virt virt_Layer;
/***********************/
struct X3D_LayerSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int activeLayer;
	struct X3D_Node *metadata;
	struct Multi_Node layers;
	struct Multi_Int32 order;
};
extern struct X3D_Virt virt_LayerSet;
/***********************/
struct X3D_Layout {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String align;
	struct X3D_Node *metadata;
	struct Multi_Float offset;
	struct Multi_String offsetUnits;
	struct Multi_String scaleMode;
	struct Multi_Float size;
	struct Multi_String sizeUnits;
	struct Multi_Int32 _align;
	struct Multi_Int32 _offsetUnits;
	struct Multi_Int32 _scaleMode;
	struct Multi_Int32 _sizeUnits;
	struct Multi_Float _scale;
};
extern struct X3D_Virt virt_Layout;
/***********************/
struct X3D_LayoutGroup {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct X3D_Node *layout;
	struct X3D_Node *viewport;
};
extern struct X3D_Virt virt_LayoutGroup;
/***********************/
struct X3D_LayoutLayer {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	int isPickable;
	struct X3D_Node *viewport;
	struct X3D_Node *layout;
};
extern struct X3D_Virt virt_LayoutLayer;
/***********************/
struct X3D_LinePickSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_String objectType;
	struct X3D_Node *pickingGeometry;
	struct Multi_Node pickTarget;
	int isActive;
	struct Multi_Node pickedGeometry;
	struct Uni_String *intersectionType;
	struct Uni_String *sortOrder;
	struct Uni_String *matchCriterion;
	int __oldEnabled;
	struct Multi_Vec3f pickedPoint;
	struct Multi_Vec3f pickedNormal;
	struct Multi_Vec3f pickedTextureCoordinate;
};
extern struct X3D_Virt virt_LinePickSensor;
/***********************/
struct X3D_LineProperties {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int applied;
	int linetype;
	float linewidthScaleFactor;
	struct Multi_Float type16dashes;
	struct Multi_Vec2f type16wiggles;
	struct Uni_String *styleStart;
	struct Uni_String *styleEnd;
	int __styleStart;
	int __styleEnd;
	void * __style16;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_LineProperties;
/***********************/
struct X3D_LineSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int autoOffset;
	struct SFVec3f direction;
	int enabled;
	float maxPosition;
	float minPosition;
	float offset;
	int isActive;
	int isOver;
	struct Uni_String *description;
	struct SFVec3f trackPoint_changed;
	struct SFVec3f translation_changed;
	struct X3D_Node *metadata;
	struct SFVec3f _oldtrackPoint;
	struct SFVec3f _oldtranslation;
	struct SFVec3f _origPoint;
	int __oldEnabled;
};
extern struct X3D_Virt virt_LineSensor;
/***********************/
struct X3D_LineSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *metadata;
	struct X3D_Node *fogCoord;
	struct Multi_Int32 vertexCount;
	void * __vertArr;
	void * __vertIndx;
	void * __starts;
	int __segCount;
	void * __linerep;
};
extern struct X3D_Virt virt_LineSet;
/***********************/
struct X3D_LoadSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	double timeOut;
	struct Multi_Node watchList;
	int isActive;
	int isLoaded;
	double loadTime;
	float progress;
	int __loading;
	int __finishedloading;
	double __StartLoadTime;
	int __oldEnabled;
};
extern struct X3D_Virt virt_LoadSensor;
/***********************/
struct X3D_LocalFog {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFColor color;
	struct Uni_String *fogType;
	float visibilityRange;
	float __fogScale;
	int __fogType;
	int enabled;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_LocalFog;
/***********************/
struct X3D_Material {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFColor emissiveColor;
	struct X3D_Node *emissiveTexture;
	int emissiveTextureChannel;
	struct X3D_Node *normalTexture;
	int normalTextureChannel;
	float transparency;
	void * _material;
	float ambientIntensity;
	struct X3D_Node *ambientTexture;
	int ambientTextureChannel;
	struct SFColor diffuseColor;
	struct X3D_Node *diffuseTexture;
	int diffuseTextureChannel;
	struct SFColor specularColor;
	float shininess;
	struct X3D_Node *specularShininessTexture;
	int specularShininessTextureChannel;
};
extern struct X3D_Virt virt_Material;
/***********************/
struct X3D_Matrix3VertexAttribute {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Matrix3f value;
	struct Uni_String *name;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_Matrix3VertexAttribute;
/***********************/
struct X3D_Matrix4VertexAttribute {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Matrix4f value;
	struct Uni_String *name;
};
extern struct X3D_Virt virt_Matrix4VertexAttribute;
/***********************/
struct X3D_MediaElementAudioSourceNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *context;
	int numberOfInputs;
	int numberOfOutputs;
	int channelCount;
	struct Uni_String *channelCountMode;
	struct Uni_String *channelInterpretation;
};
extern struct X3D_Virt virt_MediaElementAudioSourceNode;
/***********************/
struct X3D_MediaStreamAudioDestinationNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *stream;
};
extern struct X3D_Virt virt_MediaStreamAudioDestinationNode;
/***********************/
struct X3D_MediaStreamTrack {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct X3D_Node *stream;
};
extern struct X3D_Virt virt_MediaStreamTrack;
/***********************/
struct X3D_MetadataBoolean {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_Bool value;
};
extern struct X3D_Virt virt_MetadataBoolean;
/***********************/
struct X3D_MetadataDouble {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_Double value;
};
extern struct X3D_Virt virt_MetadataDouble;
/***********************/
struct X3D_MetadataFloat {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_Float value;
};
extern struct X3D_Virt virt_MetadataFloat;
/***********************/
struct X3D_MetadataInteger {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_Int32 value;
};
extern struct X3D_Virt virt_MetadataInteger;
/***********************/
struct X3D_MetadataMFBool {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Bool value;
	struct Multi_Bool valueChanged;
	struct Multi_Bool setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFBool;
/***********************/
struct X3D_MetadataMFColor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Color value;
	struct Multi_Color valueChanged;
	struct Multi_Color setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFColor;
/***********************/
struct X3D_MetadataMFColorRGBA {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_ColorRGBA value;
	struct Multi_ColorRGBA valueChanged;
	struct Multi_ColorRGBA setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFColorRGBA;
/***********************/
struct X3D_MetadataMFDouble {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Double value;
	struct Multi_Double valueChanged;
	struct Multi_Double setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFDouble;
/***********************/
struct X3D_MetadataMFFloat {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Float value;
	struct Multi_Float valueChanged;
	struct Multi_Float setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFFloat;
/***********************/
struct X3D_MetadataMFInt32 {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 value;
	struct Multi_Int32 valueChanged;
	struct Multi_Int32 setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFInt32;
/***********************/
struct X3D_MetadataMFMatrix3d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Matrix3d value;
	struct Multi_Matrix3d valueChanged;
	struct Multi_Matrix3d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFMatrix3d;
/***********************/
struct X3D_MetadataMFMatrix3f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Matrix3f value;
	struct Multi_Matrix3f valueChanged;
	struct Multi_Matrix3f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFMatrix3f;
/***********************/
struct X3D_MetadataMFMatrix4d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Matrix4d value;
	struct Multi_Matrix4d valueChanged;
	struct Multi_Matrix4d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFMatrix4d;
/***********************/
struct X3D_MetadataMFMatrix4f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Matrix4f value;
	struct Multi_Matrix4f valueChanged;
	struct Multi_Matrix4f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFMatrix4f;
/***********************/
struct X3D_MetadataMFNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node value;
	struct Multi_Node valueChanged;
	struct Multi_Node setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFNode;
/***********************/
struct X3D_MetadataMFRotation {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Rotation value;
	struct Multi_Rotation valueChanged;
	struct Multi_Rotation setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFRotation;
/***********************/
struct X3D_MetadataMFString {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String value;
	struct Multi_String valueChanged;
	struct Multi_String setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFString;
/***********************/
struct X3D_MetadataMFTime {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Time value;
	struct Multi_Time valueChanged;
	struct Multi_Time setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFTime;
/***********************/
struct X3D_MetadataMFVec2d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec2d value;
	struct Multi_Vec2d valueChanged;
	struct Multi_Vec2d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec2d;
/***********************/
struct X3D_MetadataMFVec2f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec2f value;
	struct Multi_Vec2f valueChanged;
	struct Multi_Vec2f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec2f;
/***********************/
struct X3D_MetadataMFVec3d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec3d value;
	struct Multi_Vec3d valueChanged;
	struct Multi_Vec3d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec3d;
/***********************/
struct X3D_MetadataMFVec3f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec3f value;
	struct Multi_Vec3f valueChanged;
	struct Multi_Vec3f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec3f;
/***********************/
struct X3D_MetadataMFVec4d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec4d value;
	struct Multi_Vec4d valueChanged;
	struct Multi_Vec4d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec4d;
/***********************/
struct X3D_MetadataMFVec4f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Vec4f value;
	struct Multi_Vec4f valueChanged;
	struct Multi_Vec4f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataMFVec4f;
/***********************/
struct X3D_MetadataSFBool {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int value;
	int valueChanged;
	int setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFBool;
/***********************/
struct X3D_MetadataSFColor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFColor value;
	struct SFColor valueChanged;
	struct SFColor setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFColor;
/***********************/
struct X3D_MetadataSFColorRGBA {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFColorRGBA value;
	struct SFColorRGBA valueChanged;
	struct SFColorRGBA setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFColorRGBA;
/***********************/
struct X3D_MetadataSFDouble {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	double value;
	double valueChanged;
	double setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFDouble;
/***********************/
struct X3D_MetadataSFFloat {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float value;
	float valueChanged;
	float setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFFloat;
/***********************/
struct X3D_MetadataSFImage {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 value;
	struct Multi_Int32 valueChanged;
	struct Multi_Int32 setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFImage;
/***********************/
struct X3D_MetadataSFInt32 {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int value;
	int valueChanged;
	int setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFInt32;
/***********************/
struct X3D_MetadataSFMatrix3d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFMatrix3d value;
	struct SFMatrix3d valueChanged;
	struct SFMatrix3d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFMatrix3d;
/***********************/
struct X3D_MetadataSFMatrix3f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFMatrix3f value;
	struct SFMatrix3f valueChanged;
	struct SFMatrix3f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFMatrix3f;
/***********************/
struct X3D_MetadataSFMatrix4d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFMatrix4d value;
	struct SFMatrix4d valueChanged;
	struct SFMatrix4d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFMatrix4d;
/***********************/
struct X3D_MetadataSFMatrix4f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFMatrix4f value;
	struct SFMatrix4f valueChanged;
	struct SFMatrix4f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFMatrix4f;
/***********************/
struct X3D_MetadataSFNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *value;
	struct X3D_Node *valueChanged;
	struct X3D_Node *setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFNode;
/***********************/
struct X3D_MetadataSFRotation {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFRotation value;
	struct SFRotation valueChanged;
	struct SFRotation setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFRotation;
/***********************/
struct X3D_MetadataSFString {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *value;
	struct Uni_String *valueChanged;
	struct Uni_String *setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFString;
/***********************/
struct X3D_MetadataSFTime {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	double value;
	double valueChanged;
	double setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFTime;
/***********************/
struct X3D_MetadataSFVec2d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec2d value;
	struct SFVec2d valueChanged;
	struct SFVec2d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec2d;
/***********************/
struct X3D_MetadataSFVec2f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec2f value;
	struct SFVec2f valueChanged;
	struct SFVec2f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec2f;
/***********************/
struct X3D_MetadataSFVec3d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3d value;
	struct SFVec3d valueChanged;
	struct SFVec3d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec3d;
/***********************/
struct X3D_MetadataSFVec3f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f value;
	struct SFVec3f valueChanged;
	struct SFVec3f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec3f;
/***********************/
struct X3D_MetadataSFVec4d {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec4d value;
	struct SFVec4d valueChanged;
	struct SFVec4d setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec4d;
/***********************/
struct X3D_MetadataSFVec4f {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec4f value;
	struct SFVec4f valueChanged;
	struct SFVec4f setValue;
	double tickTime;
};
extern struct X3D_Virt virt_MetadataSFVec4f;
/***********************/
struct X3D_MetadataSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_Node value;
};
extern struct X3D_Virt virt_MetadataSet;
/***********************/
struct X3D_MetadataString {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *name;
	struct Uni_String *reference;
	struct Multi_String value;
};
extern struct X3D_Virt virt_MetadataString;
/***********************/
struct X3D_MotorJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float axis1Angle;
	float axis1Torque;
	float axis2Angle;
	float axis2Torque;
	float axis3Angle;
	float axis3Torque;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	int enabledAxes;
	struct Multi_String forceOutput;
	struct X3D_Node *metadata;
	struct SFVec3f motor1Axis;
	struct SFVec3f motor2Axis;
	struct SFVec3f motor3Axis;
	float stop1Bounce;
	float stop1ErrorCorrection;
	float stop2Bounce;
	float stop2ErrorCorrection;
	float stop3Bounce;
	float stop3ErrorCorrection;
	float motor1Angle;
	float motor1AngleRate;
	float motor2Angle;
	float motor2AngleRate;
	float motor3Angle;
	float motor3AngleRate;
	int autoCalc;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_motor1Axis;
	struct SFVec3f __old_motor2Axis;
	struct SFVec3f __old_motor3Axis;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
	float __old_axis1Angle;
	float __old_axis2Angle;
	float __old_axis3Angle;
};
extern struct X3D_Virt virt_MotorJoint;
/***********************/
struct X3D_MovieTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Uni_String *description;
	int loop;
	struct X3D_Node *metadata;
	double pauseTime;
	float pitch;
	double resumeTime;
	double startTime;
	double stopTime;
	struct Multi_String url;
	double duration_changed;
	double elapsedTime;
	int isActive;
	int isPaused;
	void * _parentResource;
	int __loadstatus;
	void * __loadResource;
	int __sourceNumber;
	double __inittime;
	double __lasttime;
	int repeatS;
	int repeatT;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	float speed;
	float __frac;
	int __ctex;
	int __lowest;
	int __highest;
	void * __fw_movie;
};
extern struct X3D_Virt virt_MovieTexture;
/***********************/
struct X3D_MultiTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float alpha;
	struct SFColor color;
	struct Multi_String function;
	struct X3D_Node *metadata;
	struct Multi_String mode;
	struct Multi_String source;
	struct Multi_Node texture;
	void * __xparams;
};
extern struct X3D_Virt virt_MultiTexture;
/***********************/
struct X3D_MultiTextureCoordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Node texCoord;
};
extern struct X3D_Virt virt_MultiTextureCoordinate;
/***********************/
struct X3D_MultiTextureTransform {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Node textureTransform;
};
extern struct X3D_Virt virt_MultiTextureTransform;
/***********************/
struct X3D_NavigationInfo {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_bind;
	struct Multi_Float avatarSize;
	int headlight;
	float speed;
	struct Multi_String type;
	float visibilityLimit;
	int isBound;
	int _layerId;
	struct Multi_String transitionType;
	double bindTime;
	struct X3D_Node *metadata;
	double transitionTime;
	int transitionComplete;
};
extern struct X3D_Virt virt_NavigationInfo;
/***********************/
struct X3D_Normal {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec3f vector;
};
extern struct X3D_Virt virt_Normal;
/***********************/
struct X3D_NormalInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec3f keyValue;
	struct X3D_Node *metadata;
	struct Multi_Vec3f value_changed;
};
extern struct X3D_Virt virt_NormalInterpolator;
/***********************/
struct X3D_NurbsCurve {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double knot;
	int order;
	int tessellation;
	int closed;
	float _tscale;
	struct Multi_Vec3f __points;
	int __numPoints;
};
extern struct X3D_Virt virt_NurbsCurve;
/***********************/
struct X3D_NurbsCurve2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2d controlPoint;
	struct Multi_Double weight;
	struct Multi_Double knot;
	int order;
	int tessellation;
	int closed;
	float _tscale;
};
extern struct X3D_Virt virt_NurbsCurve2D;
/***********************/
struct X3D_NurbsOrientationInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double knot;
	int order;
	float set_fraction;
	struct SFRotation value_changed;
	struct Multi_Float _knot;
	struct Multi_Vec4f _xyzw;
	int _OK;
	struct SFVec2f _knotrange;
};
extern struct X3D_Virt virt_NurbsOrientationInterpolator;
/***********************/
struct X3D_NurbsPatchSurface {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double uKnot;
	int uOrder;
	int uDimension;
	int uTessellation;
	int uClosed;
	struct Multi_Double vKnot;
	int vOrder;
	int vDimension;
	int vTessellation;
	int vClosed;
	struct X3D_Node *texCoord;
	int solid;
	float _tscale;
};
extern struct X3D_Virt virt_NurbsPatchSurface;
/***********************/
struct X3D_NurbsPositionInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double knot;
	int order;
	float set_fraction;
	struct SFVec3f value_changed;
	struct Multi_Float _knot;
	struct Multi_Vec4f _xyzw;
	int _OK;
	struct SFVec2f _knotrange;
};
extern struct X3D_Virt virt_NurbsPositionInterpolator;
/***********************/
struct X3D_NurbsSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addGeometry;
	struct Multi_Node removeGeometry;
	struct Multi_Node geometry;
	struct X3D_Node *metadata;
	float tessellationScale;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_NurbsSet;
/***********************/
struct X3D_NurbsSurfaceInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double uKnot;
	int uOrder;
	int uDimension;
	struct Multi_Double vKnot;
	int vOrder;
	int vDimension;
	struct SFVec2f set_fraction;
	struct SFVec3f position_changed;
	struct SFVec3f normal_changed;
	struct Multi_Float _uKnot;
	struct Multi_Float _vKnot;
	struct Multi_Vec4f _controlPoint;
	int _OK;
};
extern struct X3D_Virt virt_NurbsSurfaceInterpolator;
/***********************/
struct X3D_NurbsSweptSurface {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *crossSectionCurve;
	struct X3D_Node *metadata;
	struct X3D_Node *trajectoryCurve;
	int ccw;
	int solid;
	struct Uni_String *method;
	struct X3D_Node *_patch;
	int _method;
};
extern struct X3D_Virt virt_NurbsSweptSurface;
/***********************/
struct X3D_NurbsSwungSurface {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *profileCurve;
	struct X3D_Node *trajectoryCurve;
	int ccw;
	int solid;
	struct X3D_Node *_patch;
};
extern struct X3D_Virt virt_NurbsSwungSurface;
/***********************/
struct X3D_NurbsTextureCoordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2f controlPoint;
	struct Multi_Float weight;
	struct Multi_Double uKnot;
	int uOrder;
	int uDimension;
	struct Multi_Double vKnot;
	int vOrder;
	int vDimension;
	struct Multi_Float _uKnot;
	struct Multi_Float _vKnot;
	struct Multi_Vec4f _controlPoint;
};
extern struct X3D_Virt virt_NurbsTextureCoordinate;
/***********************/
struct X3D_NurbsTrimmedSurface {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct X3D_Node *controlPoint;
	struct Multi_Double weight;
	struct Multi_Double uKnot;
	int uOrder;
	int uDimension;
	int uTessellation;
	int uClosed;
	struct Multi_Double vKnot;
	int vOrder;
	int vDimension;
	int vTessellation;
	int vClosed;
	struct X3D_Node *texCoord;
	int solid;
	struct Multi_Node addTrimmingContour;
	struct Multi_Node removeTrimmingContour;
	struct Multi_Node trimmingContour;
	float _tscale;
};
extern struct X3D_Virt virt_NurbsTrimmedSurface;
/***********************/
struct X3D_OSC_Sensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct Uni_String *description;
	struct Uni_String *protocol;
	struct Uni_String *listenfor;
	int port;
	struct Uni_String *filter;
	struct Uni_String *handler;
	struct Multi_String talksTo;
	int FIFOsize;
	int int32Inp;
	float floatInp;
	struct Uni_String *stringInp;
	int gotEvents;
	struct X3D_Node *metadata;
	struct Multi_Node _talkToNodes;
	int _status;
	void * _int32InpFIFO;
	void * _floatInpFIFO;
	void * _stringInpFIFO;
	void * _int32OutFIFO;
	void * _floatOutFIFO;
	void * _stringOutFIFO;
	struct X3D_Node *__oldmetadata;
};
extern struct X3D_Virt virt_OSC_Sensor;
/***********************/
struct X3D_OpacityMapVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *transferFunction;
};
extern struct X3D_Virt virt_OpacityMapVolumeStyle;
/***********************/
struct X3D_OrientationChaser {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct SFRotation value_changed;
	struct SFRotation initialDestination;
	struct SFRotation initialValue;
	struct SFRotation set_destination;
	struct SFRotation set_value;
	void * _buffer;
	struct SFRotation _previousvalue;
	struct SFRotation _destination;
};
extern struct X3D_Virt virt_OrientationChaser;
/***********************/
struct X3D_OrientationDamper {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct SFRotation value_changed;
	struct SFRotation initialDestination;
	struct SFRotation initialValue;
	struct SFRotation set_destination;
	struct SFRotation set_value;
	void * _values;
	struct SFRotation _input;
};
extern struct X3D_Virt virt_OrientationDamper;
/***********************/
struct X3D_OrientationInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Rotation keyValue;
	struct X3D_Node *metadata;
	struct SFRotation value_changed;
};
extern struct X3D_Virt virt_OrientationInterpolator;
/***********************/
struct X3D_OrthoViewpoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int _layerId;
	int _donethispass;
	int set_bind;
	double bindTime;
	int isBound;
	struct Uni_String *description;
	int jump;
	struct Multi_Float fieldOfView;
	struct X3D_Node *metadata;
	struct SFRotation orientation;
	struct SFVec3f position;
	struct SFVec3f centerOfRotation;
	int retainUserOffsets;
	int _initializedOnce;
	struct SFRotation _orientation;
	struct SFVec3f _position;
};
extern struct X3D_Virt virt_OrthoViewpoint;
/***********************/
struct X3D_OscillatorNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int frequency;
	struct Uni_String *type;
	float detune;
};
extern struct X3D_Virt virt_OscillatorNode;
/***********************/
struct X3D_PackagedShader {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int activate;
	struct X3D_Node *metadata;
	struct Multi_String url;
	int isSelected;
	int isValid;
	struct Uni_String *language;
	int _initialized;
	int _shaderUserNumber;
	struct X3D_Node *_shaderUserDefinedFields;
	pthread_t _shaderLoadThread;
	int _retrievedURLData;
};
extern struct X3D_Virt virt_PackagedShader;
/***********************/
struct X3D_PannerNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	struct SFVec3f position;
	struct SFRotation orientation;
	struct SFVec3f velocity;
	float coneInnerAngle;
	float coneOuterAngle;
	float coneGain;
	struct Uni_String *distanceModel;
	float maxDistance;
	struct Uni_String *panningModel;
	float refDistance;
	float rolloffFactor;
};
extern struct X3D_Virt virt_PannerNode;
/***********************/
struct X3D_ParticleSystem {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *appearance;
	struct X3D_Node *geometry;
	struct X3D_Node *metadata;
	int _shaderflags_base;
	int _shaderflags_effects;
	int _shaderflags_usershaders;
	int createParticles;
	int enabled;
	float lifetimeVariation;
	int maxParticles;
	float particleLifetime;
	struct SFVec2f particleSize;
	int isActive;
	struct X3D_Node *colorRamp;
	struct Multi_Float colorKey;
	struct X3D_Node *emitter;
	struct Uni_String *geometryType;
	struct Multi_Node physics;
	struct X3D_Node *texCoordRamp;
	struct Multi_Float texCoordKey;
	void * _tris;
	void * _ttex;
	void * _ltex;
	void * _particles;
	double _lasttime;
	int _lastEnabled;
	int _geometryType;
	float _remainder;
};
extern struct X3D_Virt virt_ParticleSystem;
/***********************/
struct X3D_PeriodicWave {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int frequency;
	struct Uni_String *type;
	float detune;
};
extern struct X3D_Virt virt_PeriodicWave;
/***********************/
struct X3D_PhysicalMaterial {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFColor emissiveColor;
	struct X3D_Node *emissiveTexture;
	int emissiveTextureChannel;
	struct X3D_Node *normalTexture;
	int normalTextureChannel;
	float transparency;
	void * _material;
	struct SFColor baseColor;
	struct X3D_Node *baseTexture;
	int baseTextureChannel;
	float metallic;
	struct X3D_Node *metallicRoughnessTexture;
	int metallicRoughnessTextureChannel;
	float roughness;
};
extern struct X3D_Virt virt_PhysicalMaterial;
/***********************/
struct X3D_PickableGroup {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct Multi_String objectType;
	int pickable;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_PickableGroup;
/***********************/
struct X3D_PixelTexture {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 image;
	struct X3D_Node *metadata;
	int repeatS;
	int repeatT;
	struct X3D_Node *textureProperties;
	void * _parentResource;
	int __textureTableIndex;
};
extern struct X3D_Virt virt_PixelTexture;
/***********************/
struct X3D_PixelTexture3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 image;
	struct X3D_Node *metadata;
	int repeatS;
	int repeatT;
	int repeatR;
	struct X3D_Node *textureProperties;
	int __textureTableIndex;
	void * _parentResource;
	int _needs_gradient;
};
extern struct X3D_Virt virt_PixelTexture3D;
/***********************/
struct X3D_PlaneSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int autoOffset;
	struct SFRotation axisRotation;
	int enabled;
	struct SFVec2f maxPosition;
	struct SFVec2f minPosition;
	struct SFVec3f offset;
	int isActive;
	int isOver;
	struct Uni_String *description;
	struct SFVec3f trackPoint_changed;
	struct SFVec3f translation_changed;
	struct X3D_Node *metadata;
	int sensorLocalOutput;
	struct SFVec3f _oldtrackPoint;
	struct SFVec3f _oldtranslation;
	struct SFVec3f _origPoint;
	int __oldEnabled;
};
extern struct X3D_Virt virt_PlaneSensor;
/***********************/
struct X3D_PointEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f direction;
	struct X3D_Node *metadata;
	struct SFVec3f position;
	float speed;
	float variation;
	float mass;
	float surfaceArea;
};
extern struct X3D_Virt virt_PointEmitter;
/***********************/
struct X3D_PointLight {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int global;
	int on;
	float ambientIntensity;
	float intensity;
	struct SFColor color;
	struct SFVec3f attenuation;
	struct SFVec3f location;
	float radius;
	struct SFVec4f _loc;
	struct SFVec4f _col;
	struct SFVec4f _amb;
};
extern struct X3D_Virt virt_PointLight;
/***********************/
struct X3D_PointPickSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_String objectType;
	struct X3D_Node *pickingGeometry;
	struct Multi_Node pickTarget;
	int isActive;
	struct Multi_Node pickedGeometry;
	struct Uni_String *intersectionType;
	struct Uni_String *sortOrder;
	struct Uni_String *matchCriterion;
	int __oldEnabled;
	struct Multi_Vec3f pickedPoint;
	int _oldisActive;
	struct Multi_Node _oldpickTarget;
	struct Multi_Node _oldpickedGeometry;
	struct Multi_Vec3f _oldpickedPoint;
	struct SFVec3f _bboxCenter;
	struct SFVec3f _bboxSize;
	struct Uni_String *set_intersectionType;
	struct Uni_String *set_sortOrder;
};
extern struct X3D_Virt virt_PointPickSensor;
/***********************/
struct X3D_PointProperties {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float pointSizeScaleFactor;
	float pointSizeMinValue;
	float pointSizeMaxValue;
	struct Multi_Float pointSizeAttenuation;
	struct Uni_String *colorMode;
	struct X3D_Node *metadata;
	int _colormode;
	struct SFVec3f _attenuation;
	int _pointMethod;
};
extern struct X3D_Virt virt_PointProperties;
/***********************/
struct X3D_PointSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int autoOffset;
	int enabled;
	struct SFVec3f maxPosition;
	struct SFVec3f minPosition;
	struct SFVec3f offset;
	int isActive;
	int isOver;
	struct Uni_String *description;
	struct SFVec3f trackPoint_changed;
	struct SFVec3f translation_changed;
	struct X3D_Node *metadata;
	struct SFVec3f _oldtrackPoint;
	struct SFVec3f _oldtranslation;
	struct SFVec3f _origPoint;
	int __oldEnabled;
};
extern struct X3D_Virt virt_PointSensor;
/***********************/
struct X3D_PointSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	int _pointsVBO;
	int _coloursVBO;
	int _fogcoordVBO;
	int _npoints;
	int _colourSize;
};
extern struct X3D_Virt virt_PointSet;
/***********************/
struct X3D_Polyline2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2f lineSegments;
	void * __linerep;
};
extern struct X3D_Virt virt_Polyline2D;
/***********************/
struct X3D_PolylineEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_coordIndex;
	int set_coordinate;
	struct X3D_Node *coord;
	struct SFVec3f direction;
	struct X3D_Node *metadata;
	float speed;
	float variation;
	struct Multi_Int32 coordIndex;
	float mass;
	float surfaceArea;
	int _method;
	int _nseg;
	void * _segs;
	void * _portions;
};
extern struct X3D_Virt virt_PolylineEmitter;
/***********************/
struct X3D_Polypoint2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2f point;
};
extern struct X3D_Virt virt_Polypoint2D;
/***********************/
struct X3D_PositionChaser {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct SFVec3f value_changed;
	struct SFVec3f initialDestination;
	struct SFVec3f initialValue;
	struct SFVec3f set_destination;
	struct SFVec3f set_value;
	void * _buffer;
	struct SFVec3f _previousvalue;
	struct SFVec3f _destination;
};
extern struct X3D_Virt virt_PositionChaser;
/***********************/
struct X3D_PositionChaser2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct SFVec2f value_changed;
	struct SFVec2f initialDestination;
	struct SFVec2f initialValue;
	struct SFVec2f set_destination;
	struct SFVec2f set_value;
	void * _buffer;
	struct SFVec2f _previousvalue;
	struct SFVec2f _destination;
};
extern struct X3D_Virt virt_PositionChaser2D;
/***********************/
struct X3D_PositionDamper {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct SFVec3f value_changed;
	struct SFVec3f initialDestination;
	struct SFVec3f initialValue;
	struct SFVec3f set_destination;
	struct SFVec3f set_value;
	void * _values;
	struct SFVec3f _input;
};
extern struct X3D_Virt virt_PositionDamper;
/***********************/
struct X3D_PositionDamper2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct SFVec2f value_changed;
	struct SFVec2f initialDestination;
	struct SFVec2f initialValue;
	struct SFVec2f set_destination;
	struct SFVec2f set_value;
	void * _values;
	struct SFVec2f _input;
};
extern struct X3D_Virt virt_PositionDamper2D;
/***********************/
struct X3D_PositionInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec3f keyValue;
	struct X3D_Node *metadata;
	struct SFVec3f value_changed;
};
extern struct X3D_Virt virt_PositionInterpolator;
/***********************/
struct X3D_PositionInterpolator2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Vec2f keyValue;
	struct X3D_Node *metadata;
	struct SFVec2f value_changed;
};
extern struct X3D_Virt virt_PositionInterpolator2D;
/***********************/
struct X3D_PrimitivePickSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_String objectType;
	struct X3D_Node *pickingGeometry;
	struct Multi_Node pickTarget;
	int isActive;
	struct Multi_Node pickedGeometry;
	struct Uni_String *intersectionType;
	struct Uni_String *sortOrder;
	struct Uni_String *matchCriterion;
	int __oldEnabled;
};
extern struct X3D_Virt virt_PrimitivePickSensor;
/***********************/
struct X3D_ProgramShader {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int activate;
	struct X3D_Node *metadata;
	struct Multi_Node programs;
	int isSelected;
	int isValid;
	struct Uni_String *language;
	int _initialized;
	int _shaderUserNumber;
	pthread_t _shaderLoadThread;
	int _retrievedURLData;
};
extern struct X3D_Virt virt_ProgramShader;
/***********************/
struct X3D_ProjectionVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	float intensityThreshold;
	struct Uni_String *type;
	int _type;
};
extern struct X3D_Virt virt_ProjectionVolumeStyle;
/***********************/
struct X3D_Proto {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node __children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Multi_Node _sortedChildren;
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	void * __protoDeclares;
	void * __externProtoDeclares;
	void * __nodes;
	void * __subcontexts;
	void * __GC;
	void * __protoDef;
	int __protoFlags;
	struct X3D_Node *__prototype;
	struct X3D_Node *__parentProto;
	void * __ROUTES;
	void * __EXPORTS;
	void * __IMPORTS;
	void * __DEFnames;
	void * __IS;
	void * __scripts;
	struct Multi_String url;
	struct Multi_String __oldurl;
	void * __afterPound;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
	void * __typename;
	int load;
	int __oldload;
	double __unitlengthfactor;
	int __specversion;
};
extern struct X3D_Virt virt_Proto;
/***********************/
struct X3D_ProximitySensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	struct SFVec3f size;
	int enabled;
	int isActive;
	struct SFVec3f position_changed;
	struct SFRotation orientation_changed;
	double enterTime;
	double exitTime;
	struct SFVec3f centerOfRotation_changed;
	struct X3D_Node *metadata;
	int __hit;
	struct SFVec3f __t1;
	struct SFRotation __t2;
	int __oldEnabled;
};
extern struct X3D_Virt virt_ProximitySensor;
/***********************/
struct X3D_Pyramid {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int solid;
	void * __ifsnode;
};
extern struct X3D_Virt virt_Pyramid;
/***********************/
struct X3D_QuadSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_QuadSet;
/***********************/
struct X3D_ReceiverPdu {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	double timestamp;
	struct Uni_String *address;
	int port;
	struct Uni_String *multicastRelayHost;
	int multicastRelayPort;
	struct Uni_String *networkMode;
	int isNetworkReader;
	int isNetworkWriter;
	int isStandAlone;
	double readInterval;
	double writeInterval;
	int rtpHeaderExpected;
	int isRtpHeaderHeard;
	int _registered;
	struct X3D_Node *_dsock;
	double _lasttime;
	int _pduchange_networksensor;
	struct X3D_Node *_oldState;
	int entityID;
	int applicationID;
	int siteID;
	struct Multi_String geoSystem;
	struct SFVec3d geoCoords;
	struct X3D_Node *__geoSystem;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int radioID;
	int whichGeometry;
	int receiverState;
	float receivedPower;
	int transmitterEntityID;
	int transmitterApplicationID;
	int transmitterSiteID;
	int transmitterRadioID;
	int _pduchange_receiver;
};
extern struct X3D_Virt virt_ReceiverPdu;
/***********************/
struct X3D_Rectangle2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFVec2f size;
	int solid;
	struct Multi_Vec3f __points;
	int __numPoints;
};
extern struct X3D_Virt virt_Rectangle2D;
/***********************/
struct X3D_RigidBody {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float angularDampingFactor;
	struct SFVec3f angularVelocity;
	int autoDamp;
	int autoDisable;
	struct SFVec3f centerOfMass;
	float disableAngularSpeed;
	float disableLinearSpeed;
	float disableTime;
	int enabled;
	struct SFVec3f finiteRotationAxis;
	int fixed;
	struct Multi_Vec3f forces;
	struct Multi_Node geometry;
	struct SFMatrix3f inertia;
	float linearDampingFactor;
	struct SFVec3f linearVelocity;
	float mass;
	struct X3D_Node *massDensityModel;
	struct X3D_Node *metadata;
	struct SFRotation orientation;
	struct SFVec3f position;
	struct Multi_Vec3f torques;
	int useFiniteRotation;
	int useGlobalGravity;
	void * _body;
	struct SFVec3f __old_angularVelocity;
	struct SFVec3f __old_centerOfMass;
	struct SFVec3f __old_finiteRotationAxis;
	struct SFVec3f __old_linearVelocity;
	struct SFRotation __old_orientation;
	struct SFVec3f __old_position;
	void * _geomIdentityTransform;
};
extern struct X3D_Virt virt_RigidBody;
/***********************/
struct X3D_RigidBodyCollection {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node set_contacts;
	int autoDisable;
	struct Multi_Node bodies;
	float constantForceMix;
	float contactSurfaceThickness;
	float disableAngularSpeed;
	float disableLinearSpeed;
	float disableTime;
	int enabled;
	float errorCorrection;
	struct SFVec3f gravity;
	int iterations;
	struct Multi_Node joints;
	float maxCorrectionSpeed;
	struct X3D_Node *metadata;
	int preferAccuracy;
	struct X3D_Node *collider;
	void * _world;
	void * _group;
};
extern struct X3D_Virt virt_RigidBodyCollection;
/***********************/
struct X3D_ScalarChaser {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	float value_changed;
	float initialDestination;
	float initialValue;
	float set_destination;
	float set_value;
	void * _buffer;
	float _previousvalue;
	float _destination;
};
extern struct X3D_Virt virt_ScalarChaser;
/***********************/
struct X3D_ScalarDamper {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	float value_changed;
	float initialDestination;
	float initialValue;
	float set_destination;
	float set_value;
	void * _values;
	float _input;
};
extern struct X3D_Virt virt_ScalarDamper;
/***********************/
struct X3D_ScalarInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Float keyValue;
	struct X3D_Node *metadata;
	float value_changed;
};
extern struct X3D_Virt virt_ScalarInterpolator;
/***********************/
struct X3D_ScreenFontStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String family;
	int horizontal;
	struct Multi_String justify;
	struct Uni_String *language;
	int leftToRight;
	float pointSize;
	float spacing;
	struct Uni_String *style;
	int topToBottom;
};
extern struct X3D_Virt virt_ScreenFontStyle;
/***********************/
struct X3D_ScreenGroup {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_ScreenGroup;
/***********************/
struct X3D_Script {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String url;
	int directOutput;
	int mustEvaluate;
	struct X3D_Node *metadata;
	void * __scriptObj;
	void * _parentResource;
};
extern struct X3D_Virt virt_Script;
/***********************/
struct X3D_SegmentedVolumeData {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f dimensions;
	struct X3D_Node *metadata;
	struct X3D_Node *voxels;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	void * _boxtris;
	struct Multi_Node renderStyle;
	struct Multi_Bool segmentEnabled;
	struct X3D_Node *segmentIdentifiers;
};
extern struct X3D_Virt virt_SegmentedVolumeData;
/***********************/
struct X3D_ShadedVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	int lighting;
	int shadows;
	struct X3D_Node *material;
	struct Uni_String *phaseFunction;
	int _phaseFunction;
};
extern struct X3D_Virt virt_ShadedVolumeStyle;
/***********************/
struct X3D_ShaderPart {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	struct Uni_String *type;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
	struct X3D_Node *_shaderUserDefinedFields;
};
extern struct X3D_Virt virt_ShaderPart;
/***********************/
struct X3D_ShaderProgram {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_String url;
	struct Uni_String *type;
	int __loadstatus;
	void * _parentResource;
	void * __loadResource;
	struct X3D_Node *_shaderUserDefinedFields;
};
extern struct X3D_Virt virt_ShaderProgram;
/***********************/
struct X3D_Shape {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *appearance;
	struct X3D_Node *geometry;
	struct X3D_Node *metadata;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int _shaderflags_base;
	int _shaderflags_effects;
	int _shaderflags_usershaders;
	int __visible;
	int __occludeCheckCount;
	int __Samples;
};
extern struct X3D_Virt virt_Shape;
/***********************/
struct X3D_SignalPdu {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	double timestamp;
	struct Uni_String *address;
	int port;
	struct Uni_String *multicastRelayHost;
	int multicastRelayPort;
	struct Uni_String *networkMode;
	int isNetworkReader;
	int isNetworkWriter;
	int isStandAlone;
	double readInterval;
	double writeInterval;
	int rtpHeaderExpected;
	int isRtpHeaderHeard;
	int _registered;
	struct X3D_Node *_dsock;
	double _lasttime;
	int _pduchange_networksensor;
	struct X3D_Node *_oldState;
	int entityID;
	int applicationID;
	int siteID;
	struct Multi_String geoSystem;
	struct SFVec3d geoCoords;
	struct X3D_Node *__geoSystem;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int radioID;
	int whichGeometry;
	struct Multi_Int32 data;
	int dataLength;
	int encodingScheme;
	int sampleRate;
	int samples;
	int tdlType;
	int _pduchange_signal;
};
extern struct X3D_Virt virt_SignalPdu;
/***********************/
struct X3D_SilhouetteEnhancementVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	float silhouetteBoundaryOpacity;
	float silhouetteRetainedOpacity;
	float silhouetteSharpness;
};
extern struct X3D_Virt virt_SilhouetteEnhancementVolumeStyle;
/***********************/
struct X3D_SingleAxisHingeJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f anchorPoint;
	struct SFVec3f axis;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	struct Multi_String forceOutput;
	float maxAngle;
	struct X3D_Node *metadata;
	float minAngle;
	float stopBounce;
	float stopErrorCorrection;
	float angle;
	float angleRate;
	struct SFVec3f body1AnchorPoint;
	struct SFVec3f body2AnchorPoint;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_anchorPoint;
	struct SFVec3f __old_axis;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
};
extern struct X3D_Virt virt_SingleAxisHingeJoint;
/***********************/
struct X3D_SliderJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f axis;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	struct Multi_String forceOutput;
	float maxSeparation;
	struct X3D_Node *metadata;
	float minSeparation;
	float sliderForce;
	float stopBounce;
	float stopErrorCorrection;
	float separation;
	float separationRate;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_axis;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
};
extern struct X3D_Virt virt_SliderJoint;
/***********************/
struct X3D_Sound {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f direction;
	float intensity;
	struct SFVec3f location;
	float maxBack;
	float maxFront;
	struct X3D_Node *metadata;
	float minBack;
	float minFront;
	float priority;
	struct X3D_Node *source;
	int spatialize;
	int __sourceNumber;
	struct SFVec3f __lastlocation;
	double __lasttime;
};
extern struct X3D_Virt virt_Sound;
/***********************/
struct X3D_Sphere {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	float radius;
	int solid;
	struct Multi_Vec3f __points;
	int _sideVBO;
	int __SphereIndxVBO;
	void * __pindices;
	int __wireindicesVBO;
};
extern struct X3D_Virt virt_Sphere;
/***********************/
struct X3D_SphereSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int autoOffset;
	int enabled;
	struct SFRotation offset;
	int isActive;
	struct SFRotation rotation_changed;
	struct SFVec3f trackPoint_changed;
	struct SFVec3f _oldtrackPoint;
	struct SFRotation _oldrotation;
	int isOver;
	struct Uni_String *description;
	struct X3D_Node *metadata;
	struct SFVec3f _origPoint;
	struct SFVec3f _origNormalizedPoint;
	float _radius;
	int __oldEnabled;
};
extern struct X3D_Virt virt_SphereSensor;
/***********************/
struct X3D_SplinePositionInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	int closed;
	struct Multi_Float key;
	struct Multi_Vec3f keyValue;
	struct Multi_Vec3f keyVelocity;
	struct X3D_Node *metadata;
	int normalizeVelocity;
	struct SFVec3f value_changed;
	struct Multi_Vec3f _T0;
	struct Multi_Vec3f _T1;
};
extern struct X3D_Virt virt_SplinePositionInterpolator;
/***********************/
struct X3D_SplinePositionInterpolator2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	int closed;
	struct Multi_Float key;
	struct Multi_Vec2f keyValue;
	struct Multi_Vec2f keyVelocity;
	struct X3D_Node *metadata;
	int normalizeVelocity;
	struct SFVec2f value_changed;
	struct Multi_Vec2f _T0;
	struct Multi_Vec2f _T1;
};
extern struct X3D_Virt virt_SplinePositionInterpolator2D;
/***********************/
struct X3D_SplineScalarInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	int closed;
	struct Multi_Float key;
	struct Multi_Float keyValue;
	struct Multi_Float keyVelocity;
	struct X3D_Node *metadata;
	int normalizeVelocity;
	float value_changed;
	struct Multi_Float _T0;
	struct Multi_Float _T1;
};
extern struct X3D_Virt virt_SplineScalarInterpolator;
/***********************/
struct X3D_SpotLight {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int global;
	int on;
	float ambientIntensity;
	float intensity;
	struct SFColor color;
	struct SFVec3f attenuation;
	float beamWidth;
	float cutOffAngle;
	struct SFVec3f direction;
	struct SFVec3f location;
	float radius;
	struct SFVec4f _dir;
	struct SFVec4f _loc;
	struct SFVec4f _col;
	struct SFVec4f _amb;
};
extern struct X3D_Virt virt_SpotLight;
/***********************/
struct X3D_SquadOrientationInterpolator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float set_fraction;
	struct Multi_Float key;
	struct Multi_Rotation keyValue;
	struct X3D_Node *metadata;
	int normalizeVelocity;
	int closed;
	struct SFRotation value_changed;
	struct Multi_Float _normkey;
	struct Multi_Rotation _normkeyValue;
};
extern struct X3D_Virt virt_SquadOrientationInterpolator;
/***********************/
struct X3D_StaticGroup {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Node children;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	struct Multi_Node __sibAffectors;
	int __transparency;
	int __solid;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_StaticGroup;
/***********************/
struct X3D_StringSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int deletionAllowed;
	int enabled;
	struct Uni_String *enteredText;
	struct Uni_String *finalText;
	int isActive;
	struct X3D_Node *metadata;
	int singleton;
	int _initialized;
	int __oldEnabled;
};
extern struct X3D_Virt virt_StringSensor;
/***********************/
struct X3D_SurfaceEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_coordIndex;
	int set_coordinate;
	struct X3D_Node *metadata;
	float speed;
	float variation;
	struct Multi_Int32 coordIndex;
	float mass;
	struct X3D_Node *surface;
	float surfaceArea;
	struct X3D_Node *geometry;
	void * _ifs;
};
extern struct X3D_Virt virt_SurfaceEmitter;
/***********************/
struct X3D_Switch {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node choice;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	int whichChoice;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __isX3D;
};
extern struct X3D_Virt virt_Switch;
/***********************/
struct X3D_Teapot {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	int solid;
	void * __ifsnode;
};
extern struct X3D_Virt virt_Teapot;
/***********************/
struct X3D_TexCoordChaser2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	int isActive;
	double duration;
	double _bufferendtime;
	double _steptime;
	struct Multi_Vec2f value_changed;
	struct Multi_Vec2f initialDestination;
	struct Multi_Vec2f initialValue;
	struct Multi_Vec2f set_destination;
	struct Multi_Vec2f set_value;
	void * _buffer;
	struct Multi_Vec2f _previousvalue;
	struct Multi_Vec2f _destination;
};
extern struct X3D_Virt virt_TexCoordChaser2D;
/***********************/
struct X3D_TexCoordDamper2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	void * _p;
	void * _t;
	double tau;
	float tolerance;
	int isActive;
	int order;
	double _tau;
	double _lasttick;
	int _takefirstinput;
	struct Multi_Vec2f value_changed;
	struct Multi_Vec2f initialDestination;
	struct Multi_Vec2f initialValue;
	struct Multi_Vec2f set_destination;
	struct Multi_Vec2f set_value;
	void * _values;
	struct Multi_Vec2f _input;
};
extern struct X3D_Virt virt_TexCoordDamper2D;
/***********************/
struct X3D_Text {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *fontStyle;
	struct Multi_Float length;
	float maxExtent;
	struct X3D_Node *metadata;
	struct Multi_String string;
	struct Multi_Vec2f lineBounds;
	struct SFVec3f origin;
	int solid;
	struct SFVec2f textBounds;
	int _isScreen;
	void * _screendata;
};
extern struct X3D_Virt virt_Text;
/***********************/
struct X3D_TextureBackground {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_bind;
	struct Multi_Float groundAngle;
	struct Multi_Color groundColor;
	struct Multi_Float skyAngle;
	struct Multi_Color skyColor;
	double bindTime;
	int isBound;
	int _layerId;
	struct X3D_Node *metadata;
	void * _parentResource;
	struct Multi_Vec3f __points;
	struct Multi_Vec3f __colours;
	int __quadcount;
	int __VBO;
	struct X3D_Node *frontTexture;
	struct X3D_Node *backTexture;
	struct X3D_Node *topTexture;
	struct X3D_Node *bottomTexture;
	struct X3D_Node *leftTexture;
	struct X3D_Node *rightTexture;
	struct Multi_Float transparency;
};
extern struct X3D_Virt virt_TextureBackground;
/***********************/
struct X3D_TextureCoordinate {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2f point;
};
extern struct X3D_Virt virt_TextureCoordinate;
/***********************/
struct X3D_TextureCoordinate3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec3f point;
};
extern struct X3D_Virt virt_TextureCoordinate3D;
/***********************/
struct X3D_TextureCoordinate4D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec4f point;
};
extern struct X3D_Virt virt_TextureCoordinate4D;
/***********************/
struct X3D_TextureCoordinateGenerator {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *mode;
	struct Multi_Float parameter;
};
extern struct X3D_Virt virt_TextureCoordinateGenerator;
/***********************/
struct X3D_TextureProjectorParallel {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *description;
	struct SFVec3f location;
	struct SFVec3f direction;
	struct SFVec3f upVector;
	struct Multi_Float fieldOfView;
	float aspectRatio;
	float nearDistance;
	float farDistance;
	int global;
	int on;
	struct X3D_Node *texture;
	int backCull;
	struct SFVec4f _dir;
	struct SFVec4f _loc;
	struct SFVec4f _upVec;
};
extern struct X3D_Virt virt_TextureProjectorParallel;
/***********************/
struct X3D_TextureProjectorPerspective {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Uni_String *description;
	struct SFVec3f location;
	struct SFVec3f direction;
	struct SFVec3f upVector;
	float fieldOfView;
	float aspectRatio;
	float nearDistance;
	float farDistance;
	int global;
	int on;
	struct X3D_Node *texture;
	int backCull;
	struct SFVec4f _dir;
	struct SFVec4f _loc;
	struct SFVec4f _upVec;
};
extern struct X3D_Virt virt_TextureProjectorPerspective;
/***********************/
struct X3D_TextureProperties {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float anisotropicDegree;
	struct SFColorRGBA borderColor;
	int borderWidth;
	struct Uni_String *boundaryModeS;
	struct Uni_String *boundaryModeT;
	struct Uni_String *boundaryModeR;
	struct Uni_String *magnificationFilter;
	struct X3D_Node *metadata;
	struct Uni_String *minificationFilter;
	struct Uni_String *textureCompression;
	float texturePriority;
	int generateMipMaps;
};
extern struct X3D_Virt virt_TextureProperties;
/***********************/
struct X3D_TextureTransform {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec2f center;
	struct X3D_Node *metadata;
	float rotation;
	struct SFVec2f scale;
	struct SFVec2f translation;
};
extern struct X3D_Virt virt_TextureTransform;
/***********************/
struct X3D_TextureTransform3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFVec3f translation;
};
extern struct X3D_Virt virt_TextureTransform3D;
/***********************/
struct X3D_TextureTransformMatrix3D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFMatrix4f matrix;
};
extern struct X3D_Virt virt_TextureTransformMatrix3D;
/***********************/
struct X3D_TimeSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	double cycleInterval;
	int enabled;
	int loop;
	struct X3D_Node *metadata;
	double pauseTime;
	double resumeTime;
	double startTime;
	double stopTime;
	double cycleTime;
	double elapsedTime;
	float fraction_changed;
	int isActive;
	double isPaused;
	double time;
	double __inittime;
	double __ctflag;
	int __oldEnabled;
	double __lasttime;
};
extern struct X3D_Virt virt_TimeSensor;
/***********************/
struct X3D_TimeTrigger {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int set_boolean;
	double triggerTime;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_TimeTrigger;
/***********************/
struct X3D_ToneMappedVolumeStyle {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct X3D_Node *surfaceNormals;
	struct SFColorRGBA coolColor;
	struct SFColorRGBA warmColor;
};
extern struct X3D_Virt virt_ToneMappedVolumeStyle;
/***********************/
struct X3D_TouchSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct SFVec3f hitNormal_changed;
	struct SFVec3f hitPoint_changed;
	struct SFVec2f hitTexCoord_changed;
	struct SFVec3f _oldhitNormal;
	struct SFVec3f _oldhitPoint;
	struct SFVec2f _oldhitTexCoord;
	int isActive;
	int isOver;
	struct Uni_String *description;
	double touchTime;
	struct X3D_Node *metadata;
	int __oldEnabled;
};
extern struct X3D_Virt virt_TouchSensor;
/***********************/
struct X3D_TrackingSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct SFVec3f position;
	struct SFRotation rotation;
	int isActive;
	struct Uni_String *description;
	struct X3D_Node *metadata;
	int isPositionAvailable;
	int isRotationAvailable;
};
extern struct X3D_Virt virt_TrackingSensor;
/***********************/
struct X3D_Transform {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct SFVec3f center;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct SFRotation rotation;
	struct SFVec3f scale;
	struct SFRotation scaleOrientation;
	struct SFVec3f translation;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int __do_center;
	int __do_trans;
	int __do_rotation;
	int __do_scaleO;
	int __do_scale;
	int __do_anything;
	struct Multi_Node _sortedChildren;
};
extern struct X3D_Virt virt_Transform;
/***********************/
struct X3D_TransformSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	struct SFVec3f size;
	int enabled;
	int isActive;
	struct SFVec3f position_changed;
	struct SFRotation orientation_changed;
	double enterTime;
	double exitTime;
	struct X3D_Node *targetObject;
	struct X3D_Node *metadata;
	int __hit;
	struct SFVec3f __t1;
	struct SFRotation __t2;
	int __oldEnabled;
};
extern struct X3D_Virt virt_TransformSensor;
/***********************/
struct X3D_TransmitterPdu {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	int isActive;
	double timestamp;
	struct Uni_String *address;
	int port;
	struct Uni_String *multicastRelayHost;
	int multicastRelayPort;
	struct Uni_String *networkMode;
	int isNetworkReader;
	int isNetworkWriter;
	int isStandAlone;
	double readInterval;
	double writeInterval;
	int rtpHeaderExpected;
	int isRtpHeaderHeard;
	int _registered;
	struct X3D_Node *_dsock;
	double _lasttime;
	int _pduchange_networksensor;
	struct X3D_Node *_oldState;
	int entityID;
	int applicationID;
	int siteID;
	struct Multi_String geoSystem;
	struct SFVec3d geoCoords;
	struct X3D_Node *__geoSystem;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	int radioID;
	int whichGeometry;
	int radioEntityTypeKind;
	int radioEntityTypeDomain;
	int radioEntityTypeCountry;
	int radioEntityTypeCategory;
	int radioEntityTypeNomenclature;
	int radioEntityTypeNomenclatureVersion;
	struct SFVec3f antennaLocation;
	int antennaPatternLength;
	int antennaPatternType;
	struct SFVec3f relativeAntennaLocation;
	int inputSource;
	int transmitState;
	float power;
	int frequency;
	float transmitFrequencyBandwidth;
	int lengthOfModulationParameters;
	int modulationTypeDetail;
	int modulationTypeMajor;
	int modulationTypeSpreadSpectrum;
	int modulationTypeSystem;
	int cryptoSystem;
	int cryptoKeyID;
	int _pduchange_transmitter;
};
extern struct X3D_Virt virt_TransmitterPdu;
/***********************/
struct X3D_TriangleFanSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct Multi_Int32 fanCount;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_TriangleFanSet;
/***********************/
struct X3D_TriangleSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_TriangleSet;
/***********************/
struct X3D_TriangleSet2D {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct Multi_Vec2f vertices;
	int solid;
	struct Multi_Vec2f __texCoords;
	void * __wireindices;
};
extern struct X3D_Virt virt_TriangleSet2D;
/***********************/
struct X3D_TriangleStripSet {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node attrib;
	struct X3D_Node *color;
	struct X3D_Node *coord;
	struct X3D_Node *fogCoord;
	struct X3D_Node *metadata;
	struct X3D_Node *normal;
	struct Multi_Int32 stripCount;
	struct X3D_Node *texCoord;
	int ccw;
	int colorPerVertex;
	int normalPerVertex;
	int solid;
	struct Multi_Int32 _coordIndex;
};
extern struct X3D_Virt virt_TriangleStripSet;
/***********************/
struct X3D_TwoSidedMaterial {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	float ambientIntensity;
	float backAmbientIntensity;
	struct SFColor backDiffuseColor;
	struct SFColor backEmissiveColor;
	float backShininess;
	struct SFColor backSpecularColor;
	float backTransparency;
	struct SFColor diffuseColor;
	struct SFColor emissiveColor;
	struct X3D_Node *metadata;
	float shininess;
	int separateBackColor;
	struct SFColor specularColor;
	float transparency;
	void * _material;
	void * _backMaterial;
};
extern struct X3D_Virt virt_TwoSidedMaterial;
/***********************/
struct X3D_UniversalJoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f anchorPoint;
	struct SFVec3f axis1;
	struct SFVec3f axis2;
	struct X3D_Node *body1;
	struct X3D_Node *body2;
	struct X3D_Node *metadata;
	struct Multi_String forceOutput;
	float stop1Bounce;
	float stop1ErrorCorrection;
	float stop2Bounce;
	float stop2ErrorCorrection;
	struct SFVec3f body1AnchorPoint;
	struct SFVec3f body1Axis;
	struct SFVec3f body2AnchorPoint;
	struct SFVec3f body2Axis;
	void * _joint;
	int _forceout;
	struct SFVec3f __old_anchorPoint;
	struct SFVec3f __old_axis1;
	struct SFVec3f __old_axis2;
	struct X3D_Node *__old_body1;
	struct X3D_Node *__old_body2;
};
extern struct X3D_Virt virt_UniversalJoint;
/***********************/
struct X3D_UnlitMaterial {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct X3D_Node *metadata;
	struct SFColor emissiveColor;
	struct X3D_Node *emissiveTexture;
	int emissiveTextureChannel;
	struct X3D_Node *normalTexture;
	int normalTextureChannel;
	float transparency;
	void * _material;
};
extern struct X3D_Virt virt_UnlitMaterial;
/***********************/
struct X3D_Viewpoint {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int _layerId;
	int _donethispass;
	int set_bind;
	double bindTime;
	int isBound;
	struct Uni_String *description;
	int jump;
	float fieldOfView;
	struct X3D_Node *metadata;
	struct SFRotation orientation;
	struct SFVec3f position;
	struct SFVec3f centerOfRotation;
	int retainUserOffsets;
	struct Uni_String *fovMode;
	float aspectRatio;
	int _initializedOnce;
	struct SFRotation _orientation;
	struct SFVec3f _position;
};
extern struct X3D_Virt virt_Viewpoint;
/***********************/
struct X3D_ViewpointGroup {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	struct Multi_Node children;
	struct Uni_String *description;
	int displayed;
	struct X3D_Node *metadata;
	int retainUserOffsets;
	struct SFVec3f size;
	struct X3D_Node *__proxNode;
};
extern struct X3D_Virt virt_ViewpointGroup;
/***********************/
struct X3D_Viewport {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node addChildren;
	struct Multi_Node removeChildren;
	struct Multi_Node __sibAffectors;
	struct Multi_Node children;
	struct X3D_Node *metadata;
	struct Multi_Float clipBoundary;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
};
extern struct X3D_Virt virt_Viewport;
/***********************/
struct X3D_VisibilitySensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f center;
	int enabled;
	struct SFVec3f size;
	double enterTime;
	double exitTime;
	int isActive;
	struct X3D_Node *metadata;
	int __visible;
	int __occludeCheckCount;
	struct Multi_Vec3f __points;
	int __Samples;
	int __oldEnabled;
};
extern struct X3D_Virt virt_VisibilitySensor;
/***********************/
struct X3D_VolumeData {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f dimensions;
	struct X3D_Node *metadata;
	struct X3D_Node *voxels;
	struct SFVec3f bboxCenter;
	struct SFVec3f bboxSize;
	int visible;
	int bboxDisplay;
	void * _boxtris;
	struct X3D_Node *renderStyle;
};
extern struct X3D_Virt virt_VolumeData;
/***********************/
struct X3D_VolumeEmitter {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Int32 set_coordIndex;
	int set_coordinate;
	struct X3D_Node *coord;
	struct SFVec3f direction;
	struct X3D_Node *metadata;
	float speed;
	float variation;
	struct Multi_Int32 coordIndex;
	int internal;
	float mass;
	float surfaceArea;
	void * _ifs;
};
extern struct X3D_Virt virt_VolumeEmitter;
/***********************/
struct X3D_VolumePickSensor {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	int enabled;
	struct X3D_Node *metadata;
	struct Multi_String objectType;
	struct X3D_Node *pickingGeometry;
	struct Multi_Node pickTarget;
	int isActive;
	struct Multi_Node pickedGeometry;
	struct Uni_String *intersectionType;
	struct Uni_String *sortOrder;
	struct Uni_String *matchCriterion;
	int __oldEnabled;
};
extern struct X3D_Virt virt_VolumePickSensor;
/***********************/
struct X3D_WaveShaperNode {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_Node connect;
	int curve;
	struct Uni_String *oversample;
};
extern struct X3D_Virt virt_WaveShaperNode;
/***********************/
struct X3D_WindPhysicsModel {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct SFVec3f direction;
	int enabled;
	float gustiness;
	struct X3D_Node *metadata;
	float speed;
	float turbulence;
	float _frameSpeed;
};
extern struct X3D_Virt virt_WindPhysicsModel;
/***********************/
struct X3D_WorldInfo {
       int _nodeType; /* unique integer for each type */ 
       int _renderFlags; /*sensitive, etc */ 
       int _hit; 
       int _change; 
       int _ichange; 
       struct Vector* _parentVector; 
       double _dist; /*sorting for blending */ 
       float _extent[6]; /* used for boundingboxes - +-x, +-y, +-z */ 
       struct X3D_PolyRep *_intern; 
       int referenceCount; /* if this reaches zero, nobody wants it anymore */ 
       int _defaultContainer; /* holds the container */
       void* _gc; /* ptr to vector of ptrs to free */
       struct X3D_Node* _executionContext; /* scene or protoInstance */
 	/*** node specific data: *****/
	struct Multi_String info;
	struct Uni_String *title;
	struct X3D_Node *metadata;
};
extern struct X3D_Virt virt_WorldInfo;
extern struct X3D_Virt* virtTable[];

extern const int *NODE_OFFSETS[];

int getSAI_X3DNodeType (int FreeWRLNodeType);

#endif /* __FREEWRL_STRUCTS_H__ */
